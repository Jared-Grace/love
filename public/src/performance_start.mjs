import { performance_next } from "../../../love/public/src/performance_next.mjs";
export function performance_start() {
  const name = "start";
  let measurements = [];
  const p = performance_next(p, name);
  return p;
}
