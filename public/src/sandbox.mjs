import { repo_functions_move } from "../../../love/public/src/repo_functions_move.mjs";
export async function sandbox() {
  let v = await repo_functions_move(f_names, repo_name_to);
  ("emoji_no", "text_alphabet_includes", "l");
}
