import { app_replace_rule_sets_fns_names_others } from "../../../love/public/src/app_replace_rule_sets_fns_names_others.mjs";
import { folder_previous_join } from "../../../love/public/src/folder_previous_join.mjs";
export async function sandbox() {
  let filtered = await app_replace_rule_sets_fns_names_others();
  return filtered;
  let joined = folder_previous_join(path);
}
