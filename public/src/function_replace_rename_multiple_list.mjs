import { function_replace_rename_multiple_generic } from "../../../love/public/src/function_replace_rename_multiple_generic.mjs";
export async function function_replace_rename_multiple_list(f_names, from, to) {
  return await function_replace_rename_multiple_generic(f_names, from, to);
}
