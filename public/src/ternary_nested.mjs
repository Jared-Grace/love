import { ternary } from "../../../love/public/src/ternary.mjs";
export function ternary_nested(condition, on_true, on_false) {
  let result = ternary(condition, on_true, on_false);
  return result;
}
