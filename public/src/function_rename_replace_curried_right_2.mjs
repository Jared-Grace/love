import { function_rename_replace } from "../../../love/public/src/function_rename_replace.mjs";
export function function_rename_replace_curried_right_2(from, to) {
  let r2 = async function function_rename_replace_curried_right_2_result(
    f_name_before,
  ) {
    let r = await function_rename_replace(f_name_before, from, to);
    return r;
  };
  return r2;
}
