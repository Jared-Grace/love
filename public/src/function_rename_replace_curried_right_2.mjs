import { function_rename_replace } from "../../../love/public/src/function_rename_replace.mjs";
export async function function_rename_replace_curried_right_2(from, to) {
  return async function function_rename_replace_curried_right_2_result(
    f_name_before,
  ) {
    return await function_rename_replace(f_name_before, from, to);
  };
}
