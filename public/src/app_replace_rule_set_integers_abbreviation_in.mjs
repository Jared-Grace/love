import { object_merge } from "../../../love/public/src/object_merge.mjs";
export function app_replace_rule_set_integers_abbreviation_in(i) {
  object_merge(i, {
    i: ["", "i", "nteger (non-negative whole number)"],
  });
}
