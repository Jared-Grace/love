{
  "comment": [
    "Regression corpus for .claude/hooks/bash-command-guard.py, checked through",
    "the real hook via guard_check (see reference_guard_check_tester memory).",
    "Every case uses `ls` / `git status` as the trusted inner verb, so the",
    "expected verdicts test the guard's STRUCTURE, not this project's allow",
    "list. Removing `ls` from permissions.allow would invalidate the corpus.",
    "decision: allow = auto-approved, deny = hard floor, ask = guard actively",
    "prompts with a reason, silent = guard abstains so the native permission",
    "engine decides (silent is NOT the same as 'will not prompt').",
    "A case may also carry reason_includes: a substring the guard's own",
    "explanation must contain. That text is how a reader learns the right",
    "move (reword this / use the sandboxed form), so it is protected too.",
    "MACHINE-DEPENDENT: the rm cases hard-code the scratchpad prefix, which",
    "the hook derives as /tmp/claude-<uid>/<repo-root-with-dashes>/. On a",
    "different uid or checkout path those three cases go red for an",
    "environmental reason, not a real regression - update the paths."
  ],
  "cases": [
    { "command": "ls", "decision": "allow", "reason_includes": "already in permissions.allow", "why": "trusted verb, bare - and the allow says which rule carried it" },
    { "command": "ls && ls", "decision": "allow", "why": "chain of trusted verbs" },
    { "command": "ls | tail -4", "decision": "allow", "why": "pipe of trusted verbs" },
    { "command": "FOO=bar ls", "decision": "allow", "why": "inert assignment is stripped" },
    { "command": "PATH=/tmp ls", "decision": "silent", "why": "execution-influencing assignment fails closed" },

    { "command": "time (timeout 900 node scripts/ai.mjs qa_gate_run > /dev/null 2>&1)", "decision": "deny", "reason_includes": "Drop the parentheses", "why": "the parentheses are pure grouping and every verb inside is trusted, so the human was being asked to approve a command two characters from an approved one. Deny naming the stripped form costs no human click - the caller reads it and reissues" },
    { "command": "time node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "the form the deny points at" },
    { "command": "time (cd /tmp && ls)", "decision": "silent", "why": "a cd OUTLIVES the command once the parentheses go, and the working directory persists between calls - the subshell is load-bearing, so this keeps prompting rather than being rewritten" },
    { "command": "time (SP=/tmp; ls)", "decision": "silent", "why": "an assignment is contained by the subshell the same way; excluded for the same reason" },
    { "command": "( time node scripts/ai.mjs qa_gate_run ) 2>&1 | grep real", "decision": "silent", "why": "here the parentheses decide WHOSE stderr the redirect catches - stripping them would silently measure something else, so only the whole-command `time ( ... )` shape is offered" },
    { "command": "time (rm -rf /home/j)", "decision": "silent", "why": "the stripped form must itself pass before the deny is offered; a deny that names a command the guard would also refuse is worse than the prompt it replaced" },
    { "command": "time (ls; (echo x))", "decision": "silent", "why": "a nested subshell is not the simple grouping this rewrites" },
    { "command": "NODE_OPTIONS=\"--require /tmp/evil.js\" node scripts/ai.mjs qa_gate_run", "decision": "silent", "why": "REGRESSION 2026-07-27: this was ALLOW. An interpreter's options variable names code to load before the script runs, so it is LD_PRELOAD one level up - and it rode in on the granted function's trust, which never got to run at all. The most granted verb in the repo made it the sharpest version of the hole" },
    { "command": "NODE_OPTIONS='--import file:///tmp/evil.mjs' node scripts/ai.mjs apps_names", "decision": "silent", "why": "--import is the same injection as --require through the ESM loader" },
    { "command": "NODE_OPTIONS=--cpu-prof node scripts/ai.mjs apps_names", "decision": "silent", "why": "a benign value is still refused - the guard reads the NAME, not what today's value happens to say, because a value it had to judge is a value it could judge wrongly" },
    { "command": "PYTHONSTARTUP=/tmp/evil.py python3 x.py", "decision": "silent", "why": "the other interpreters are covered for the same reason; being second-choice here is not a safety property" },
    { "command": "NODE_ENV=production node scripts/ai.mjs apps_names", "decision": "allow", "why": "the boundary: NODE_ENV is an everyday data assignment, which is why the dangerous names are listed explicitly rather than by a 'NODE_' prefix" },

    { "command": "time ls", "decision": "allow", "why": "time unwraps to inner verb" },
    { "command": "time git status", "decision": "allow", "why": "time unwraps to a 2-word verb" },
    { "command": "timeout 30 ls", "decision": "allow", "why": "timeout <dur> unwraps" },
    { "command": "xargs ls", "decision": "allow", "why": "flagless xargs unwraps" },
    { "command": "time timeout 30 ls", "decision": "allow", "why": "wrappers stack" },

    { "command": "time -p ls", "decision": "silent", "why": "flag form is not the transparent shape" },
    { "command": "timeout -k 5 30 ls", "decision": "silent", "why": "flag form is not the transparent shape" },
    { "command": "xargs -I{} ls", "decision": "silent", "why": "flag form is not the transparent shape" },
    { "command": "time", "decision": "silent", "why": "wrapper with no inner command" },

    { "command": "time rm -rf /", "decision": "silent", "why": "wrapper launders no trust" },
    { "command": "timeout 30 rm -rf /", "decision": "silent", "why": "wrapper launders no trust" },
    { "command": "time notaverb_xyz", "decision": "silent", "why": "unknown inner verb still prompts" },

    { "command": "node -e 1", "decision": "deny", "reason_includes": "unshare --net --map-root-user", "why": "the deny must hand back the sandboxed alternative, not just refuse" },
    { "command": "time node -e 1", "decision": "deny", "why": "hard floor survives a wrapper" },
    { "command": "python3 -c 'print(1)'", "decision": "deny", "reason_includes": "one seam wearing many names", "why": "python -c is arbitrary command-line code, floored like node -e so it self-corrects instead of prompting the human" },
    { "command": "python -c 'x'", "decision": "deny", "reason_includes": "none is ever auto-trusted", "why": "python2/python spelling caught alongside python3" },
    { "command": "cat x | python3 -c 'import sys'", "decision": "deny", "reason_includes": "Edit tool", "why": "python -c denied in any pipe segment, not just at the start - and the reason leads with the Edit tool, since rewriting a file is what the scripted forms keep being reached for" },
    { "command": "sh -c 'rm -rf /tmp/x'", "decision": "deny", "reason_includes": "narrowed to a safe shape", "why": "a shell given -c writes a command line the guard never sees, the widest arbitrary-execution seam of all - floored, not left to prompt" },
    { "command": "bash -c 'echo hi'", "decision": "deny", "reason_includes": "one seam wearing many names", "why": "harmless-looking code is still arbitrary code; the floor reads the shape, not the payload" },
    { "command": "timeout 5 bash -c 'ls'", "decision": "deny", "reason_includes": "one seam wearing many names", "why": "a timeout prefix is unwrapped before the floor looks, so it cannot launder a shell eval" },
    { "command": "perl -e 'print 1'", "decision": "deny", "reason_includes": "one seam wearing many names", "why": "enumerating interpreters is the point - a floor naming one program is a signpost to the next" },
    { "command": "ruby -e 'puts 1'", "decision": "deny", "reason_includes": "one seam wearing many names", "why": "same seam, third name" },
    { "command": "find . -exec sh -c ls {} \\;", "decision": "ask", "why": "a shell reached through find -exec is a different word position and keeps its existing answer - the floor catches a shell invoked as the command itself" },
    { "command": "grep 'sh -c' notes.txt", "decision": "allow", "why": "quote-aware: a shell eval quoted inside a grep pattern is literal text, so grep stays trusted" },

    { "command": "cat >> notes.md << 'EOF'\nhi\nEOF", "decision": "deny", "reason_includes": "use the Write tool", "why": "a heredoc append is the shell writing a file blind - Edit names the text it replaces and fails loudly when a peer moved it, which is what several Claudes in one working directory need" },
    { "command": "cat > new.txt << 'EOF'\nhi\nEOF", "decision": "deny", "reason_includes": "Edit tool", "why": "creating a file the same way is the same answer, and Write is the tool named for it" },
    { "command": "tee -a f.md << 'EOF'\nhi\nEOF", "decision": "deny", "reason_includes": "Edit tool", "why": "tee is the other spelling of the same redirect" },
    { "command": "cat << EOF > f.txt\nhi\nEOF", "decision": "deny", "reason_includes": "Edit tool", "why": "the redirect may follow the heredoc rather than precede it" },
    { "command": "cat << EOF", "decision": "ask", "why": "a heredoc with nowhere to land is not a file write and keeps its existing answer - both halves are required" },
    { "command": "grep 'cat >> f << EOF' notes.txt", "decision": "allow", "why": "quoted spans are blanked before the text match, so mentioning the shape is not performing it - the case that first fired this floor wrongly" },
    { "command": "ls > out.txt", "decision": "ask", "why": "redirecting a COMMAND's output to a file is untouched; only a heredoc, where the text is already in hand, is floored" },
    { "command": "python3 report.py", "decision": "silent", "why": "python on a script FILE is a different surface (the hook itself runs that way) - left alone, not floored" },
    { "command": "grep 'python3 -c' notes.txt", "decision": "allow", "why": "quote-aware: 'python3 -c' inside a grep pattern is literal text, not a python invocation, so grep stays trusted" },
    { "command": "python3 - <<PY\nprint(1)\nPY", "decision": "deny", "reason_includes": "standard input", "why": "a heredoc is -c with the code moved off the argument list - and it was a live bypass, since << is exactly what makes a command unparseable" },
    { "command": "node - <<JS\nconsole.log(1)\nJS", "decision": "deny", "reason_includes": "standard input", "why": "node reads its program from stdin the same way python does, so the same floor has to cover it" },
    { "command": "python3 <<< 'print(1)'", "decision": "deny", "reason_includes": "standard input", "why": "a herestring is the third spelling of the same thing" },
    { "command": "python3 -", "decision": "deny", "reason_includes": "standard input", "why": "a bare - in place of a script name is the explicit spelling, and it parses, so tokens catch it" },
    { "command": "grep 'python3 <<EOF' notes.txt", "decision": "allow", "why": "the text fallback is reachable only after the tokenizer gives up, so a quoted heredoc in a grep pattern still parses and stays trusted" },
    { "command": "ls -", "decision": "allow", "why": "a lone - is only an eval when the program reads one; ls keeps its trust" },
    { "command": "node scripts/r.mjs command_line_generic ls", "decision": "deny", "reason_includes": "denied even if allow-listed", "why": "the deny must explain that an allow rule cannot reopen the floor" },
    { "command": "node scripts/ai.mjs command_line_generic ls", "decision": "deny", "why": "the floor covers every dispatcher, not just r.mjs" },
    { "command": "node scripts/ai.mjs command_line_interactive 'echo x'", "decision": "deny", "reason_includes": "denied even if allow-listed", "why": "command_line_interactive spawnSyncs an arbitrary command from its arg - an eval seam, floored like command_line_generic" },
    { "command": "node scripts/ai.mjs command_line_code_ignore 'rm -rf /'", "decision": "deny", "why": "command_line_code_ignore forwards a command to command_line_generic - direct invocation is denied" },
    { "command": "node scripts/ai.mjs command_line_folder /tmp 'rm -rf /'", "decision": "deny", "why": "command_line_folder runs a command from its arg in a folder - eval seam" },
    { "command": "node scripts/ai.mjs command_line_stdout x", "decision": "silent", "why": "command_line_stdout does not run a command from its arg (text-only), so it is NOT floored - the deny set is scoped to the runners, not the whole command_line_ prefix" },
    { "command": "node scripts/g.mjs command_line_generic ls", "decision": "deny", "why": "the floor covers every dispatcher, not just r.mjs" },
    { "command": "node scripts/rl.mjs eval_console_log_replace x", "decision": "deny", "why": "the floor covers every dispatcher, not just r.mjs" },
    { "command": "node /home/j/repos/love/scripts/r.mjs command_line_generic ls", "decision": "deny", "why": "an absolute dispatcher path does not route around the floor" },

    { "command": "node scripts/r.mjs firebase_deploy", "decision": "deny", "reason_includes": "scripts/ai.mjs", "why": "Claude uses ai.mjs; r.mjs is the human's seam, and the deny points there" },
    { "command": "node scripts/g.mjs ai_git", "decision": "deny", "why": "g.mjs is a non-ai dispatcher, denied for Claude" },
    { "command": "node scripts/rl.mjs qa_gate_run", "decision": "deny", "why": "rl.mjs is a non-ai dispatcher, denied for Claude" },
    { "command": "node scripts/r.mjs w", "decision": "deny", "why": "even a bare shorthand start of a non-ai dispatcher is denied" },
    { "command": "node /home/j/repos/love/scripts/g.mjs firebase_deploy", "decision": "deny", "why": "absolute path to a non-ai dispatcher is still denied" },
    { "command": "node scripts/ai.mjs firebase_deploy", "decision": "allow", "why": "ai.mjs is the sanctioned seam and firebase_deploy is allow-listed for it" },
    { "command": "node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "the non-ai deny must not touch ai.mjs itself" },

    { "command": "node /home/j/repos/love/scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "this repo's own absolute path to ai.mjs folds onto the relative spelling, so the one generated rule covers both. Without the fold a rule is literal text and every absolute-path call prompted" },
    { "command": "timeout 300 node /home/j/repos/love/scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "timeout unwraps and the absolute path folds - the two wrappers compose" },
    { "command": "node /home/j/repos/love/scripts/ai.mjs ai_git_command_args function_rename a,b", "decision": "allow", "why": "the fold happens before the four-word fn-named fold, so a per-function command rule matches the absolute spelling too" },
    { "command": "node /home/j/other/scripts/ai.mjs qa_gate_run", "decision": "silent", "why": "the fold is pinned to THIS repo root - another checkout's ai.mjs runs functions the human never granted, so it must not inherit the rule" },
    { "command": "node /home/j/repos/love/scripts/ai.mjs command_line_generic ls", "decision": "deny", "why": "the eval floor runs before the fold and is keyed on the function name, so spelling the path out cannot route around it" },
    { "command": "node scripts/p.mjs", "decision": "deny", "why": "the whole scripts/ dir is fenced to ai.mjs, not just the dispatchers" },
    { "command": "node scripts/u.mjs", "decision": "deny", "why": "a human utility under scripts/ is off-limits to Claude" },
    { "command": "node scripts/temp/probe.mjs", "decision": "deny", "reason_includes": "sandboxed unshare", "why": "unsandboxed temp is denied and pointed at the sandboxed form" },

    { "command": "ls $(ls)", "decision": "allow", "why": "substitution whose inner command is trusted" },
    { "command": "ls $(git status)", "decision": "allow", "why": "substitution with a trusted 2-word verb" },
    { "command": "ls $(ls $(ls))", "decision": "allow", "why": "nested substitution, trusted throughout" },
    { "command": "ls $(ls) && ls", "decision": "allow", "why": "substitution inside a chain" },
    { "command": "FOO=$(ls) ls", "decision": "allow", "why": "substitution in an inert assignment" },
    { "command": "ls $(rm -rf /)", "decision": "ask", "why": "untrusted command hiding inside a trusted-looking one" },
    { "command": "ls `ls`", "decision": "ask", "why": "backticks are not parsed as substitution, so they stay unapproved" },

    { "command": "ls \"$(ls)\"", "decision": "allow", "why": "double quotes do not stop substitution being evaluated" },
    { "command": "ls \"$(rm -rf /)\"", "decision": "ask", "why": "the inner command is still validated inside double quotes" },
    { "command": "ls '$(rm -rf /)'", "decision": "allow", "why": "single quotes suppress substitution, so this is an inert argument" },
    { "command": "ls 'a;rm -rf /'", "decision": "allow", "why": "a quoted semicolon is data, not a statement separator" },
    { "command": "echo x | tr ';' 'n'", "decision": "allow", "why": "a quoted separator char whose whole value is ';' is a word (an argument to tr), not a real statement separator - the separator-sentinel change stops it being mis-split" },
    { "command": "echo x | tr '|' ','", "decision": "allow", "why": "same for a quoted pipe char passed as an argument" },
    { "command": "ls; rm -rf /", "decision": "ask", "why": "a REAL unquoted ; still splits into statements, so the untrusted rm second statement is caught - the sentinel change must not weaken this" },
    { "command": "find . -exec ls {} \\;", "decision": "allow", "why": "-exec's ';' terminator (now surviving as a token) is dropped after {} and the exec verb ls is trusted" },
    { "command": "find . -exec rm {} \\;", "decision": "ask", "why": "-exec running an untrusted verb (rm) is refused even with the terminator handled" },
    { "command": "ls \"; rm -rf /\"", "decision": "allow", "why": "same, in double quotes" },
    { "command": "ls \"\\$(rm -rf /)\"", "decision": "allow", "why": "backslash-escaped dollar inside double quotes is literal text" },
    { "command": "ls \\$(rm -rf /)", "decision": "ask", "why": "unquoted, the backslash escapes only the dollar - the parens still open a statement" },
    { "command": "ls \"a b\"", "decision": "allow", "why": "quoted argument with a space stays one word" },

    { "command": "wc -c < public/examples.js", "decision": "allow", "why": "input redirection from a file is read-only - equivalent to passing the path as an arg, which wc already allows" },
    { "command": "sort < a.txt | head", "decision": "allow", "why": "input redirect works inside a pipe of trusted verbs" },
    { "command": "printf \"%d\" \"$(wc -c < public/examples.js)\"", "decision": "allow", "why": "a subshell whose inner command uses an input redirect now validates - the < is dropped and wc is trusted" },
    { "command": "wc -c < $(rm -rf /)", "decision": "ask", "why": "a command substitution in the redirect target must NOT be treated as a plain path - the metacharacter charset rejects it, so it stays unapproved" },
    { "command": "S=/tmp/claude-1000/-home-j-repos-love; tr ',' '\\n' < $S/a.txt > $S/b.txt", "decision": "allow", "why": "a leading $VAR assigned a literal path earlier in the same command resolves on the INPUT redirect as it already did on the output one - reading is safer than the writing that was already allowed" },
    { "command": "wc -c < $UNSET_VAR/a.txt", "decision": "ask", "why": "an unresolved $VAR input target still fails the plain-path charset - resolution only ever adds recognition, it never lets a $ through" },
    { "command": "S=/tmp/claude-1000/-home-j-repos-love; echo \"count: $(wc -c < $S/a.txt)\"", "decision": "allow", "why": "a VAR assigned on the command line resolves INSIDE a $(...) too - the substitution is validated with the outer assignments in scope, so the same read is not rejected merely for being wrapped" },
    { "command": "echo \"count: $(wc -c < $UNSET_VAR/a.txt)\"", "decision": "ask", "why": "carrying outer assignments into a substitution must not make an unresolved $VAR pass there either" },
    { "command": "S=/etc; echo \"$(ls)\" > $S/passwd", "decision": "silent", "why": "resolving an outer VAR feeds the scratchpad check, never bypasses it - a non-scratchpad value stays unapproved" },
    { "command": "curl -s -o /dev/null -w \"%{http_code}\\n\" \"http://localhost:8080/love/public/dev/g_verify.html?chapter=MRK01\"", "decision": "allow", "why": "the exact 7-word status-probe template against this project's own dev server - a query string is part of how the dev pages are addressed and cannot widen a probe whose host is pinned to localhost and whose body goes to /dev/null" },
    { "command": "curl -s -o /dev/null -w \"%{http_code}\\n\" \"http://evil.com/x?a=1\"", "decision": "silent", "why": "the host is pinned to localhost/127.0.0.1 - allowing a query string must not turn the template into a general-purpose fetch tool" },
    { "command": "curl -s -o /dev/null -w \"%{http_code}\\n\" -T secrets.txt \"http://localhost:8080/x?a=1\"", "decision": "silent", "why": "the template is an exact word shape, so an added upload flag falls through to a prompt rather than being pattern-matched loosely" },
    { "command": "curl -s -o stolen.txt -w \"%{http_code}\\n\" \"http://localhost:8080/x\"", "decision": "silent", "why": "-o must be /dev/null verbatim, so the response body can never be written to a real file" },
    { "command": "cat <(rm -rf /)", "decision": "ask", "why": "process substitution runs a command - excluded from the input-redirect allowance" },
    { "command": "cat <> /tmp/claude-1000/-home-j-repos-love/x", "decision": "ask", "why": "<> opens the target for WRITE too, so it is not a read-only input redirect" },
    { "command": "cat << EOF", "decision": "ask", "why": "heredoc is not the plain-file input-redirect shape" },
    { "command": "cat < /dev/tcp/evil.com/443", "decision": "ask", "why": "/dev/tcp opens a NETWORK socket via bash redirect - not equivalent to a plain file arg, so excluded from the input-redirect allowance" },
    { "command": "wc -c < /dev/udp/1.2.3.4/53", "decision": "ask", "why": "/dev/udp is the same network-pseudo-device hole as /dev/tcp and stays blocked" },
    { "command": "echo \"$((5 - 3))\"", "decision": "allow", "why": "arithmetic expansion is pure integer math with no side effects - collapsed to an inert placeholder" },
    { "command": "echo \"$(( $(rm -rf /) + 1 ))\"", "decision": "ask", "why": "a command substitution nested inside arithmetic would run - the inert check refuses it rather than trusting the whole span" },
    { "command": "echo \"$(( a[$(rm -rf /)] ))\"", "decision": "ask", "why": "array-subscript command substitution inside arithmetic is refused (the $( in the body fails the inert check)" },
    { "command": "echo \"$(( a[i] ))\"", "decision": "ask", "why": "any array subscript in arithmetic is refused - bash re-evaluates a subscript as arithmetic, the indirect-injection vector, so [ is banned outright" },

    { "command": "ls 2>&1", "decision": "allow", "why": "fd duplication writes no file" },
    { "command": "ls > /tmp/x", "decision": "ask", "reason_includes": "prefer rewording into separate Bash calls", "why": "the ask must teach the reword, which is the whole point of asking instead of denying" },
    { "command": "ls >/dev/null", "decision": "allow", "why": "the glued /dev/null redirect writes nothing observable - always carved out" },
    { "command": "ls > /dev/null", "decision": "allow", "why": "a space before /dev/null is tolerated - same harmless redirect as the glued form (bash accepts the space)" },
    { "command": "ls 2> /dev/null", "decision": "allow", "why": "fd-prefixed /dev/null redirect with a space is the same discard" },
    { "command": "ls > /dev/null 2>&1", "decision": "allow", "why": "the standard `> /dev/null 2>&1` discard idiom - space before /dev/null, then a glued fd-dup" },
    { "command": "ls > /dev/nullX", "decision": "ask", "why": "not exactly /dev/null - the target must be followed by a separator, so /dev/nullX is a real file write" },
    { "command": "ls > /dev/null/x", "decision": "ask", "why": "writing INTO /dev/null/x is a real path, not the /dev/null discard" },
    { "command": "ls >> /home/j/.bashrc", "decision": "ask", "why": "append to a startup file" },
    { "command": "S=/tmp/claude-1000/-home-j-repos-love/s/scratchpad; ls > $S/b2.txt", "decision": "allow", "why": "a VAR=<literal scratchpad path> assignment lets the guard resolve a $VAR redirect target and confirm it lands in the scratchpad (MACHINE-DEPENDENT prefix, like the rm cases)" },
    { "command": "S=/tmp/claude-1000/-home-j-repos-love/s/scratchpad; ls > ${S}/b2.txt", "decision": "allow", "why": "${VAR} braces resolve the same as $VAR" },
    { "command": "S=/etc; ls > $S/passwd", "decision": "silent", "why": "resolving $S to a non-scratchpad path leaves it blocked - variable resolution feeds the scratchpad check, never bypasses it" },
    { "command": "S=$(echo /tmp); ls > $S/x", "decision": "silent", "why": "a value built from $(...) is not a literal, so $S is never captured or resolved" },
    { "command": "ls > $UNDEF/x", "decision": "ask", "why": "an unassigned variable is not in the literal map, so the $UNDEF/x target stays unresolved and unapproved" },

    { "command": "find . -type f", "decision": "allow", "why": "find with no exec surface" },
    { "command": "find . -exec ls {} \\;", "decision": "allow", "why": "-exec running a trusted verb" },
    { "command": "find . -exec rm {} \\;", "decision": "ask", "why": "-exec running an untrusted verb" },
    { "command": "find . -name x -delete", "decision": "ask", "why": "-delete is destructive without any exec" },

    { "command": "sed -n 's/a/b/' f.txt", "decision": "allow", "why": "the accepted sed shape: optional -n, one script, then filenames" },
    { "command": "sed 's/a/b/' f.txt", "decision": "allow", "why": "bare positional script is the other accepted form" },
    { "command": "sed -i 's/a/b/' f.txt", "decision": "silent", "why": "-i edits in place, outside the accepted shape" },
    { "command": "sed -f script.sed f.txt", "decision": "silent", "why": "-f hides the real script from the command string" },
    { "command": "sed -e 's/a/b/' -e 's/c/d/' f.txt", "decision": "silent", "why": "exactly one script is accepted, never a second -e" },

    { "command": "rm /tmp/claude-1000/-home-j-repos-love/x.txt", "decision": "allow", "why": "file-only rm inside the Claude scratchpad (path is uid+repo derived - see note)" },
    { "command": "rm -f /tmp/claude-1000/-home-j-repos-love/x.txt", "decision": "allow", "why": "-f is one of the two permitted flag chars" },
    { "command": "rm -r /tmp/claude-1000/-home-j-repos-love/d", "decision": "silent", "why": "recursion is refused even inside the scratchpad - and now falls through as silent rather than ask, because the Bash(rm -r <scratchpad>:*) allow rule that used to supply a matched leading verb was removed: it promised exactly what this case refuses" },
    { "command": "rm /tmp/other/x.txt", "decision": "silent", "why": "outside the scratchpad, rm is never a trusted verb" },

    { "command": "mkdir -p /tmp/claude-1000/-home-j-repos-love/scratchpad/sub", "decision": "allow", "why": "creating a dir inside the Claude scratchpad destroys nothing; the multi-word allow rule for this could never match through verb_of, so a helper earns it by path" },
    { "command": "mkdir /tmp/claude-1000/-home-j-repos-love/d", "decision": "allow", "why": "no flag is fine too - the path is what earns trust" },
    { "command": "mkdir -m 777 /tmp/claude-1000/-home-j-repos-love/d", "decision": "silent", "why": "-m takes a mode value and is outside the 'p'/'v' flag allowlist, so it fails closed" },
    { "command": "mkdir -p /tmp/claude-1000/-home-j-repos-love/../escape", "decision": "ask", "why": "'..' traversal out of the scratchpad fails normpath-equality" },
    { "command": "mkdir -p /etc/evil", "decision": "silent", "why": "outside the scratchpad, mkdir is never a trusted verb" },

    { "command": "mv /tmp/claude-1000/-home-j-repos-love/a /tmp/claude-1000/-home-j-repos-love/b", "decision": "allow", "why": "both ends inside the scratchpad, so the move is contained" },
    { "command": "mv /tmp/claude-1000/-home-j-repos-love/a /etc/cron.d/x", "decision": "ask", "why": "destination outside the scratchpad would plant a file anywhere - both ends are checked, not just the source" },
    { "command": "mv /home/j/repos/love/js/bible.mjs /tmp/claude-1000/-home-j-repos-love/b", "decision": "silent", "why": "source outside the scratchpad would quietly remove real repo source" },
    { "command": "mv /tmp/claude-1000/-home-j-repos-love/a", "decision": "ask", "why": "a single path is not a move; at least two non-flag arguments are required" },
    { "command": "mv -t /etc /tmp/claude-1000/-home-j-repos-love/a", "decision": "silent", "why": "-t carries the destination as a flag value, escaping the positional check, so it fails closed" },

    { "command": "mount", "decision": "allow", "why": "bare mount only lists current mounts" },
    { "command": "mount /dev/sda1 /mnt", "decision": "silent", "why": "mount with arguments actually mounts" },

    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos/love -e 'ls'", "decision": "allow", "why": "the sanctioned sandboxed node eval from CLAUDE.md" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos/love scripts/temp/x.mjs", "decision": "allow", "why": "the sanctioned sandboxed node script form" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/ -e 'ls'", "decision": "deny", "reason_includes": "sanctioned shape", "why": "sandbox shape but read scope widened past the repo: near-miss deny hands Claude the canonical form instead of prompting the human" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos -e 'ls'", "decision": "deny", "reason_includes": "sanctioned shape", "why": "eval form pointed at the repo's PARENT dir - near-miss deny" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos scripts/temp/x.mjs", "decision": "deny", "reason_includes": "sanctioned shape", "why": "script form pointed at the repo's PARENT dir - near-miss deny" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos /home/j/repos/love/scripts/temp/menu_preview.mjs", "decision": "deny", "reason_includes": "RELATIVE", "why": "the real case: BOTH a parent read path AND an ABSOLUTE script path - the generalized near-miss catches it and shows the canonical relative form" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos/love /home/j/repos/love/scripts/temp/x.mjs", "decision": "deny", "reason_includes": "sanctioned shape", "why": "read path is CORRECT but the script path is absolute - near-miss still fires on the script-path deviation alone" },
    { "command": "unshare --net --map-root-user -- node --permission --allow-fs-read=/home/j/repos/love/.. -e 'ls'", "decision": "deny", "reason_includes": "sanctioned shape", "why": "non-normalized path is now caught by the generalized near-miss (any non-sanctioned sandbox-prefix attempt is advised, not silently prompted)" },
    { "command": "unshare --map-root-user -- node --permission --allow-fs-read=/home/j/repos/love -e 'ls'", "decision": "silent", "why": "no --net, so the network is not actually blocked" },
    { "command": "node --permission --allow-fs-read=/home/j/repos/love -e 'ls'", "decision": "deny", "why": "unsandboxed node -e hits the hard floor even with node's own flags" },

    { "command": "git commit -m ai", "decision": "deny", "reason_includes": "ai_git", "why": "raw git commit is refused and redirected to the sanctioned commit fn, so Claude self-corrects instead of prompting the human" },
    { "command": "git add -A", "decision": "deny", "reason_includes": "git_ac_call_folder_try", "why": "raw git add (the commit-flow prelude) redirected to the sanctioned fn too" },
    { "command": "cd /tmp && git add -A && git commit -m ai 2>&1 | tail -2", "decision": "deny", "reason_includes": "ai_git", "why": "the real pasted shape: cd + add + commit + redirect + pipe all parse, and the git-write statement triggers the redirect deny" },
    { "command": "ls && git commit -m x", "decision": "deny", "reason_includes": "never raw git", "why": "a trusted leading verb cannot launder a raw git commit later in the chain - the floor beats the allow" },
    { "command": "grep 'git commit' MEMORY.md", "decision": "allow", "why": "quote-aware: 'git commit' inside a grep pattern is literal text, not a git command, so grep stays trusted" },

    { "command": "git checkout .claude/settings.json && echo reverted", "decision": "deny", "reason_includes": "working directory is shared", "why": "the real pasted shape - a peer reverting a file to re-test discards every other Claude's uncommitted edit to it, and a human approving the prompt cannot see what was in flight" },
    { "command": "git checkout -- js/foo.mjs", "decision": "deny", "reason_includes": "Edit tool", "why": "the redirect names the only undo that touches just your own change" },
    { "command": "git restore f.txt", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "restore is checkout's newer spelling and discards the same way" },
    { "command": "git reset --hard origin/main", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "a hard reset takes the whole shared tree with it" },
    { "command": "git clean -fd", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "clean deletes peers' untracked files, which are not even recoverable from the index" },
    { "command": "git stash", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "a bare stash sweeps the whole working tree, peers' edits included" },
    { "command": "git -C /home/j/repos/love checkout .", "decision": "deny", "reason_includes": "working directory is shared", "why": "the -C form discards peers' work exactly as the bare one does - it only changes where it is typed from, so the exemption that protects an exact -C grant does not apply to discarding" },
    { "command": "git -C /home/j/repos/love/ reset --hard", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "a trailing slash is the same directory" },
    { "command": "git -C /dev/shm/love_qa/repos/love checkout --detach HEAD", "decision": "allow", "reason_includes": "moves no branch", "why": "the repo-root floor does not reach a scratch worktree, and this exact shape is auto-approved on its own merits" },
    { "command": "git -C /home/j/repos/love commit -m x", "decision": "silent", "why": "the commit floor still spares every -C form, so an exact `git -C ...` rule the human granted is never overridden" },
    { "command": "git -C /home/j/repos/love status", "decision": "allow", "why": "reading is untouched" },
    { "command": "git -C /dev/shm/love_qa/repos/love checkout --detach 4be5407e36", "decision": "allow", "reason_includes": "moves no branch", "why": "advancing a scratch snapshot recurs every gate run, and detaching moves no branch, so the only state overwritten belongs to that worktree" },
    { "command": "git -C /dev/shm/love_qa/repos/love checkout main", "decision": "silent", "why": "a BRANCH checkout is not the allowed shape - a worktree's .git is the real repository, so tmpfs says nothing about what it can reach; --detach is what makes it safe, not the path" },
    { "command": "git -C /dev/shm/love_qa/repos/love commit -m x", "decision": "silent", "why": "same reason - a commit from tmpfs writes objects everyone else shares" },
    { "command": "git -C /home/j/repos/love checkout --detach HEAD", "decision": "deny", "reason_includes": "throws away uncommitted work", "why": "the same shape aimed at the real working directory stays floored; the allow is keyed to /dev/shm" },
    { "command": "git -C /dev/shm/x/love checkout --detach HEAD; rm -rf /", "decision": "silent", "why": "the allow requires the whole command to be that one statement, so nothing can ride along behind it" },
    { "command": "git stash list", "decision": "allow", "why": "list only reads, and it is already granted - a floor runs before the allow decision, so it has to spare the reading spelling or it silently revokes the human's rule" },
    { "command": "git diff --stat", "decision": "allow", "why": "reading what changed is untouched by the discard floor" },
    { "command": "grep 'git checkout' notes.txt", "decision": "allow", "why": "quote-aware: the shape mentioned inside a pattern is not the shape performed" },

    { "command": "cd /home/j/repos/love; node scripts/ai.mjs list_add | head -3", "decision": "deny", "reason_includes": "separate Bash calls", "why": "one untrusted piece among trusted ones splits cleanly, so Claude is told to split rather than the human asked about the whole chain. The ungranted piece must name a LIVE function: a made-up name is denied earlier by the dead-name floor and never reaches this branch" },
    { "command": "ls; git status; node scripts/ai.mjs list_add", "decision": "deny", "reason_includes": "needs its own call", "why": "the message names which piece is left, so the split is mechanical rather than a guess" },
    { "command": "cat f.txt; node scripts/ai.mjs list_add; node scripts/ai.mjs list_single", "decision": "ask", "why": "TWO untrusted pieces - splitting would turn one prompt into two, so the ask stands. This is the boundary the narrow condition exists to hold" },
    { "command": "ls; git status", "decision": "allow", "why": "an all-trusted chain never reaches the split branch" },
    { "command": "for f in a b; do node scripts/ai.mjs list_add $f; done", "decision": "silent", "why": "a ';' inside a loop separates parts of ONE command - shell structure words mean the command is left whole" },
    { "command": "ls | tail -4", "decision": "allow", "why": "a pipe is not a split point: its halves have no meaning apart" },
    { "command": "ls; rm -rf /", "decision": "ask", "why": "one untrusted piece, but not one this branch may recommend running alone - only a dispatcher call is proposed for splitting, so everything else keeps the answer it had" },
    { "command": "S=/etc; ls > $S/passwd", "decision": "silent", "why": "the second piece reads what the first set, so the pieces are not independent and splitting would not preserve them - a variable anywhere stops the proposal" },
    { "command": "git status", "decision": "allow", "why": "read-only git subcommand stays allowed - the floor catches only add/commit writes" },

    { "command": "ls -la f | awk '{print $5}'", "decision": "deny", "reason_includes": "never auto-trusted", "why": "awk in a pipe segment is refused (it can exec/write) and redirected to a trusted verb, so Claude self-corrects instead of prompting the human" },
    { "command": "awk '{print $1}' foo.txt", "decision": "deny", "reason_includes": "wc -c", "why": "bare awk denied too, and the reason names the trusted rewrite (wc/cut/grep -o)" },
    { "command": "grep 'awk' notes.txt", "decision": "allow", "why": "quote-aware: 'awk' inside a grep pattern is literal text, not the awk program, so grep stays trusted" },
    { "command": "gawk 'BEGIN{print 1}'", "decision": "deny", "reason_includes": "never auto-trusted", "why": "gawk/mawk/nawk variants are the same eval seam as awk and denied alike" },

    { "command": "for i in 1 2; do ls; done", "decision": "allow", "why": "for block with trusted body" },
    { "command": "if ls; then ls; fi", "decision": "allow", "why": "if block with trusted body" },
    { "command": "while true; do ls; done", "decision": "allow", "why": "while blocks are modelled like for/if" },
    { "command": "until false; do ls; done", "decision": "allow", "why": "until has the same shape as while" },
    { "command": "while ls; do ls; done", "decision": "allow", "why": "a real command as the loop condition" },
    { "command": "while rm -rf /; do ls; done", "decision": "silent", "why": "the CONDITION is verb-checked too - a trusted body cannot carry it" },
    { "command": "while true; do rm -rf /; done", "decision": "silent", "why": "untrusted body" },
    { "command": "while true; do for i in 1 2; do ls; done; done", "decision": "allow", "why": "for nests inside while" },
    { "command": "for i in 1 2; do while true; do ls; done; done", "decision": "allow", "why": "and while nests inside for" },
    { "command": "while true; do ls; done; rm -rf /", "decision": "silent", "why": "an untrusted statement after the loop still counts" },
    { "command": "true", "decision": "allow", "why": "inert builtin, like break/continue" },
    { "command": "case x in a) ls ;; esac", "decision": "silent", "why": "case stays unmodelled - its patterns need ) which tokenize rejects" },

    { "command": "rm -f /home/j/repos/love/scripts/temp/wrap_preview.mjs", "decision": "allow", "why": "file-only rm of a direct child of the gitignored scripts/temp throwaway dir" },
    { "command": "rm /home/j/repos/love/scripts/temp/probe.mjs", "decision": "allow", "why": "no flag is also fine - the path is what earns trust, not the flags" },
    { "command": "rm -rf /home/j/repos/love/scripts/temp/wrap_preview.mjs", "decision": "silent", "why": "recursion ('r') is refused even inside scripts/temp - no scripts/temp rm rule, so it falls through to a prompt" },
    { "command": "rm -f /home/j/repos/love/scripts/temp/../../js/bible.mjs", "decision": "silent", "why": "'..' traversal out of scripts/temp fails normpath-equality" },
    { "command": "rm -f /home/j/repos/love/scripts/temp/nested/x.mjs", "decision": "silent", "why": "only direct children of scripts/temp qualify, not a nested subdirectory" },
    { "command": "rm -f /home/j/repos/love/js/bible.mjs", "decision": "silent", "why": "outside scripts/temp, rm is never a trusted verb" },

    { "command": "rm -f scripts/temp/marker_defect_probe.mjs", "decision": "allow", "why": "the RELATIVE spelling is the one a command run from a repo root actually uses; requiring the absolute form bought a prompt and no safety, since scripts/temp is gitignored scratch in every repo the relative form could name" },
    { "command": "rm scripts/temp/probe.mjs", "decision": "allow", "why": "no flag is fine relatively too - the path earns the trust" },
    { "command": "rm -f scripts/temp/a.mjs scripts/temp/b.mjs", "decision": "allow", "why": "several throwaways at once, every one of them confined" },
    { "command": "rm -f scripts/temp/a.mjs js/bible.mjs", "decision": "silent", "why": "EVERY path must be confined - one real source file among the throwaways refuses the whole command" },
    { "command": "rm -rf scripts/temp/probe.mjs", "decision": "silent", "why": "recursion is refused on the relative spelling exactly as on the absolute one" },
    { "command": "rm -f scripts/temp/../../js/bible.mjs", "decision": "silent", "why": "normpath equality refuses traversal without needing to know the working directory" },
    { "command": "rm -f scripts/temp/nested/x.mjs", "decision": "silent", "why": "direct children only, relative spelling included" },
    { "command": "rm -f ./scripts/temp/probe.mjs", "decision": "silent", "why": "'./' is refused by normpath equality - the check demands an already-normalized path rather than normalizing one for you, so there is a single spelling to reason about" },
    { "command": "rm -f scripts/temp/", "decision": "silent", "why": "the directory itself is not a file, and removing it is not what the exception is for" },
    { "command": "rm -f scripts/temp/*.mjs", "decision": "allow", "why": "a glob with no '/' expands only inside the directory it sits in, so the pattern can name nothing the literal form could not - clearing throwaways had cost a prompt twice before this" },
    { "command": "rm -f scripts/temp/prof.mjs scripts/temp/*.cpuprofile", "decision": "allow", "why": "the real case: literal names and a pattern in one command, every one of them confined" },
    { "command": "rm -f scripts/temp/*", "decision": "allow", "why": "the whole folder's files; a subdirectory it matches still needs -r to remove, which is refused" },
    { "command": "rm -f /home/j/repos/love/scripts/temp/*.cpuprofile", "decision": "allow", "why": "patterns work on the absolute spelling too - the two are the same directory" },
    { "command": "rm -f scripts/temp/.*", "decision": "silent", "why": "the one exception to 'globs never match .. ' is a pattern starting with a dot, so this names the PARENT - refused here rather than left to rm's own refusal" },
    { "command": "rm -f scripts/temp/..*", "decision": "silent", "why": "same near-miss spelled the other way" },
    { "command": "rm -f scripts/temp/.hidden", "decision": "allow", "why": "a LITERAL dotfile is fine - only a dot-leading PATTERN is refused, so the tightening costs nothing real" },
    { "command": "rm -f scripts/temp/**", "decision": "silent", "why": "under globstar this descends into subdirectories; harmless in a throwaway folder, but it would make 'direct child only' quietly false, and an invariant nobody can trust is worse than a narrower rule" },
    { "command": "rm -f scripts/temp/*/x.mjs", "decision": "silent", "why": "a '/' after the pattern is what would let it reach further in, and '/' is not in the name class" },
    { "command": "rm -f scripts/temp/../js/*.mjs", "decision": "silent", "why": "traversal is refused whether or not a pattern follows it" },
    { "command": "rm -rf scripts/temp/*", "decision": "silent", "why": "recursion stays refused with a pattern exactly as with a literal name" },

    { "command": "/usr/bin/time -f \"%e s\" node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "a format string is printed, never executed, and /usr/bin/time is the spelling you need precisely BECAUSE you want -f - the shell builtin has no such flag" },
    { "command": "/usr/bin/time node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "no flags at all is the plain wrapper case" },
    { "command": "/usr/bin/time --format=%e node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "the glued long form is the same flag" },
    { "command": "/usr/bin/time -v node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "value-less flags are an explicit allowlist, same discipline as git's globals" },
    { "command": "/usr/bin/time -o /home/j/.bashrc node scripts/ai.mjs qa_gate_run", "decision": "silent", "why": "-o NAMES A FILE TO WRITE - a wrapper that can write a caller-named file is not transparent, it is a file write wearing a timer's name" },
    { "command": "/usr/bin/time -a -o out.txt node scripts/ai.mjs qa_gate_run", "decision": "silent", "why": "--append is refused with its output flag" },
    { "command": "/usr/bin/time -f %e node scripts/r.mjs firebase_deploy", "decision": "deny", "why": "the deny floor reads THROUGH the wrapper - wrapping a fenced dispatcher in a timer changes nothing" },
    { "command": "/usr/bin/time -f %e rm -rf /home/j", "decision": "silent", "why": "the inner command is what gets checked; the wrapper grants no trust of its own" },
    { "command": "/usr/bin/time -f %e", "decision": "silent", "why": "a wrapper with nothing to wrap is not a wrapper" },
    { "command": "aaa=..; rm -f /home/j/repos/love/scripts/temp/$aaa/file.mjs", "decision": "silent", "why": "the guard never expands variables - '$' is outside the path char-allowlist, so a var holding '..' can't smuggle traversal past the textual check" },
    { "command": "aaa=..; rm -f /home/j/repos/love/scripts/temp/${aaa}/file.mjs", "decision": "silent", "why": "braced ${...} expansion is refused for the same reason - '$', '{', '}' are not allowlisted path chars" },
    { "command": "rm -f /home/j/repos/love/scripts/temp/$(echo ..)/file.mjs", "decision": "silent", "why": "command substitution '$(...)' can't hide a '..' - '$', '(', ')' fail the path char-allowlist" },
    { "command": "rm -f /home/j/repos/love/scripts/temp/*", "decision": "allow", "why": "WAS silent on the reasoning that a glob fails the char-allowlist. Deliberately changed 2026-07-27 after clearing throwaways cost a prompt twice: the guard still never expands the pattern, it relies instead on the shell's own rule that a glob with no '/' cannot leave its directory - so the pattern names nothing the literal form could not" },
    { "command": "rm -f \"/home/j/repos/love/scripts/temp/../js/bible.mjs\"", "decision": "silent", "why": "quotes are stripped at tokenize, exposing the literal '..' which normpath-equality then rejects" },

    { "command": "node scripts/ai.mjs guard_corpus_dead_name_never_defined", "decision": "deny", "reason_includes": "No function named", "why": "a dispatcher call to a name no repo defines can only throw, so a prompt costs a click and still fails - deny instead, and let the agent loop fix the name" },
    { "command": "node scripts/ai.mjs functions_shadowing_baselin_report", "decision": "deny", "reason_includes": "Closest live names:", "why": "a dead name is nearly always a real one misremembered, so the deny has to carry the neighbours - a refusal with no correction just moves the dead end" },
    { "command": "node scripts/ai.mjs ao", "decision": "deny", "reason_includes": "is an alias key, not a function name", "why": "an alias reaching this seam has an EXACT fix, not a guess: name what it points at. ai.mjs refuses shorthand anyway, so this only converts a guaranteed throw into a redirect" },
    { "command": "node scripts/ai.mjs qa_gate_run", "decision": "allow", "why": "the dead-name floor must not touch a live, granted function - it fires only on names that resolve to no file" },
    { "command": "node scripts/ai.mjs $f", "decision": "silent", "why": "a name held in a variable is one the guard cannot resolve, so the floor abstains rather than guessing - unresolvable is not the same as dead" },
    { "command": "ls; node scripts/ai.mjs guard_corpus_dead_name_never_defined", "decision": "deny", "reason_includes": "No function named", "why": "every statement is checked, so a dead name does not gain cover by riding in a chain" },
    { "command": "node scripts/ai.mjs command_line_generic ls", "decision": "deny", "reason_includes": "arbitrary code/commands", "why": "floor ORDER: a live-but-dangerous function keeps the danger message. Safety outranks correctness when both apply" },
    { "command": "node scripts/r.mjs guard_corpus_dead_name_never_defined", "decision": "deny", "reason_includes": "Two seams", "why": "floor ORDER the other way: the wrong seam is the first thing wrong, so the dead name never gets to speak. Only ai.mjs calls are name-checked" },
    { "command": "grep 'node scripts/ai.mjs guard_corpus_dead_name_never_defined' notes.txt", "decision": "allow", "why": "quote-aware like every other floor: a dispatcher call quoted inside a search pattern is mentioned, not performed" }
  ]
}
