import { js_statements_span_made_below_assert } from "../../../js/js_statements_span_made_below_assert.mjs";
export const example = {
  kind: "rejection",
  title: "Refuse to cut out a run that reads a name the lines below it make",
  note: [
    "The judgement inside the cut, asked on its own. ",
    { fn: js_statements_span_made_below_assert.name },
    " is handed the run of lines about to become a function, and the lines ",
    "behind it, and refuses — because the run reads ",
    { code: "a" },
    " and nothing makes ",
    { code: "a" },
    " until after the run has ended.",
    " ",
    "That is legal where it stands, and only one thing makes it legal: the ",
    "read is written inside a function the run makes and does not call. By ",
    "the time a button or a timer reaches it the line below has run and the ",
    "name holds a value. The read is written inside the run and happens ",
    "outside it.",
    " ",
    "A cut ends that patience. The name becomes something the new function is ",
    "handed, and it is handed over at the call site — which stands above the ",
    "line that fills it. A read that was waiting becomes a read taken at ",
    "once, from nothing.",
    " ",
    "The mirror of the refusal on writing back into a name born above the ",
    "run, and the pair is one rule: a run may not reach backwards to write, ",
    "and may not reach forwards to read. Either way it is asking to be ",
    "somewhere other than where it is, and a cut can only move it to one ",
    "place.",
    " ",
    "Measured on the screen for editing a function. Its body was cut into ",
    "eight in a row, and the very first cut carried away both a name read ",
    "only inside a listener and a helper made below the run. Replayed against ",
    "this judgement afterwards, it names them both. What the chain left was a ",
    "set of pieces that could be put in no order at all — the last one makes ",
    "what the first one is handed — and the only thing that noticed was the ",
    "reading that asks which names nothing binds.",
    " ",
    "Checked directly rather than through the whole cut, so a failure here ",
    "names the judgement and not the arithmetic around it.",
  ],
  call: `${js_statements_span_made_below_assert.name}([function lambda8(la) { return work(la, a); }; let fds = list_adder_unique(lambda8);], [let a = { node: ast };], "app_a_function_key")`,
  expectText: `throws — the run reads a, which only the lines below it bring into being`,
  fn: js_statements_span_made_below_assert.name,
  args: [
    {
      parse: "statements",
      code: [
        "function lambda8(la) { return work(la, a); }",
        "let fds = list_adder_unique(lambda8);",
      ],
    },
    { parse: "statements", code: ["let a = { node: ast };"] },
    { parse: "value", value: "app_a_function_key" },
  ],
  expect: "throw",
};
