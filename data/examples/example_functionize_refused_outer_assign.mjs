import { js_statements_outer_assign_assert } from "../../js/js_statements_outer_assign_assert.mjs";
export const example = {
  kind: "rejection",
  title: "Refuse to cut out a run that moves a mark bound above it",
  note: [
    "The judgement inside the cut, asked on its own. ",
    { fn: js_statements_outer_assign_assert.name },
    " is handed the run of lines about to become a function and refuses, ",
    "because ",
    { code: "cursor" },
    " is given a new value here and was brought into being somewhere above. ",
    "Carried into a function of its own that write lands on a parameter — a ",
    "copy that dies with the call — so every line behind goes on reading the ",
    "value the mark held before the run.",
    " ",
    "This is the mirror of the question about what a run hands back, and the ",
    "two are near enough alike to be mistaken for each other. That one asks ",
    "which names the run brings into being that the lines behind still read. A ",
    "name never born in the run cannot be found by asking what the run gives ",
    "birth to, which is why the first question saw nothing here and a second ",
    "one had to be asked.",
    " ",
    "Every such write is refused, not only the ones that turn out to matter, ",
    "because whether it matters cannot be settled by reading the lines behind. ",
    "A run inside a loop reaches its own first line again on the next turn ",
    "round, so the reading is not behind the run at all — and that is the case ",
    "this was measured on. It cost nine of two hundred and forty three people ",
    "their place, and nothing went red.",
    " ",
    "Checked directly rather than through the whole cut, so a failure here ",
    "names the judgement and not the arithmetic around it.",
  ],
  call: `${js_statements_outer_assign_assert.name}([let r = taken(pool, cursor); cursor = property_get(r, "cursor");], "plant_filled")`,
  expectText: `throws — the run moves cursor, which was brought into being above it`,
  fn: js_statements_outer_assign_assert.name,
  args: [
    {
      parse: "statements",
      code: [
        "let r = taken(pool, cursor);",
        'cursor = property_get(r, "cursor");',
      ],
    },
    { parse: "value", value: "plant_filled" },
  ],
  expect: "throw",
};
