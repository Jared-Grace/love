import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_picture_note_parts() {
  "The parts of a lyric video's background picture a note can be filed against, one press each, so that saying where the fault is costs a watcher a press rather than a sentence.";
  "THESE FOUR ARE THE FAULTS THE PIPELINE ALREADY HAS MACHINERY FOR, which is the only evidence there is until somebody has watched a psalm through. Letters drawn into a picture is what the lettering detector was built to find; words that cannot be read against a picture is what the gamma and contrast fields on a picture exist to fix; a picture that shows what the verse does not say is what a description is rewritten for. Nothing here is a guess about a fault nobody has a remedy for.";
  "THE LIST SHOULD SHRINK RATHER THAN GROW. A part nobody ever files against is a press to read past on every picture of every psalm, and four presses is already most of what a phone screen has room for under a playing video. Whoever has watched a book through should take out whatever went unused.";
  "TIMING IS ONE OF THE FOUR EVEN THOUGH IT IS NOT ABOUT THE DRAWING. A picture that changes a line too early is the second commonest thing a watcher will see and it is not a fault in the picture at all - it is a fault in the moment the document hands the picture over. Without a press of its own it would be filed against the scene, where whoever redraws the picture would look for something that was never wrong with it. It is filed against the picture that starts, because that is the one that appeared too soon.";
  arguments_assert(arguments, 0);
  let names = ["lettering", "contrast", "scene", "timing"];
  return names;
}
