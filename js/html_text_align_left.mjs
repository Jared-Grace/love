import { html_text_align } from "./html_text_align.mjs";
export function html_text_align_left(component) {
  "line the text inside this component up against its left edge, which is worth saying out loud inside a centered parent: without it the centering reaches all the way down into the sentences";
  html_text_align(component, "left");
}
