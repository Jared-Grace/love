import { arguments_assert } from "./arguments_assert.mjs";
export function bless_crossing_width() {
  arguments_assert(arguments, 0);
  ("How many squares wide the one crossing on a block's road is.");
  ("TWO, and the number is the whole argument between a way across and a gap in a fence. One");
  ("square is a hole the width of a person, and a hole is what a reader steps through when");
  ("nothing else is open rather than a place anybody chose. Two squares is wide enough to");
  ("read as a thing that was built - somewhere two people pass each other, which is what a");
  ("crossing is for.");
  ("Not wider, because every square of it is a square somebody may step off the kerb into,");
  ("and the point of a crossing is that there are FEW such squares and they are all the same");
  ("one place. A crossing half the block wide is the road being open again with paint on it.");
  let width = 2;
  return width;
}
