export function files_to_commit_delete_probe() {
  "created only to be deleted, so the note of files to commit can be seen to record a removal as well as a write";
  let gone = "gone";
  return gone;
}
