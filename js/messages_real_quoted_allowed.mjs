import { arguments_assert } from "./arguments_assert.mjs";
export function messages_real_quoted_allowed() {
  arguments_assert(arguments, 0);
  ("The messages that arrived through the app which this repo may hold word for word although they are too long to be trivial - each one here because the repo is where it came from.");
  ("★ THIS IS NOT A LIST OF THINGS LET OFF. A message in it has to be one this repo wrote and sent to itself, and the proof of that is the only thing that puts a line here: the words are the repo's own, so holding them is holding what it already said rather than quoting a stranger. A message somebody else sent can never earn a line, however harmless it reads - that judgment is exactly the one this gate exists to stop being made case by case.");
  ("It is spelled out rather than worked out from the words, because nothing in the text of a message says who sent it. The mirror keeps what arrived and not why, so the one thing that could tell the app's own test apart from a person writing in is a record made by whoever knows - and a record of one line is cheaper than a rule that would have to guess.");
  let allowed = ["verses app: automated end-to-end test, please ignore"];
  return allowed;
}
