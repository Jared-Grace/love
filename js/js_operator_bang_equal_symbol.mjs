export function js_operator_bang_equal_symbol() {
  "the loose not-equals sign, which is to say a bang and one equals sign rather than two";
  "Held here rather than spelled where it is used, because two places want it now: the operator itself, and the class that says how strongly javascript works it out.";
  "No lesson teaches this sign - a learner is shown the two-sign form and only that - so it is here for the printer's sake and not the curriculum's.";
  let v = "!=";
  return v;
}
