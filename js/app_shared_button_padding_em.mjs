export function app_shared_button_padding_em() {
  "the room a button keeps around whatever is written on it, as a multiple of the size of that writing";
  "Said in ems so it grows with the writing rather than staying a fixed number of dots, which is what keeps a button the same shape on a phone as on a screen.";
  "It is asked for by name rather than written where it is used, because a second thing now has to be exactly this tall: a run of code standing in for a button has the button taken off it, and it can only look like the buttons above and below it if it is given back the room they keep. Two numbers meant to be equal, written out twice, are two numbers that end up unequal.";
  let v = "0.3";
  return v;
}
