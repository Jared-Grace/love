export function qa_gates_run_failed_suffix() {
  "what a run of the gates writes after the names in its closing summary, and what a reader taking those names back out strips off the end";
  "It is the other half of the words around the list, and it is spelled once here for the same reason the opening words are. A run builds that sentence and a reader pulls it apart again, so the two are one agreement wearing two faces - and an agreement spelled twice is one that can be half changed, after which the reader quietly hands back a name with a word still stuck to it.";
  let v = " failed";
  return v;
}
