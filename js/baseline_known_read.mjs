export async function baseline_known_read(path) {
  "What a ratchet file already held. Every gate that measures against what the repo already carried, rather than against zero, keeps its list under the same key in the same shape, so reading one is the same act whichever gate is asking.";
  "Four gates had written this out separately, alike to the character but for which function spelled the path. The name of the file is the only thing that distinguishes them, so the name of the file is the only thing that stays at the call.";
  let parsed = await file_read_json(path);
  let known = property_get(parsed, "known");
  return known;
}
