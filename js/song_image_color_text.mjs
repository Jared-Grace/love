import { arguments_assert } from "./arguments_assert.mjs";
export function song_image_color_text() {
  "the light gray the song-image tool writes its ordinary words in - a candidate's title, the symbol a couplet is being pictured for, the number on a button nobody has opened";
  "The tool's page is near black, so its plain text steps down from white rather than up from it. That is the whole difference between this and the grays the light pages use, and the reason it is named here rather than taken from those.";
  arguments_assert(arguments, 0);
  let c = "#bebebe";
  return c;
}
