export function function_open_suffix() {
  "The ending a fn name carries when showing the human a file is the point of it rather than something it might do along the way.";
  "It is the only signal in the repo for that, which is why a reachability question that cannot be settled by imports or by calls is settled by this. Both of those count a branch that is never taken, so each on its own grew a set of window openers until it covered most of the repo.";
  let suffix = "_open";
  return suffix;
}
