import { arguments_assert } from "./arguments_assert.mjs";
export function commits_message_rules_since() {
  "The commit from which what a commit message says is a failure rather than a fact about the past.";
  "NO RULE ABOUT MESSAGES COULD BE PUT TO THE WHOLE HISTORY AND STILL MEAN ANYTHING. Seven commits before this one are named after an alias key and two carry somebody's home folder out into a public log, and none of them can be edited without every commit id since being thrown away. A gate reading all of history would either carry nine grandfathered entries forever or be red forever. Naming a place instead lets it ratchet against nothing at all, which is the only setting a rule about what gets written today should ever be at.";
  "ONE PLACE ANSWERS FOR EVERY RULE ABOUT MESSAGES. The rules are separate readings because they catch separate faults, but they all became rules on the same day and a second place to move would be a second way to make a gate green without the commit being accounted for.";
  "IT IS THE HEAD OF THE DAY THE GATE WAS WRITTEN, 2026-08-22, and it should not be moved forward afterwards. Moving it is how a red gate gets made green without the offending commit being accounted for, which is the one thing this must not make easy.";
  arguments_assert(arguments, 0);
  let commit = "e5accf843f8f6566a5c1ffcd0937b3e8889a75f0";
  return commit;
}
