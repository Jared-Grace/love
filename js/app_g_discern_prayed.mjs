export function app_g_discern_prayed(discern) {
  "call when the player prays for discernment: they have been given it, so the Holy Spirit now keeps them from disregarding it — every non-discernment choice is blocked (force righteousness; God keeps His own, Jude 24)";
  discern.prayed = true;
}
