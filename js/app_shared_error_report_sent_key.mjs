import { text_frozen } from "./text_frozen.mjs";
export function app_shared_error_report_sent_key() {
  ("the word this device files its last sent report under, so a report that has already gone up is not sent again");
  ("What is kept is the report itself rather than a count or a time, because the only question worth asking before sending is whether anything has changed since last time - and a copy of what went answers that on its own, with nothing to keep in step.");
  ("Frozen for the same reason every other word filed in somebody's browser is: it is already written on disks this repo will never see again, and a word that followed a rename would send every device's whole history up a second time.");
  let key = text_frozen("sent");
  return key;
}
