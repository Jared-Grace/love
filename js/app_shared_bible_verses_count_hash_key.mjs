import { text_frozen } from "./text_frozen.mjs";
export function app_shared_bible_verses_count_hash_key() {
  "The word standing before how many verses a bible page shows at once, so a link somebody saved or sent still opens on the run of verses it was saved on.";
  "It is written as a word that must not change rather than as a plain one, unlike the chapter and verse words beside it. Those are single letters nothing will ever be named; this is an ordinary English word, so the day anything in this repo is called count the canonicalizing pass would read this as a reference to that function, and a link already sent would start following a rename.";
  let v = text_frozen("count");
  return v;
}
