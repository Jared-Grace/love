import { arguments_assert } from "./arguments_assert.mjs";
export function html_error_notice_button_texts() {
  arguments_assert(arguments, 0);
  ("the words on the one thing a person can do when the app could not start: load it again. most boots that die die once, so trying again is a real answer rather than a polite gesture.");
  ("written out in every language an app here speaks to its reader in, for the same reason the notice above it is: the page carrying it is built before anything is running, so the language is handed in rather than asked for.");
  let r = {
    en: "Try again",
    ur: "دوبارہ کوشش کریں",
    translated_from: {
      ur: "Try again",
    },
  };
  return r;
}
