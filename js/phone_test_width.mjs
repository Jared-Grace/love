export function phone_test_width() {
  "how wide a phone screen is taken to be when a page is checked at phone size - the narrow end of the phones people actually hold, so a page that fits this fits the roomier ones above it too";
  let v = 360;
  return v;
}
