export function app_code_lesson_chip_font_size() {
  "the size of the number written inside a coloured chip, a little under the text around it so the chip reads as a label on the sentence rather than as part of it";
  let v = "0.85em";
  return v;
}
