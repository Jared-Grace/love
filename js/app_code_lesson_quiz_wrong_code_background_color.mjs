export function app_code_lesson_quiz_wrong_code_background_color() {
  "the colour a choice written as code takes once the learner has pressed it and it turns out not to be the one";
  "Quieter than the red a choice made of words takes, because the block it goes on is far bigger. The same red that labels a small button covers a whole line here and reads as an alarm rather than as an answer. A wrong guess in a quiz is not a fault - it is how the learner finds out - and a colour that shouts turns guessing into being caught, which is the one thing the exercise needs them to go on doing.";
  "Muted and NOT faded. Half of a colour reads as the choice having been switched off rather than answered, which loses the thing the colour was there to say. Dark enough for the white lettering already in the block to stay readable, plainly red, and no louder than it has to be to be seen.";
  let r = "#a15252";
  return r;
}
