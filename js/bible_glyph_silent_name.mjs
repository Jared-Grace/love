import { arguments_assert } from "./arguments_assert.mjs";
export function bible_glyph_silent_name() {
  arguments_assert(arguments, 0);
  ("The name a picture table gives a part that is drawn as nothing at all - neither a picture nor its letters.");
  ("SILENT IS NOT UNDRAWN, and the difference is the whole reason this name exists. A part with no picture shows the word's letters, which marks it as work still to do. A silent part is finished: the reader loses nothing when it is left out, the way the Hebrew article is carried by the noun it sits on. So a table says silent on purpose and in its own words, rather than by leaving a key out, where a forgotten word and a deliberate one would look the same.");
  ("It is no picture's name, so nothing can ever draw it by mistake.");
  let r = "silent";
  return r;
}
