import { fn_name } from "./fn_name.mjs";
export function functions_rewrite_seams() {
  "The commands that write a function out again when they are told its name, rather than handed its tree. Calling one of these means a file changed on disk under a name the calling run may already be holding a loaded copy of.";
  "IT IS THE HALF OF A STALE READ THAT CAN BE SEEN WITHOUT WALKING ANYTHING. The reading built on this asks a much more expensive question - what else the same run goes on to call, and what those calls reach - and asking it of every function in the repo would be a walk per function. Importing one of these is the cheap test that says the question is worth asking at all.";
  "NAMED RATHER THAN DERIVED FROM WHAT REACHES THE WRITING SEAMS, and that is not laziness. Almost every command here reaches a writing seam sooner or later, so a derived list would name most of the repo. What separates these is that the thing rewritten arrives as a WORD - which is exactly the case where the run holding the old version has nothing to tell it the word now means something else.";
  "A register grows by being added to. A rewriting command missing from here makes the reading quieter rather than wrong, so add the name when a new one is written.";
  let f_name = fn_name("function_transform");
  let f_name2 = fn_name("function_transform_auto");
  let f_name3 = fn_name("function_transform_multiple");
  let f_name4 = fn_name("function_auto_checked");
  let f_name5 = fn_name("function_select_apply_args");
  let f_name6 = fn_name("function_select_apply_args_auto");
  let f_name7 = fn_name("function_select_multiple_apply_args");
  let f_name8 = fn_name("function_rename");
  let f_name9 = fn_name("function_prose_replace");
  let spelled = fn_name("function_span_call_existing");
  let spelled2 = fn_name("function_identifier_replace");
  let names = [
    f_name,
    f_name2,
    f_name3,
    f_name4,
    f_name5,
    f_name6,
    f_name7,
    f_name8,
    f_name9,
    spelled,
    spelled2,
  ];
  return names;
}
