export function web_assets_song_folder_name() {
  "The folder every picture belonging to a song lives in, under the assets folder.";
  "THE SONGS ARE PLURAL FROM THE START, even though only one of them has pictures today. A second song is already listed on the music page and more are intended, so a layout with no room for the song's name in it would have to be moved the day the second one is drawn - and moving it strands every address already handed out. The room costs one folder now and cannot be added later for free.";
  "It sits beside the pictures that belong to no song rather than inside them, because a song's picture is not one of those: it is chosen against a couplet and it changes when a different attempt is picked, where a picture in the general folder is placed once and stays.";
  let folder_name = "song";
  return folder_name;
}
