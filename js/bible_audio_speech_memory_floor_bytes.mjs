import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { gigabyte_bytes } from "./gigabyte_bytes.mjs";
import { multiply } from "./multiply.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function bible_audio_speech_memory_floor_bytes() {
  function_duplicate_kind_parallel();
  ("How little memory this machine may have left before a worker stops taking another chapter.");
  ("★ IT PROTECTS THE DESKTOP AND NOT THE RECORDING, WHICH IS WHY IT IS FOUR GIGABYTES AND NOT SOME SMALLER MARGIN. A worker about to start its next chapter already holds its own model, so what this reserve buys is not room for the recording but room for whatever else the person at this keyboard is doing. On 2026-08-28 the kernel chose its victims by score and took the browser three times and the editor once, none of which the recorder would have noticed; the person did.");
  ("★ IT IS THE WEAKER OF THE TWO MARKS AND MUST NOT BE READ ALONE. This figure counts pages that could be swapped out, so it goes on reporting room that exists only while there is somewhere to put them. Measured while that same machine was killing processes, it read seven gigabytes. The swap mark is the one that saw the wall coming.");
  arguments_assert(arguments, 0);
  let a_gigabyte = gigabyte_bytes();
  let bytes = multiply(4, a_gigabyte);
  return bytes;
}
