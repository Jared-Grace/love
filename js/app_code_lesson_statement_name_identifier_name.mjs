export function app_code_lesson_statement_name_identifier_name() {
  ("the name the cup is given on the screen that says a name can be any identifier");
  ("A word, because the whole of what this screen adds is that a name does not have to be the single letter every cup so far has been called. A second letter would show that a name can be some other letter.");
  let name = "joy";
  return name;
}
