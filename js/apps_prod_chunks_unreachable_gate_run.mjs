import { apps_prod_chunks_unreachable } from "./apps_prod_chunks_unreachable.mjs";
import { fn_name } from "./fn_name.mjs";
import { list_empty_is_assert_json } from "./list_empty_is_assert_json.mjs";
export async function apps_prod_chunks_unreachable_gate_run() {
  "Refuses while any app already being served sends for an extra script of its own that whoever is being served cannot get";
  "Its sibling asks the same thing of the folder an app is about to be sent out of, and says so itself: that folder is what will be served rather than what is being served, so it can be entirely in order in the same moment the thing people have in front of them is broken. This is the other half, and it is the only one whose green means a person can open the page";
  "The reading it stands on was written and then never called by anything, so the fault it names could be found by hand and could not be found by asking. A reading nobody runs is a reading that goes quietly wrong, and it is one line of work away from being a gate";
  "Deliberately not among the questions the whole-repo run asks. Measured at sixty-seven seconds against twenty-seven live apps, every second of it network, and the fault can only arrive when something is sent out - so it is asked at a sending rather than at every commit, where it would be paid for constantly and could tell nobody anything new";
  "What to do about it is to send the apps it names out again. The sibling's repair cannot help here: that one works out who is faulty by reading the folder, so where the folder is right and what went out is wrong it finds nobody and does nothing";
  let faulty = await apps_prod_chunks_unreachable();
  list_empty_is_assert_json(faulty, {
    faulty,
    hint: fn_name("qa_apps_commit_deploy_remembered"),
  });
  let r = {
    faulty: 0,
  };
  return r;
}
