export function app_g_verify_note_font_size() {
  "the size of a suggestion already written out under the passage, a little under the body so several of them can sit together without crowding it";
  let v = "0.9em";
  return v;
}
