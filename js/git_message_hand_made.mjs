export function git_message_hand_made() {
  "the commit message a change gets when no named command made it - a hand edit, so nothing named could be spelled here. Every reader of the log that asks whether the vocabulary was used is asking about exactly this word, and a reader spelling it for itself would go on measuring the old word the day the writer emits a new one";
  let v = "ai";
  return v;
}
