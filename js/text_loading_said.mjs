import { arguments_assert } from "./arguments_assert.mjs";
export function text_loading_said(what) {
  arguments_assert(arguments, 1);
  ("$plain what");
  ("The words to show in the place something is being fetched into, naming the thing that is on its way.");
  ("IT NAMES WHAT IS COMING AND NOT MERELY THAT SOMETHING IS. A bare word for waiting is only half an improvement: it says the press was heard without saying what was heard, so a person who pressed the chapter button and a person who pressed the book button read the same line and cannot tell whether the right one answered.");
  ("There is no full stop and no trailing dots. This is a line saying what is happening rather than a sentence, and dots asking to be read as time passing are read by some people as text that was cut off.");
  ("The English lives here rather than at each place that waits, so the one word every wait shares has one place to change.");
  let said = "Loading " + what;
  return said;
}
