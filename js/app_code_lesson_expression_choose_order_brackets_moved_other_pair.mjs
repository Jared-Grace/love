import { arguments_assert } from "./arguments_assert.mjs";
import { js_code_parenthesis_left } from "./js_code_parenthesis_left.mjs";
import { js_code_parenthesis_right } from "./js_code_parenthesis_right.mjs";
export function app_code_lesson_expression_choose_order_brackets_moved_other_pair() {
  arguments_assert(arguments, 0);
  ("the row put above the second of two runs on the moving brackets lesson: the same line again with the marks round the other pair");
  ("It says the same line rather than a line, because the words below it are the words above it and a learner who reads them as a fresh question has lost the whole of what the screen is showing.");
  ("Handed back as a row to be painted rather than painted here, so the card that carries it decides where it sits and this says only what it says.");
  let left_bracket = js_code_parenthesis_left();
  let right_bracket = js_code_parenthesis_right();
  let row = [
    "Now the same line with the ",
    left_bracket,
    " and ",
    right_bracket,
    " round the other pair",
  ];
  return row;
}
