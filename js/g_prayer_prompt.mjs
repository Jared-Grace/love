export function g_prayer_prompt() {
  "the prompt heading over the prayer choices on a prayer screen the FIRST time it is asked - what would you like to pray to God";
  let v = "What would you like to pray to God?";
  return v;
}
