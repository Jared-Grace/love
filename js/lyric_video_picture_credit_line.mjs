import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_picture_credit_line() {
  function_duplicate_kind_parallel();
  ("The one sentence that says who drew the pictures behind the words, written to be read by a person under a published video.");
  ("★ IT EXISTS BECAUSE THE MACHINE-READABLE ANSWER DOES NOT SURVIVE BEING MADE INTO A VIDEO. Each drawing arrives with a signed record sealed inside the file itself, naming the company, the model, and the fact that no camera was involved. That record was read out of a drawing on disk and found whole; the same drawings turned into a video and the finished video searched for any part of it came back with none, because the picture is re-encoded frame by frame into something that has no room to carry it. So the only wording anybody will ever see of it is a sentence somebody writes, and this is that sentence.");
  ("★ IT SAYS WHAT THE SEALED RECORD SAYS AND NOTHING MORE. The record names the model and states in a standard vocabulary that the image was wholly made by a trained model rather than captured. Those are the two claims here. Saying less would leave a watcher thinking a person painted these; saying more - about the singing, about the words - would be a claim this sentence has no evidence for, and a disclosure that overreaches is worse than none because it teaches a reader that the accurate parts might be guesses too.");
  ("★ IT IS ONE LINE SO THAT IT CAN GO WHERE IT CANNOT BE CUT. A video description is thrown away whole when it runs long, so anything written under a passage is written to a length, and the verses are trimmed to fit. A disclosure trimmed off is a disclosure that was never made. Kept to one line it can sit at the top with the heading, in the part that is always kept and is also the part shown before a reader presses to see more.");
  arguments_assert(arguments, 0);
  let line =
    "The pictures in this video were generated by FLUX.2, an image model made by Black Forest Labs. No photograph or painting by a person was used.";
  return line;
}
