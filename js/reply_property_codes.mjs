import { arguments_assert } from "./arguments_assert.mjs";
export function reply_property_codes() {
  arguments_assert(arguments, 0);
  ("The name of the place a half-finished reply keeps the pieces of code a rule wants run later.");
  ("It is named here rather than spelled at each site because the place is written to by one function and set up by another, and the two spelling it separately is exactly how one of them came to be writing into nothing at all.");
  let v = "codes";
  return v;
}
