export function binisaya_word_morphology_marker() {
  "The words binisaya.com heads its word-root-affix line with, which is how that line is found on a page that says nothing else about where it is.";
  "It is spelled here once because it belongs to somebody else's page and can change without warning. When it does, one reading goes quiet rather than several going wrong, and the gate that watches a known word is what says so.";
  let v = "Word - rootword - affixes<br>";
  return v;
}
