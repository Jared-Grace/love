export function js_types_loop_node() {
  ("The three kinds of node whose header is a scope of its own: the counting loop and the two that walk something.");
  ("Written out in two places before this, and the second copy is what a getter is for - a pass that knew only two of the three would read a name bound by the third as bound nowhere.");
  let types_loop = ["ForStatement", "ForInStatement", "ForOfStatement"];
  return types_loop;
}
