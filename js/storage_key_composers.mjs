import { arguments_assert } from "./arguments_assert.mjs";
import { fn_name } from "./fn_name.mjs";
export function storage_key_composers() {
  "Every function that makes a browser storage key out of an owner and a word.";
  "There was one of these and the reading that watches published names was written around that fact. A second arrived the day a page needed to say whose setting it was reading without importing the whole app that filed it - the join is still written once, and the second is the door that takes the owner's name on its own and the first now goes through it.";
  "Neither chooses a store, so neither is a seam: they are what a seam calls. They are named together here so the reading that asks who composes a key cannot learn about one and walk past the other.";
  arguments_assert(arguments, 0);
  let f_name = fn_name("storage_key_get");
  let f_name2 = fn_name("storage_key_name_get");
  let composers = [f_name, f_name2];
  return composers;
}
