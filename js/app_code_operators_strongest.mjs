import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_bang_symbol } from "./js_operator_bang_symbol.mjs";
export function app_code_operators_strongest() {
  arguments_assert(arguments, 0);
  ("the operators a code lesson solves before every other one, by their symbols: !");
  ("A class above * and /, because ! is worked out before anything around it: !true && false takes the ! first, and a line built as a shape has to print back as the line it means. Given the same strength as * it would print without the brackets that say which side it holds.");
  let v = js_operator_bang_symbol();
  let symbols = [v];
  return symbols;
}
