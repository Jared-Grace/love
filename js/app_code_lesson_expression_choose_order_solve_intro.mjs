import { arguments_assert } from "./arguments_assert.mjs";
import { html_div_cycle_code } from "./html_div_cycle_code.mjs";
export function app_code_lesson_expression_choose_order_solve_intro(parent) {
  arguments_assert(arguments, 1);
  ("the one sentence that says what is different about this lesson, at the head of the card the difference happens in");
  ("Everything it used to be said with - the word operator, and the run of four steps the learner already knows - now stands above the card, because that is the telling and this is the doing. What is left here is the hinge between the two, and it belongs with the doing: it is the last thing read before the first press.");
  ("It names the one word that changed rather than announcing a new lesson. The learner knows the run; the third step of it is being handed over to them, and saying so is the whole of what this screen has to add.");
  html_div_cycle_code(parent, [
    "Now, instead of being given the replaced value, you will choose the correct value for the operator",
  ]);
}
