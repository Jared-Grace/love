import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_note_name_tag_darkness() {
  arguments_assert(arguments, 0);
  ("how much of a name's colour is left in the ground the name is written on when the name itself is the coloured thing: a little over a quarter of it");
  ("ONE NUMBER FOR ALL THREE COLOURS, NOT ONE PER COLOUR. Each colour on its own would clear the reading floor at a different darkness - the red needs the most darkening and the green the least - and three numbers would be three chances to nudge one and leave the others behind. Measured, the darkening that satisfies the tightest of them satisfies the other two with room to spare, so the tightest one sets it and the number is a fact about the palette rather than about any colour in it.");
  ("★ SET BY MEASUREMENT AND NOT BY EYE. At this darkness the closest pair - the red letter on its own darkened red - reads at about 4.9 to 1, against a floor of 4.5 for ordinary lettering, and the darkened red reads against the pale card at about 12.7 to 1. The green and the blue are further apart still on both counts. Those figures come from the same contrast reckoning the note colours are gated by, so a change to any colour here is caught rather than admired.");
  ("Darker than it needs to be would be simpler to defend and would cost the thing being bought. Taken all the way to black, every name's ground is the same black and the second copy of the colour is gone - which is the ordinary dark chip, and a perfectly good design, but not this one. Kept just dark enough, the ground is still visibly the name's own hue, so the whole mark carries the colour and not only the few strokes of the letter.");
  let darkness = 0.28;
  return darkness;
}
