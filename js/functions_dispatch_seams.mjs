import { fn_name } from "./fn_name.mjs";
export function functions_dispatch_seams() {
  "The functions that run whatever repo function their own argument names. Typed at a dispatcher they hand over the machine, because the name they are given can be a command runner; reached from source they are how the repo works at all, because there the name is decided by the code rather than by whoever typed the line.";
  "That split is why they are listed apart from the command seams. A command seam is dangerous wherever it is reached from - whoever reaches it can pass a command - so reaching one is a reason to refuse a grant. These two are dangerous only in the third word of a command line. Listing them beside the command seams was tried, and it refused thirty standing grants that were never at risk: the service account is read by running a function the user's own config names, so every Bible and gloss function that touches storage reaches this line, and none of them can steer it.";
  "So this half feeds the guard's floor and nothing else. The floor is what the hole needed anyway - the guard matches the function name in the third word, so handing the banned name to one of these moved it to the fourth word, where nothing looked, and the grant check could not see it either, because these call nothing by name and the import graph shows a leaf.";
  let f_name = fn_name("function_run");
  let f_name2 = fn_name("function_run_unalias");
  let names = [f_name, f_name2];
  return names;
}
