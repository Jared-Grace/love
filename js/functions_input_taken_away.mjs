import { fn_name } from "./fn_name.mjs";
export function functions_input_taken_away() {
  "The dispatcher functions that remove their own input file once they have finished reading it. Each one is handed a staging file an author wrote, stores every passage out of it, and then deletes the file itself.";
  "The list exists for the bash guard, which can only read a command line. A caller who does not know the removal is already done writes it on the end as a second command, and the chain is then never grantable whatever is in it - so a run the guard would have allowed untouched stops and asks the human instead. Seven hundred such interruptions were measured in one week. The guard reads this list to answer that chain with the bare command rather than a question.";
  let f_name = fn_name("app_ceb_bible_gloss_write_chapter_file");
  let f_name2 = fn_name("app_en_learn_bible_gloss_urdu_write_chapter_file");
  ("Both reach ",
    fn_name("gloss_write_chapter_file_generic"),
    ", which is where the removal lives and where the reason for it is written down. A third caller of that function belongs here too.");
  let names = [f_name, f_name2];
  return names;
}
