import { arguments_assert } from "./arguments_assert.mjs";
import { functions_span_cut_all_starts_with } from "./functions_span_cut_all_starts_with.mjs";
export async function functions_span_cut_all() {
  arguments_assert(arguments, 0);
  ("Walks every function standing over the ceiling that holds a cuttable run, cutting each one out under a name worked out from the word its last line ends on, and asks the list once more at the end.");
  ("The repo-wide half of the cut, and the twin of the lifting walk beside it. That one has taken every closure it can reach, so what is left standing over the ceiling is what this is for: the straight runs of work between the closures, which are what most of a long body here is actually made of.");
  ("The whole of the walk lives in the one beside this that takes a word to begin at, and this is that walk asked for a word every name begins with. Nothing begins with nothing, so an empty word turns nobody away and the two answer identically - which is the point of spelling it this way rather than twice.");
  ("★ ON A REPO SEVERAL PEOPLE ARE CUTTING IN AT ONCE, REACH FOR THE ONE THAT TAKES A WORD. Each cut commits itself as it lands, so a walk over the whole folder can file somebody else's half-finished file under the name of a command that never touched it. Naming the part of the repo you are working in is how a walker stays inside it.");
  let r = await functions_span_cut_all_starts_with("");
  return r;
}
