export function functions_work_size_ceiling() {
  ("how many lines of work one function may hold before the gate calls it too big");
  ("forty, which is about a screen. The number is not a claim that forty is right and forty-one is wrong - it is where a reader stops being able to hold the whole of a function at once, and past it a function is read in pieces whether or not it was written in pieces.");
  ("Measured on 2026-08-03 the repo held 2704 functions, of which 132 stood above this and 24 above a hundred. That is why the number arrives with a record of what already stands above it rather than as a rule from today: a ceiling with nothing behind it either fails 132 times on the first run or has to be set above the largest thing there is, and the largest thing there is happens to be a list of names.");
  ("Lowering this is the point of the whole family, and the order is fixed: empty the record first, then drop the number. Dropping it first only refills the record, which is the one move that undoes the ratchet.");
  let v = 40;
  return v;
}
