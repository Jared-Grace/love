export function app_g_verify_label_font_size() {
  "the size of a small spaced-out label naming a section on the verify page, well under the text it introduces because the spacing and the weight are what make it read as a heading";
  let v = "0.72em";
  return v;
}
