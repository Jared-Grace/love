import { app_code_lesson_expression_brackets_moved_intro } from "./app_code_lesson_expression_brackets_moved_intro.mjs";
import { app_code_lesson_expression_brackets_moved_title_name_id } from "./app_code_lesson_expression_brackets_moved_title_name_id.mjs";
import { app_code_lesson_expression_brackets_value_generic } from "./app_code_lesson_expression_brackets_value_generic.mjs";
import { app_code_lesson_expression_choose_order_brackets_moved_expression } from "./app_code_lesson_expression_choose_order_brackets_moved_expression.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_lesson_expression_brackets_moved() {
  arguments_assert(arguments, 0);
  ("the whole line answered in one go: (false && true) || true asked for its value, with nothing to press");
  ("The twin of the lesson that walks the same line a press at a time, and the last of the four lessons the brackets are taught by. A learner who can press the bracketed pair first has still never been asked to hold both steps at once, which is the thing they will need in front of real code.");
  ("Its lines are drawn by the very same maker that pressing lesson draws its lines with, which is what makes the two lessons one family met twice.");
  ("Half of this family's lines are ones the marks change nothing about, and they are left in rather than filtered out. A lesson that only ever asked about lines the brackets overturn would teach that a pair of marks is a warning, and the learner would go on to read every bracketed line as a line whose ordinary order has been undone.");
  let name_id = app_code_lesson_expression_brackets_moved_title_name_id();
  let lesson = app_code_lesson_expression_brackets_value_generic(
    name_id,
    app_code_lesson_expression_brackets_moved_intro,
    app_code_lesson_expression_choose_order_brackets_moved_expression,
  );
  return lesson;
}
