export function app_replace_proof_arrow_font_size() {
  "the size of the arrow drawn between two steps of a proof, large because the arrow is the thing being read there rather than an ornament beside the text";
  let v = "2.2em";
  return v;
}
