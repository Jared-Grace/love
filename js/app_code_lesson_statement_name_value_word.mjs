export function app_code_lesson_statement_name_value_word() {
  ("what the cup in the lesson on giving a value a name has in it, and what the code in that lesson puts under the name");
  ("One word for both, because the whole of the lesson is that the two are the same thing said twice. If the picture held one thing and the code another, the second would be a new fact rather than the first one written down.");
  let word = "grape";
  return word;
}
