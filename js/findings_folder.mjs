export function findings_folder() {
  "Where what a check already found out is kept, beside the data folder rather than inside it, so nothing sweeping the data folder mistakes a record of the past for something the present depends on";
  "Not called history, though that is the everyday word for it, because this repo already says history when it means the one git keeps - the reading that asks whether a folder has its own. One word answering to two things is how a reader ends up sure of the wrong one. Findings is what the repo already calls what a check turned up, so the record of what checks turned up wears the same word.";
  "A record of the past spells names, and the two commands that ask whether a name is still spoken for both read every file in the data folder. So a function named once by a red gate could never be deleted again, and a rename would rewrite what a past run actually said. Measured the day this folder was made: sixty-nine commits recorded, two hundred and fifty-two live functions held undeletable by nothing but that record, and seven names in it belonging to functions already gone. Being a separate folder is the whole mechanism - those sweeps start at the data folder and never see this one.";
  let v = "history";
  return v;
}
