export function duplicate_kind_work() {
  "The answer meaning a function that really does something, and so is worth holding to a ratchet when another function shares its shape.";
  let r = "work";
  return r;
}
