import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_lesson_expression_choose_order_same_strength_operator_count } from "./app_code_lesson_expression_choose_order_same_strength_operator_count.mjs";
import { app_code_lesson_expression_choose_order_same_strength_above_generic } from "./app_code_lesson_expression_choose_order_same_strength_above_generic.mjs";
export function app_code_lesson_expression_choose_order_same_strength_above(
  root,
  context,
) {
  arguments_assert(arguments, 2);
  ("what stands above the card: one three-operator line of this lesson's own kind taken all the way down, and nothing else");
  ("Laid out by the one that lays out both same-strength pressing lessons, handed this lesson's count. What to leave off such a screen - the card naming the stronger operators, a closing sentence, a heading - is reasoned there, once for the two of them.");
  ("It used to say the lesson after it names the stronger operators. That stopped being true when the all-at-once twin of this one was put in between the two press-at-a-time lessons, so the reasoning is now written by what the card does rather than by a place in a list, since a place is what went stale. Corrected 2026-09-10.");
  let count =
    app_code_lesson_expression_choose_order_same_strength_operator_count();
  app_code_lesson_expression_choose_order_same_strength_above_generic(
    root,
    count,
  );
}
