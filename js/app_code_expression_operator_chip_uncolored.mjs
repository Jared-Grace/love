import { html_color_inherit } from "./html_color_inherit.mjs";
import { html_cursor_default } from "./html_cursor_default.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
import { html_box_shadow_set } from "./html_box_shadow_set.mjs";
import { html_style_background_color_set } from "./html_style_background_color_set.mjs";
export function app_code_expression_operator_chip_uncolored(span) {
  arguments_assert(arguments, 1);
  ("take the pale chip's look back off an operator - its fill, its lettering, its edge and the hand cursor over it - so it goes on reading as part of the line rather than as something to press");
  ("The chip is the whole of how this screen says press me. An operator that cannot be pressed and still wears one is a control that answers nothing, and a learner pressing it learns only that the screen is broken.");
  ("All four go together, because any one of them left behind is still a mark, and a mark on a line of code is read as meaning something.");
  ("The cursor is the one of the four that is only seen at the moment it matters. Fill, lettering and edge are read from across the screen; the hand shows up when the mouse is already over the operator, with the press half made. An operator gone plain that still puts a hand under the mouse says press me at exactly the moment it is least true.");
  ("The room the chip was holding either side of itself is NOT given back here, and that is the whole reason this is look alone. Room is the only part of a chip the rest of the line can feel: the fill going changes nothing but the operator's own square, while the room going pulls everything to the right of it along. So the two are wanted one after the other rather than together - the look where it stands, and then the closing up, watched.");
  html_style_background_color_set(span, "transparent");
  html_color_inherit(span);
  html_box_shadow_set(span, "none");
  html_cursor_default(span);
}
