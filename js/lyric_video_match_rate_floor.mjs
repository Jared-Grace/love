import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_match_rate_floor() {
  arguments_assert(arguments, 0);
  ("How much of a written psalm a transcriber that was shown no words must nevertheless hear in the recording, before the recording and the writing are believed to be the same song.");
  ("★ IT SITS IN THE MIDDLE OF AN EMPTY STRETCH AND IS NOT A ROUND NUMBER CHOSEN FOR BEING ROUND. Twenty six songs known to be paired correctly scored between 0.794 and 0.979, and no gap anywhere inside that run is wider than 0.067; a psalm deliberately checked against a different psalm's singing scored 0.191. So every number from about a fifth to about four fifths separates the two perfectly, and this one stands in the middle of that stretch with roughly three tenths of clearance on either side. A floor with clearance survives a song a little worse than any heard so far and a mismatch a little better.");
  ("★ NINE TENTHS SOUNDS LIKE THE CAREFUL CHOICE AND WOULD THROW OUT A THIRD OF THE SONGS THAT ARE RIGHT. Singing is not speech: a held note, a repeated line, and a voice under music each cost the transcriber words it would have caught in a reading, so the correct pairings do not crowd up near one. Reading the number as a quality score is what makes a high floor look safe, and it is not one - it answers whether this is the right recording at all, and how well the words then land is asked elsewhere, by two readings disagreeing about a line.");
  let rate = 0.5;
  return rate;
}
