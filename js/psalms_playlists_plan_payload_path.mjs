import { folder_gitignore_join } from "./folder_gitignore_join.mjs";
export function psalms_playlists_plan_payload_path() {
  "Where the short list of what the Psalm playlists still need waits for something signed in to pick it up.";
  let path = folder_gitignore_join("psalms_playlists_plan_payload.json");
  return path;
}
