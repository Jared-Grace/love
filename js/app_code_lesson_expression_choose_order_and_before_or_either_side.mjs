import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_and_symbol } from "./js_operator_and_symbol.mjs";
import { js_operator_or_symbol } from "./js_operator_or_symbol.mjs";
export function app_code_lesson_expression_choose_order_and_before_or_either_side() {
  arguments_assert(arguments, 0);
  ("the row saying the && may stand on either side of the ||, ready to be written above the second of two worked lines");
  ("It is the sentence the brackets lessons end on, in the same words, about the && rather than about a pair of brackets. A learner recognises a sentence they have already read in the second it takes, and stops to compare a near-copy against it.");
  ("What it says is the whole of why the second line is walked at all: the && is solved first because of what it IS, not because of where it stands, and a lesson that only ever printed it leftmost was teaching the wrong half of that.");
  ("It is handed back rather than written onto a card, because the caller puts it above a line about to be walked a step at a time and the card is made there.");
  let and_symbol = js_operator_and_symbol();
  let or_symbol = js_operator_or_symbol();
  let words = ["The ", and_symbol, " can be on either side of the ", or_symbol];
  return words;
}
