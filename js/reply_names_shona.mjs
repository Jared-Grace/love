import { arguments_assert } from "./arguments_assert.mjs";
export function reply_names_shona() {
  arguments_assert(arguments, 0);
  ("Given names in Shona, spoken in Zimbabwe.");
  let names = ["farai", "rutendo", "tapiwa", "tendai"];
  return names;
}
