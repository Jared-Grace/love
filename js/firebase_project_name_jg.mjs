export function firebase_project_name_jg() {
  "The name of the firebase project everything here is hosted under, written once so a rename is one edit rather than a search.";
  let r = "jared-grace";
  return r;
}
