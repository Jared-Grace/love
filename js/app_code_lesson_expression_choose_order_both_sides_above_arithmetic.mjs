import { app_code_lesson_expression_comparing_a_comparison_recall } from "./app_code_lesson_expression_comparing_a_comparison_recall.mjs";
import { app_code_lesson_expression_sides_one_operator_say } from "./app_code_lesson_expression_sides_one_operator_say.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_lesson_expression_choose_order_both_sides_above_generic } from "./app_code_lesson_expression_choose_order_both_sides_above_generic.mjs";
export function app_code_lesson_expression_choose_order_both_sides_above_arithmetic(
  root,
  tree,
  intro,
) {
  arguments_assert(arguments, 3);
  ("the run above the card for a line whose two sides are arithmetic written with one shared operator: the shared telling, opening on the comparison recall and reasoning by counting that operator");
  ("Two lessons hand this the same two painters - the one with === in the middle and the one with any of the other five - because their lines differ in the comparison and in nothing else. Chosen at both, the day either painter is improved it is improved on one screen and left alone on the other, and nothing goes red to say so.");
  app_code_lesson_expression_choose_order_both_sides_above_generic(
    root,
    tree,
    app_code_lesson_expression_comparing_a_comparison_recall,
    app_code_lesson_expression_sides_one_operator_say,
    intro,
  );
}
