import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_lesson_expression_choose_order_both_sides_above_generic } from "./app_code_lesson_expression_choose_order_both_sides_above_generic.mjs";
export function app_code_lesson_expression_choose_order_both_sides_above_arithmetic(
  root,
  tree,
  intro,
) {
  arguments_assert(arguments, 3);
  ("the run above the card for a line whose two sides are arithmetic: the shared telling, with the two sentences that name what the sides are filled in");
  ("Two lessons hand this the same two sentences - the one with === in the middle and the one with any of the other five - because their lines differ in the comparison and in nothing else. Written at both, the day the wording of either sentence is improved it is improved on one screen and left alone on the other, and nothing goes red to say so.");
  app_code_lesson_expression_choose_order_both_sides_above_generic(
    root,
    tree,
    intro,
    " are arithmetic, so they go before the ",
    "Numbers and arithmetic solve to the same value every time",
  );
}
