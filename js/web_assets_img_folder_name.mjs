export function web_assets_img_folder_name() {
  "The folder every picture that is not tied to one app lives in, under the assets folder.";
  "THIS IS THE ONE PLACE THE PICTURES' HOME IS SPELLED. Moving them is meant to be an edit to this word and nothing else, which only holds while every address is built through it rather than typed again somewhere.";
  let folder_name = "img";
  return folder_name;
}
