import { app_code_lesson_statement_name_copy } from "./app_code_lesson_statement_name_copy.mjs";
export function app_code_lessons_read_through_last_fn() {
  "the last lesson handed to a learner on either page they are ever sent to - the built site, and latest: the ones written after it are finished code and have not been read through by the person handing the track to a learner yet";
  "Named for having been READ THROUGH rather than for a stage, because two stages ask it and a third does not. A name saying prod would be read as a fact about one address, and the day latest started asking the same question the name would have said the wrong thing about it - which is how thirty-two unread lessons stood reachable on latest while the cut was believed to be holding.";
  "A learner meeting a lesson nobody has read through has no way to tell it from one that has been, so a run that stops at a known-good place is worth more to them than a longer one they have to judge for themselves. Every lesson after this one is still built, still opens on the working copies, and costs nothing to release - moving this name down the list is the whole of releasing them.";
  "This one, because it is the last screen of the run that only makes names and reads them back. A name is given a value, given another one, and here read on the right of an equals so that a second name holds what the first one holds - which is where a learner is first told the rule that a name may be written wherever a value may be written.";
  "The lesson after it takes that same rule into a place it has not been seen in, a name on each side of a plus, and every lesson after that goes on putting names into the shapes already taught. So the cut falls where a sentence has just been finished rather than in the middle of spending it.";
  "The run behind it is whole on its own terms too: the lessons that stand a mark in front of a line come in twos, one asking which operator goes first and one solving a line of that shape, and the last of those pairs is now five lessons inside the released run rather than at its edge.";
  "It sat fourteen lessons earlier before, at the first place parentheses were put around a comparison joined by and or or. Those fourteen were read through and released together.";
  "The lesson is named by its own function rather than by a count, so it goes on meaning this lesson when lessons are put in ahead of it, and a rename follows it.";
  let fn = app_code_lesson_statement_name_copy;
  return fn;
}
