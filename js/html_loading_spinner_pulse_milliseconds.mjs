export function html_loading_spinner_pulse_milliseconds() {
  "how long one expansion of the outer ring takes. the css transition and the loop that picks the next size both read this, so they can never drift apart";
  let r = 1000;
  return r;
}
