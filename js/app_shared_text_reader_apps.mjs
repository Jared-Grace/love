import { fn_name } from "./fn_name.mjs";
export function app_shared_text_reader_apps() {
  "The apps that have promised to meet their reader in a language of their own, named one by one.";
  "It is a list somebody writes on rather than something worked out, because the promise is not visible in the code. Nearly every app here reaches the picking, since the panel for writing to the developer is shared by all of them and speaks the reader's language - so an app would be held to a promise it never made, on the strength of one shared panel it merely opens.";
  "An english app with english words on it is not in breach. Writing an app's name here is the act of promising, and until somebody does it the words being in english is what was intended.";
  "Each app is named rather than brought in. This is shared code, and a shared thing that fetched one app would hand that whole app to every other app that ever asked it a question - which here would be the whole point of the list defeating itself, since the list exists to name apps this shared code is not part of.";
  "The game joined on 2026-08-23, and what it cost was two sentences. It looked far too big to promise until the reader was actually run over it: what a player is shown is drawn from content, so the words typed into calls were almost all on the screens the person building it opens, and those are not promised to anybody. Do not guess at this before measuring it - the guess was that the game would be the expensive one.";
  let f_name = fn_name("app_en_learn_bible");
  let f_name2 = fn_name("app_g");
  let names = [f_name, f_name2];
  return names;
}
