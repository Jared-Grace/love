import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function app_search_query_hash_word_gap() {
  function_duplicate_kind_parallel();
  ("the query holds space-separated words; in the url a space becomes this so the browser does not turn it into %20");
  let v = "+";
  return v;
}
