export function zzz_probe_wire_gate_run() {
  "a throwaway gate that complains about nothing, here only to be found unlisted and listed by the command that lists them";
  let r = {
    probed: 0,
  };
  return r;
}
