export function app_code_lesson_statement_name_copy_name() {
  ("the third name, used only by the lesson where a cup is filled from another cup: the one that is filled");
  ("The two names before it are the pair every earlier screen uses, and they keep what they hold on this screen - nothing is emptied and nothing is renamed. So the cup that is filled has to be a third one, and it needs a name of its own.");
  ("The next letter along, because a learner reading three cups left to right meets a, b and c in the order the alphabet already gave them, and so has nothing to work out about which name is new.");
  let name = "c";
  return name;
}
