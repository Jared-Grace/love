export function bible_search_folder() {
  "The one place in storage the search index keeps its words, a file to a word.";
  "Named here because two things want it and they have to agree: the reading that puts a word up there and asks for it back, and the sweep that takes down the words the index no longer knows. A sweep looking under a different name would find nothing and say it had tidied.";
  let folder = "bible_search";
  return folder;
}
