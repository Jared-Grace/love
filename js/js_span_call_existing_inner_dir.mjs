import { arguments_assert } from "./arguments_assert.mjs";
import { js_statement_find_name_inner } from "./js_statement_find_name_inner.mjs";
import { js_span_call_existing_dir_generic } from "./js_span_call_existing_dir_generic.mjs";
export async function js_span_call_existing_inner_dir(
  dir,
  f_name,
  address_from,
  address_to,
  f_name_call,
) {
  arguments_assert(arguments, 5);
  ("The hermetic core of the fold that reaches inside a loop: both ends are taken on the line the name is really written on, however deep that is.");
  ("Everything it does is held one name down, and the only thing said here is which reader finds the two ends.");
  ("It exists so that the twin the duplications in this repo actually need can be watched by a folder example. The climbing core beside it addresses the top of the body, so on the case this one is for it would cut the whole loop and the answer would always be no.");
  let select_statement = js_statement_find_name_inner;
  let output = await js_span_call_existing_dir_generic(
    dir,
    f_name,
    address_from,
    address_to,
    f_name_call,
    select_statement,
  );
  return output;
}
