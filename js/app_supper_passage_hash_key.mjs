export function app_supper_passage_hash_key() {
  "The word standing before which passage in the link the supper page shows, so a link somebody saved or sent still opens on that passage.";
  let v = "sp";
  return v;
}
