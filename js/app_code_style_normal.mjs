import { arguments_assert } from "./arguments_assert.mjs";
import { html_style_code_unfonted } from "./html_style_code_unfonted.mjs";
export function app_code_style_normal(component) {
  arguments_assert(arguments, 1);
  ("the look a written-out value wears: white behind it, black letters, and the shared code shape - corners rounded, no line around it, room to its sides");
  ("It is the shape of code without being code. What is written out is not something to type, so it must not wear the dark chip the code itself wears; but it is still the machine's own answer rather than English about it, so it keeps the shape.");
  ("The two colours live here rather than at each caller, because a second place naming them is a second place that can be changed on its own, and then one written-out value on a screen would be a different colour from another.");
  html_style_code_unfonted(component, "white", "black");
}
