import { arguments_assert } from "./arguments_assert.mjs";
export function firebase_prod_hash_absent() {
  "The word written into a record of what is being served in place of a short word for a file, when the far end says it is serving no such file at all.";
  "★ IT IS RECORDED RATHER THAN LEFT OUT, because a record one file short reads as fewer files than there are, and every later comparison against it agrees by having nothing to disagree with. A page whose live build is older than the one on disk is missing pieces by name, and that is the single most important thing such a record can say - so it says it, in the place where the short word for that file would have been.";
  "★ NOTHING CAN HASH TO IT. A short word for a file is written in the sixteen digits of a number, so a word carrying a space is one no file can ever be reduced to, however its contents change. That is what lets a reader tell not-served from served-something without being told which side of the record it is holding.";
  arguments_assert(arguments, 0);
  let word = "not served";
  return word;
}
