import { text_frozen } from "./text_frozen.mjs";
export function app_shared_bible_history_key() {
  "The word the readings this app has been left on are kept under on the device, so the writer and the readers of them cannot disagree about the spelling.";
  "It outlives a tab on purpose. What a tab keeps goes when the tab goes, and a tab going is the whole reason anybody looks here.";
  "Frozen at this site for the same reason it is listed as frozen: both halves of it read as names a function could be given, and the canonicalizing pass turns a value that spells a function's name into a reference to that function.";
  let v = text_frozen("reading_history");
  return v;
}
