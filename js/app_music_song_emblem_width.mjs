export function app_music_song_emblem_width() {
  "How wide a hymn's drawing is allowed to be where the hymn is read, as a width a stylesheet understands.";
  "IT IS A CEILING AND NOT A WIDTH, so a column narrower than this still gets a drawing that fits it. On a phone held upright the column is the narrower of the two and the drawing takes the column; on anything wider this is what stops one square shape taking the whole screen.";
  "IT IS A NUMBER SOMEBODY CHOSE BY LOOKING, WHICH IS WHY IT IS NAMED. There is no measurement that settles how large a picture beside a hymn should be - it is a judgement about how much of the page the words should keep - so the value is here under a name, to be moved by whoever next looks at the page rather than hunted for among the lines that draw it.";
  let r = "180px";
  return r;
}
