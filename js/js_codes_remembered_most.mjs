import { arguments_assert } from "./arguments_assert.mjs";
export function js_codes_remembered_most() {
  arguments_assert(arguments, 0);
  ("How many files' worth of a reading anything here keeps before dropping the");
  ("lot and starting again.");
  ("Set well above what the readings that keep things actually touch - sixteen");
  ("hundred files in the widest of them - so that a whole run is answered out of");
  ("what is kept and the limit is never reached during one. It is a floor under");
  ("something long-running rather than a budget: a process that stays up across");
  ("many runs would otherwise keep a reading for every version of every file it");
  ("had ever been shown, and each of those is filed under the file's whole text.");
  let most = 4096;
  return most;
}
