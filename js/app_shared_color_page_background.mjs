export function app_shared_color_page_background() {
  "the off-white a whole page sits on — barely lifted off pure white, so a white card still reads as raised above it; shared by every app's full-screen background so the apps look like one family";
  let c = "#fafafa";
  return c;
}
