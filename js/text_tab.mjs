export function text_tab() {
  "The tab character, as one piece of text.";
  "Named because it cannot be seen. Written straight into a line it is a run of blank space that a reader takes for spaces and an editor is free to turn into spaces, and then a split that was cutting a line in two silently stops cutting anything. The newline beside it is named here for the same reason.";
  let t = "\t";
  return t;
}
