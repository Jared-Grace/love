import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function ebible_offline_download_chunk_size() {
  function_duplicate_kind_parallel();
  ("how many chapters travel at once: enough to keep the connection busy, few enough that a slow phone stays responsive");
  let size = 12;
  return size;
}
