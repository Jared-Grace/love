import { app_shared_color_green_deep } from "./app_shared_color_green_deep.mjs";
export function app_shared_milestone_complete_background_color() {
  "the colour a milestone is painted once the learner has been all the way through it - deep green, standing to the light green of a finished lesson exactly as the deep blue of a milestone stands to the light blue of a lesson not yet reached";
  "Both pairs are said here rather than left to each caller, because what carries the meaning is the RELATION between the two: a learner reads deep as this is a milestone and green as this one is done, and either half chosen on its own breaks the reading. A milestone painted the lesson's own light green would say finished and stop saying milestone.";
  let c = app_shared_color_green_deep();
  return c;
}
