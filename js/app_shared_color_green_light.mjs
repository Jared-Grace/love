export function app_shared_color_green_light() {
  "a LIGHT green text for the player's own PRAYER on a dark background (lighter than the button green app_shared_color_green, for readability) — the player's voice, used by the thanksgiving prayers (discernment thanks + the gratitude menu)";
  let v = "#90ee90";
  return v;
}
