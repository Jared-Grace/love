export function app_shared_color_green_light() {
  "a light green. It is a value, not a meaning - what it is for is said by whoever asks for it, and it is asked for by several things that have nothing to do with each other: the background of a finished row, a player's own prayer written on a dark background, a bar filling up as work is done.";
  "it used to say it was the prayer one. That was true of the only caller it had at the time and stopped being true without anybody noticing, because a name spelling a colour is reached for by whoever wants that colour.";
  let v = "#90ee90";
  return v;
}
