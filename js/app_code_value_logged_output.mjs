export function app_code_value_logged_output() {
  ("what a program that writes something out is said to have produced, the word every label about it is built from");
  let r = "logged output";
  return r;
}
