import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { app_shared_game_npc_property_get_null_generic } from "./app_shared_game_npc_property_get_null_generic.mjs";
export function app_shared_game_npc_glow_get(npc) {
  function_duplicate_kind_parallel();
  ("The light drawn under a person to say something is true of them, found by WHO that person is - the same lookup their picture and their cross use, and keyed the same way so all three are found and moved together.");
  ("A glow is its own element sitting on the map underneath the person rather than part of the picture on top of it, which is what lets it be a soft circle wider than they are and still never cover their face. The cost of being its own element is that anybody who moves a person has to move it, and keeping a handle on it here is what makes that possible.");
  ("It is filed rather than redrawn every time the screen is painted, and that is the whole of why this exists. Redrawn, the light was placed at the tile the person had just been sent to while their picture was still crossing the one before it - so a walking person was followed by their own light running on ahead of them, arriving early and waiting. Made before anybody has taken a step and then carried, it is never placed from a tile at all.");
  ("Nothing is the ordinary answer here, not a fault - a game that lights nobody files nothing, and everybody moving asks.");
  let glow = app_shared_game_npc_property_get_null_generic(
    app_shared_game_npc_glow_get,
    npc,
  );
  return glow;
}
