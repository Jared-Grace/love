export function g_sermon_generate_book_generic_property() {
  "The word a generated explanation is filed under wherever one is stored, kept here because everything that writes one and everything that reads one has to spell it the same.";
  let g = "generated";
  return g;
}
