import { arguments_assert } from "./arguments_assert.mjs";
export function song_image_color_looking() {
  "the warm pale yellow the song-image tool marks the one thing being looked at with - the couplet now open, the words of that couplet, and the outline of the candidate standing in the frame";
  "One shade for all three because they are one fact shown in three places: the tool asks a person to compare pictures for a couplet, and every part of the screen has to agree about which couplet that is. A second yellow a shade away would read as a second kind of attention and there is only one.";
  arguments_assert(arguments, 0);
  let c = "#ffe994";
  return c;
}
