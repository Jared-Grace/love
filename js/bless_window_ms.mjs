export function bless_window_ms() {
  "How close together two blessings have to be for the second one to count as following";
  "the first - the 'immediately and sequentially' the ladder is unlocked by, in";
  "milliseconds.";
  "Ten seconds is a GUESS and is meant to be tuned by playing, not by reasoning: too short";
  "and an unhurried player never earns a rung they plainly deserved, too long and two";
  "unrelated prayers minutes apart fold into a summary that was never one prayer.";
  let ms = 10000;
  return ms;
}
