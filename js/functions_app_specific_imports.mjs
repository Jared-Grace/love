import { arguments_assert } from "./arguments_assert.mjs";
import { functions_app_specific_imports_walked } from "./functions_app_specific_imports_walked.mjs";
import { property_get } from "./property_get.mjs";
export async function functions_app_specific_imports() {
  arguments_assert(arguments, 0);
  ("Every place a function belonging to no app imports one that belongs to a single app - each answer naming the importer and the app-specific name it reached for.");
  ("Measured on the day it was written: the narrow check had just reached zero, and this one starts from what the wider set really carries.");
  ("The two questions that decide what counts moved one name along when the gate above needed to know how much had been opened. Three of the four readers here rewrite or add to a record and one lists pairs; none of them has any use for the count, so it is read off next door and left behind.");
  let told = await functions_app_specific_imports_walked();
  let offenders = property_get(told, "offenders");
  return offenders;
}
