export function ebible_derivatives_forbidden_words_least() {
  "How many words in a row two texts have to share before the sharing counts as one text having been taken from the other.";
  "Six, and the number is a judgment rather than a measurement, so it is written in one place with the reason beside it instead of being typed at each gate that asks.";
  "Below about five, two people translating the same verse into the same language on their own agree by accident: a short run is mostly the language's own common words in the order that language puts them in, and it would name every honest translation on the machine. Above about eight, a single word changed anywhere in a copied sentence hides the whole sentence, which is the failure that matters here, because the thing being looked for is text that was taken and lightly edited rather than text that was taken untouched.";
  "It is deliberately the same number for every language. A number tuned per language would be tuned by whoever was looking, on the text they happened to be looking at, and a licence gate that can be tuned until it passes is not a gate.";
  let least = 6;
  return least;
}
