export function app_code_quiz_hash_key() {
  "The word standing before the place in the quiz in the link the code app shows, so a link somebody saved or sent still opens on the question it was saved on.";
  let v = "quiz";
  return v;
}
