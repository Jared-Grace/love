import { arguments_assert } from "./arguments_assert.mjs";
import { property_set } from "./property_set.mjs";
import { app_g_bless_glows } from "./app_g_bless_glows.mjs";
export function app_g_bless_glows_show(glows, view) {
  arguments_assert(arguments, 2);
  ("Light everybody a prayer just covered, and write down who they were so the light can be");
  ("laid again where they have walked to.");
  ("Who was blessed is kept on the glowing layer itself rather than passed about, because");
  ("the light and the list of who it is on are one thing: anywhere the layer can be reached,");
  ("what belongs on it can be reached too, and the two cannot be carried apart and go out of");
  ("step.");
  property_set(glows, "glowing", view);
  app_g_bless_glows(glows, view);
}
