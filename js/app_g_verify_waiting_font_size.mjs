export function app_g_verify_waiting_font_size() {
  "the size of the line shown while a verse is being written and there is nothing else on the page, a little over the body size because for that moment it is the only thing to read";
  let v = "1.1em";
  return v;
}
