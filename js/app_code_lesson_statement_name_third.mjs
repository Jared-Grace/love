export function app_code_lesson_statement_name_third() {
  "the third name, for a screen where something is written to a name while the first two keep what they hold";
  "The two names before it are the pair every earlier screen uses, and on a screen like this they keep what they hold - nothing is emptied and nothing is renamed. So whatever is being written to has to be a third name.";
  "The next letter along, because a learner reading three cups left to right meets a, b and c in the order the alphabet already gave them, and so has nothing to work out about which name is new.";
  let name = "c";
  return name;
}
