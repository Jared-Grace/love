import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_audio_fade_seconds() {
  function_duplicate_kind_parallel();
  ("How long the song takes to come up to full loudness at the very beginning of a video.");
  ("★ IT IS FOR THE JOLT AT THE START AND NOT FOR AN EFFECT. A recording that begins in the middle of a note hands the very first sample over at full loudness, and a listener hears that as a bang or a click before they hear any music, because nothing about it rose - it simply was. This measured true of the singing it was written for: the loudest sample in the first fifth of a second was within a decibel of the loudest the whole song ever gets. A brief rise removes the bang, and a listener never notices the rise itself.");
  ("★ IT IS SHORT BECAUSE THE SINGING IS ALREADY THERE. There is no silence at the front of these recordings to fade through, so every fraction of a second spent rising is a fraction of real singing held quiet. That is the whole reason this is a fifth of a second and not the half second a fade usually is: long enough that nothing snaps on, short enough that the first word is at full voice by the time anybody could name it.");
  ("★ IT IS ONE NUMBER FOR EVERY VIDEO RATHER THAN A FIELD IN A DOCUMENT. A document says what was sung and how it is shown; how loudly the first moment arrives is a property of handing any recording at all to a listener, and no psalm has a reason to want a different one. Written into the documents it would have to be written into all of them, one at a time, to say one thing once.");
  arguments_assert(arguments, 0);
  let seconds = 0.2;
  return seconds;
}
