import { arguments_assert } from "./arguments_assert.mjs";
import { function_lift_wrapper_candidates } from "./function_lift_wrapper_candidates.mjs";
import { functions_lift_candidates_generic } from "./functions_lift_candidates_generic.mjs";
export async function functions_lift_wrapper_candidates() {
  arguments_assert(arguments, 0);
  ("Every function standing over the ceiling that holds a closure the move leaving the name behind would take, with the biggest such closure named and sized. Biggest closure first.");
  ("The wider of the two work lists. The one beside it names only what the plain move would take, which is what is always called by name; this names what the wider move would take as well, which is nearly every closure there is - a callback handed to a visitor, a renderer handed to a page. Measured on 2026-08-17 the narrower list had nothing at all to say about a lesson holding a thirty-one line closure, because that closure was handed to a mapper rather than called.");
  ("Where a row appears on both lists the plain move is still the one to make, because it takes the name away as well as the body and so frees every sibling that was reaching for it. Which of the two to make is decided per row when the move happens rather than here, so this list stays a reading of what is reachable.");
  let ranked = await functions_lift_candidates_generic(
    function_lift_wrapper_candidates,
  );
  return ranked;
}
