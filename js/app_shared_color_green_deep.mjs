export function app_shared_color_green_deep() {
  "a deep green. It is a value, not a meaning - what it is for is said by whoever asks for it.";
  "It is the deep blue of a milestone answered in green: deep enough for white words to be read on it easily, and unmistakably green while it is being deep. Neither half comes free. Copying the blue's own numbers across into green gives something visibly lighter than the blue was, because green is the colour the eye reads as brightest. Matching it to that blue by the light the two throw is the other trap though, and it was the one fallen into first: a green dimmed that far has had nearly all its greenness taken out along the way, and what is left reads as a thing behind a filter rather than as a colour somebody chose. Deep enough for white words, and still plainly green, is what is actually being asked for - so it is picked by what it looks like, not by what it measures.";
  let v = "#15803d";
  return v;
}
