export function app_shared_color_green_deep() {
  "a deep green. It is a value, not a meaning - what it is for is said by whoever asks for it.";
  "It is the deep blue of a milestone answered in green: picked so that white words sit on it as readably as they sit on that blue, which is not the same as picking a green with the blue's own numbers rearranged. Green is the colour the eye reads as brightest, so a green built to match a blue by its written recipe comes out visibly lighter than the blue it was copied from. Matched instead on how much light it actually throws, which is what darkness means to somebody looking at it.";
  let v = "#154d24";
  return v;
}
