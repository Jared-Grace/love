export function app_code_first_decimal_digit_words() {
  "the words the rounding lessons keep naming a digit by - first digit after the decimal point - written in the one place because the screen says them five times over and a learner is meant to recognise the phrase rather than read it again";
  let r = "first digit after the decimal point";
  return r;
}
