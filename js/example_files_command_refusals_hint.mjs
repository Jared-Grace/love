export function example_files_command_refusals_hint(opening) {
  ("The sentence both the gate and the writer say about a command the corpus never proves refuses, given whichever opening the two of them need.");
  ("They differ only in what they are about to do about it - one is refusing a new one, the other is being asked to bless one - and the reason it matters is the same sentence in both mouths. Written once so the two cannot come to say different things about the same finding.");
  let hint = opening;
  return hint;
}
