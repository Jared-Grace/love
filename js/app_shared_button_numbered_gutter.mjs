export function app_shared_button_numbered_gutter() {
  "the fixed width of the number column in a numbered list button - wide enough to hold a three digit index and the period after it, so a list past ninety nine entries still lines its numbers up on their periods instead of spilling the period into the title";
  let v = "4ch";
  return v;
}
