export function red_proof_folder() {
  "The wrong versions each pinned reader is checked against, in the given half of the data folder because a gate reads them to decide whether that reader's corpus can still tell right from wrong.";
  "They are files rather than arguments because a wrong version is code, and no command in this repo takes code as an argument. So the corpus of mistakes lives on disk beside the corpus of cases, and a command finds it by the name of the reader it is about.";
  let folder = "data/given/red_proofs";
  return folder;
}
