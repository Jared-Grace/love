import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_asterisk_symbol } from "./js_operator_asterisk_symbol.mjs";
import { js_operator_division_symbol } from "./js_operator_division_symbol.mjs";
import { js_operator_plus_symbol } from "./js_operator_plus_symbol.mjs";
import { js_operator_minus_symbol } from "./js_operator_minus_symbol.mjs";
import { app_code_container_light_blue } from "./app_code_container_light_blue.mjs";
import { html_div_cycle_code } from "./html_div_cycle_code.mjs";
export function app_code_lesson_expression_choose_order_three_recall(root) {
  arguments_assert(arguments, 1);
  ("the one thing put back in front of the learner before the run on the three-operator lesson: which of the four arithmetic operators are the stronger ones");
  ("It stands in a card of its own above the run, because it is not part of the run - it is what the run is about to be read with. A learner who has met the four operators pages ago needs the pair naming again before a line holds all four of them at once.");
  app_code_lesson_expression_choose_order_strong_card(root);
}
