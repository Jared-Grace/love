export function examples_folder() {
  "The worked examples a transform is tried against, in the given half of the data folder because a gate reads them to decide whether the transform still does what it did.";
  let folder = "data/given/examples";
  return folder;
}
