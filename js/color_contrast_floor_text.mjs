import { arguments_assert } from "./arguments_assert.mjs";
export function color_contrast_floor_text() {
  arguments_assert(arguments, 0);
  ("how far apart in contrast ordinary lettering and what it is written on have to be before a reader can be expected to read it: four and a half to one");
  ("★ SOMEBODY ELSE'S RULE, WHICH IS THE ENTIRE REASON IT IS WORTH HAVING. It is the web's own figure for body-sized text, and this repo did not choose it and cannot argue with it. A number this repo picked would be an opinion about what looks alright, and an opinion drifts to fit whatever was drawn last - which is exactly the failure a floor exists to stop, since the drawing that pushed it is always the drawing that seemed fine at the time.");
  ("Named rather than typed where it is used, because it is about to be asked in more than one place and two copies of a rule are two rules the moment either is touched. It is also the number a reader wants to look up when a check refuses something, and a name is what makes that findable.");
  ("The larger-lettering relaxation the same rule offers is deliberately not built. Everything measured against this so far is a name inside a line of code or on a picture, all of it at reading size or smaller, so the relaxation would apply to nothing and would only offer a way round the floor to whoever came next.");
  let floor = 4.5;
  return floor;
}
