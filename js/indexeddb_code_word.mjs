export function indexeddb_code_word() {
  "The word a function's name has to carry before it is asked whether it receives the name of a browser database or one of its stores - the one place that word is spelled";
  let v = "indexeddb";
  return v;
}
