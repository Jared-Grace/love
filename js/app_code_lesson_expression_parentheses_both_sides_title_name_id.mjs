import { app_code_lesson_name_id_category_then } from "./app_code_lesson_name_id_category_then.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
import { html_cycle_code } from "./html_cycle_code.mjs";
import { app_code_category_expressions } from "./app_code_category_expressions.mjs";
export function app_code_lesson_expression_parentheses_both_sides_title_name_id() {
  arguments_assert(arguments, 0);
  ("the home title: three comparisons, an Expressions lesson");
  ("The words its pressing twin wears, minus Solve, which is how every pair in this course is told apart. The twin walks one of these lines a press at a time; this one hands the whole line over and asks what it comes to.");
  ("It used to read A comparison inside ( and ) on both sides. That named the parentheses but not the count, and it shared no word with the twin, so the two could not be seen as a pair on the home list. The parentheses are still what this lesson earns - the lesson that first showed them put them where they changed nothing - and they are named on the card that teaches them, which is where a learner meets them anyway.");
  function paint(parent) {
    html_cycle_code(parent, ["Three comparisons"]);
  }
  ("These words used to be FROZEN, and are not any more. They once built the id, and the id is the key a learner's finished-lessons record is saved under - and this lesson is the last one the built site hands out, so that key is one somebody is holding. Shortening it to parentheses both sides was reverted the same day it was written, 2026-08-25, for exactly that reason. Ids are written down beside each lesson's own function name now, and this one is brackets_sides, which no wording here can move. Checked on 2026-09-10 before the title was changed: read the id table rather than trusting this paragraph, since a paragraph is what went stale.");
  let left = app_code_category_expressions();
  let built = app_code_lesson_name_id_category_then(left, paint);
  return built;
}
