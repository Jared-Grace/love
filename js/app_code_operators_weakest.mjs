import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_or_symbol } from "./js_operator_or_symbol.mjs";
export function app_code_operators_weakest() {
  arguments_assert(arguments, 0);
  ("the operators a code lesson solves last of all, by their symbols: ||");
  ("A class of its own below &&, because the two are not of equal strength: 3 < 5 && 2 < 4 || 1 > 9 works the && out first, and a line built as a shape has to print back as the line it means. Held in one class with &&, a shape that says and-of-a-side-that-ors would print without the brackets that say so, and read as a different line.");
  let v = js_operator_or_symbol();
  let symbols = [v];
  return symbols;
}
