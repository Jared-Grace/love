export function ebible_folder_thai() {
  "The folder the Thai bible's chapters are kept under.";
  "It is named rather than spelled out because a second reading now asks for it by name: Thai is the one language here that ends a sentence on no mark at all, so the reading that stops on a finished sentence has to know which folder that is.";
  let t = "thaKJV";
  return t;
}
