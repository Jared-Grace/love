import { arguments_assert } from "./arguments_assert.mjs";
export function reply_names_filipino() {
  arguments_assert(arguments, 0);
  ("Given names used in the Philippines that neither the English nor the Spanish list carries.");
  let names = ["angelo", "arnel"];
  return names;
}
