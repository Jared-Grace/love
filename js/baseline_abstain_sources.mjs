import { fn_name } from "./fn_name.mjs";
export function baseline_abstain_sources() {
  "The functions a reading can go through and come back from with nothing, for a reason other than there being nothing to find.";
  "Everything a ratchet usually measures is the repo itself, and the repo is either there or nothing runs at all - so an empty answer from one of those readings means what it says. These are the two ways a reading can reach past the repo: onto the drive the human's own files live on, which unplugs, and out onto the network, which fails. Both come back empty-handed and neither says so, because an abstention and a clean bill of health are the same value.";
  "Named rather than spotted by shape, because the shape is a wide one - almost anything that touches a path could in principle be handed an absent one - and a rule guessing at it would go wrong in the direction that matters: quietly excusing the next reading that walked off the end of a missing disk.";
  let f_name = fn_name("folder_user_root");
  let f_name2 = fn_name("http_local");
  let sources = [f_name, f_name2];
  return sources;
}
