export function app_supper_folders_hash_key() {
  "The word standing before the chosen bible versions in the link the supper page shows, so a link somebody saved or sent still opens with the versions it was saved with.";
  let v = "sl";
  return v;
}
