import { path_join } from "./path_join.mjs";
import { data_given_baselines_folder } from "./data_given_baselines_folder.mjs";
export function example_files_command_refusals_baseline_path() {
  "Where the ratchet on commands the corpus never proves refuse keeps what the repo already carried. Reading it, rewriting it, and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path = path_join([
    data_given_baselines_folder(),
    "example_files_command_refusals_baseline.json",
  ]);
  return path;
}
