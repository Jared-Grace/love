export function app_code_label_solve_next() {
  "what a step-at-a-time quiz asks once a part of the line has already been solved: choose what to solve next";
  "Next rather than first, because first has already happened and the learner is looking at what it left behind. Asked for first a second time, a learner reads it as the press they just made not having counted.";
  let r = "Choose what to solve next: ";
  return r;
}
