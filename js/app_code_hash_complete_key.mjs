import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_hash_complete_key() {
  arguments_assert(arguments, 0);
  ("The word standing before what to mark finished in a code app link, so opening one link on a phone marks the lessons latest has as done.");
  let v = "complete";
  return v;
}
