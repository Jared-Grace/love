export function uplifting_references_url() {
  "the shared list of which verses the verses app draws from, kept as data in firebase (under the readable /bible/ prefix) so the list can change without rebuilding the app";
  let r = "/bible/uplifting/references.json";
  return r;
}
