import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function probes_at_once() {
  function_duplicate_kind_parallel();
  ("How many questions to put at the same time when each one starts a program and then waits for it");
  ("Asking one at a time leaves this machine idle for almost the whole run - six hundred allow rules put to the guard at a twentieth of a second each was half a minute of waiting, and it was the slowest single question in the repo-wide gate. A ceiling is kept anyway because these run beside the rest of the gate, and taking the whole machine for them would only move the delay somewhere else");
  ("One number covers every such question - putting rules to the guard, putting paths to the hook, asking the history who last touched a name - because what sets it is the cost of starting a program, which is the same whichever program it is");
  let limit = 16;
  return limit;
}
