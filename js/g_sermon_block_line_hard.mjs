export function g_sermon_block_line_hard() {
  "the hard per-block line cap: a reading block may never exceed this many sermon lines (= the longest single sermon in the corpus)";
  let r = 19;
  return r;
}
