export function js_operator_double_equal_symbol() {
  "the loose equals sign, which is to say two equals signs rather than three";
  "Held here rather than spelled where it is used, because two places want it now: the operator itself, and the class that says how strongly javascript works it out.";
  "No lesson teaches this sign - a learner is shown the three-sign form and only that - so it is here for the printer's sake and not the curriculum's.";
  let v = "==";
  return v;
}
