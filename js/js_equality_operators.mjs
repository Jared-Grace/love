export function js_equality_operators() {
  "The four ways this language asks whether two things are the same, written as they are written in code.";
  "These are the language's own spelling and not a choice this repo made, so they are safe to write out - nothing here can rename them.";
  let operators = ["===", "!==", "==", "!="];
  return operators;
}
