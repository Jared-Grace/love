export function file_path_temp_marker() {
  "what stands between a file name and the run that is part way through writing it - a file wearing this is a write in progress and not yet the file itself";
  let v = ".tmp.";
  return v;
}
