import { arguments_assert } from "./arguments_assert.mjs";
export function qa_commit_named_said_kept_count() {
  "How many of the nearest judgements keep the working that their names were read out of.";
  "It is more than one because a judging takes about two hours while the folder crosses the distance a judgement is kept for in about one. Keeping only the newest therefore lets go of the working under the judgement everybody is still reading, and puts nothing in its place until the next judging lands. Measured 2026-09-06: in that gap every red gate named nobody, and a gate naming nobody counts against every app, so the whole site was unshippable for reasons that had nothing to do with any app. Keeping the one behind it as well covers the handover.";
  "Two rather than more because the cost is a whole copy of the working, which is much the largest thing in the record, and one spare covers one handover. A third would cover a second handover that cannot happen, since only one judging runs at a time.";
  arguments_assert(arguments, 0);
  let count = 2;
  return count;
}
