export function gloss_explain_replaceable_any_is(explain) {
  "Whether an explanation may be written over, when the answer is that any of them may be.";
  "$plain explain";
  "the wording is read by the tests this one stands beside, and is looked at by none of them here.";
  "It is written down as a name rather than left out, because leaving it out would mean a word's wording is written everywhere by default and the writing is narrowed only when somebody remembers to narrow it. A settled wording that belongs at every place its word stands is a real choice and should have to be made, since the wording it writes over cannot be got back.";
  let replaceable = true;
  return replaceable;
}
