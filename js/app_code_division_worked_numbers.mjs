import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_division_worked_numbers() {
  arguments_assert(arguments, 0);
  ("the two numbers every worked division in this run of lessons is worked on: 14 divided by 4");
  ("One pair, asked for rather than typed, so a learner reading down the run meets one example over and over instead of a fresh pair on every screen. Recognising the numbers is most of what makes the fifth sentence of a shape read as the same sentence as the first.");
  ("They are held here rather than in the sentence that says the formula, because the screen that first sets integer division up needs them too and it says nothing about a formula.");
  ("Fourteen and four are what they are because four does not divide into fourteen, the leftover is small enough to see, and the quotient is three - so every number on the screen, the two given and the three worked out, is one a learner can check in their head.");
  let dividend = 14;
  let divisor = 4;
  let numbers = {
    dividend,
    divisor,
  };
  return numbers;
}
