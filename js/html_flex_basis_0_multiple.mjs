import { html_flex_basis_0 } from "./html_flex_basis_0.mjs";
import { each } from "./each.mjs";
export function html_flex_basis_0_multiple(list) {
  "Make what is written inside each of these parts of a row count for nothing when the row decides how wide to make them.";
  each(list, html_flex_basis_0);
}
