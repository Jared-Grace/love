export function process_first_id() {
  "The number the machine gives the first process it starts.";
  "One, on every machine that runs these programs. It is written here once because it is an identity rather than a setting: the first process is what everything else is descended from, and what a left-behind process is handed back to.";
  let id = "1";
  return id;
}
