export function html_loading_message_text() {
  "the single line shown under the loading spinner, shared by the runtime overlay (html_loading_overlay) and the build-time static splash (html_code_loading_splash) so the wording never drifts between them";
  let r = "One moment, please 🙏";
  return r;
}
