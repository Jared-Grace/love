import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_evil_pause_ms() {
  arguments_assert(arguments, 0);
  ("How long the hunter stands over somebody they have just killed before hunting again. Long enough for the player to see what happened and where.");
  let ms = 1600;
  return ms;
}
