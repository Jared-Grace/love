export function qa_gate_recent_seconds() {
  "How lately a file has to have been committed for a complaint about it to read as somebody's live work rather than as standing debt.";
  "An hour, which is a guess written down rather than a measurement. It wants to be longer than the gap between one peer's commits and shorter than the gap between one peer's topics, and neither of those has been measured - so it is put in a name of its own, where moving it is one edit that stands in the log.";
  "The reading it feeds is advice and never a refusal, so a window set wrongly costs a reader one wrong guess about whose work they are looking at. That is the whole exposure, and it is why a guess is allowed to stand here at all.";
  let seconds = 3600;
  return seconds;
}
