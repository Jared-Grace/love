export function app_shared_screen_tab_key_last() {
  "the name the key of this tab's most recent pick is kept under";
  "EVERY pick a tab has made is still in storage - the lesson it opened and the review it opened both - so nothing in the values themselves says which of them the learner was last looking at. What does say it is which key was written last, and this is where that name is put.";
  let key = "tab_key_last";
  return key;
}
