import { bible_glyph_chapters_verse_marks_underdrawn_baseline_path } from "./bible_glyph_chapters_verse_marks_underdrawn_baseline_path.mjs";
import { baseline_known_growth_assert } from "./baseline_known_growth_assert.mjs";
export async function bible_glyph_chapters_verse_marks_underdrawn_baseline_growth_assert(
  known,
) {
  "Refuse to record an offender the baseline did not already hold. A ratchet that can be rewritten in both directions is not a ratchet, and the rewrite would be reached for at exactly the moment the gate went red, which is the moment it was doing its job.";
  "The first seeding has no file to compare against and is allowed, and so is any rewrite that only drops names.";
  let path = bible_glyph_chapters_verse_marks_underdrawn_baseline_path();
  await baseline_known_growth_assert(
    known,
    path,
    "recording these as known would bless a new offence rather than repair it - these verses draw a mark and leave another word the table seats on that same mark standing in plain English letters, so draw that word too, or say in the chapter's prose why it stays in letters",
  );
}
