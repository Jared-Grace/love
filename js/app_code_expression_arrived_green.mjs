import { app_code_expression_colored_slowly } from "./app_code_expression_colored_slowly.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
import { html_style_code_green_nowrap } from "./html_style_code_green_nowrap.mjs";
export function app_code_expression_arrived_green(span) {
  arguments_assert(arguments, 1);
  ("pick out the one number in a written line that has just arrived in it, in the same green the sentence above named it in - and let that green come up rather than be there already");
  ("Green because it is the same number the learner watched travel down onto the line a moment ago, wearing this colour the whole way. Saying it again in the sentence that reads the new line back to them is what joins the two: the thing that moved, and the line it moved into.");
  ("It comes up rather than being there from the first frame, because the words around it are already finished by then. A colour that is simply present is read as part of how the sentence is written; the same colour arriving is read as pointing at something.");
  app_code_expression_colored_slowly(span);
  html_style_code_green_nowrap(span);
}
