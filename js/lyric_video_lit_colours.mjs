import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_lit_colours() {
  "The colours a sung word may light to, each under the plain name a document writes, spelled the way the renderer reads a colour.";
  "★ A DOCUMENT NAMES A COLOUR AND NEVER SPELLS ONE. The renderer reads a colour blue first and red last, which is backwards from the way every other thing writes one, so a person writing gold into a document would write the number for a light sky blue and see it on the screen before they saw the mistake. A name cannot be written backwards, and a name nothing answers to is caught where it is read rather than drawn.";
  "★ RED IS FIRST BECAUSE IT IS WHAT A LINE THAT SAYS NOTHING GETS, and that is the whole of why these are held apart from the function that picks one: the resting colour is a fact about this list, and which line wants which is a fact about a song.";
  "The three are what the crucifixion, the resurrection and the grave ask for in a song that goes through all three. A fourth is added when a song needs one, not before.";
  arguments_assert(arguments, 0);
  let colours = {
    red: "&H0000FF&",
    gold: "&H00D7FF&",
    slate: "&HC08060&",
  };
  return colours;
}
