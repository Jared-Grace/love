import { arguments_assert } from "./arguments_assert.mjs";
export function js_string_quote() {
  arguments_assert(arguments, 0);
  ("the mark that begins a string and ends it again");
  ("Three places were each spelling this character for themselves - the one that wraps a word into code, the lesson that introduces strings, and the lesson that writes a string out - and a curriculum that taught one mark while writing another would be teaching something false rather than merely inconsistent.");
  let quote = '"';
  return quote;
}
