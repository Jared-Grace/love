// Syntax color for punctuation (braces, parens, semicolons, operators) — a
// de-emphasized grey so structure recedes and names/keywords/literals stand out.
export function app_shared_color_punctuation() {
  let r = "#6a737d";
  return r;
}
