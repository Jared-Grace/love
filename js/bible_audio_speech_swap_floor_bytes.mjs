import { multiply } from "./multiply.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function bible_audio_speech_swap_floor_bytes() {
  "How little swap this machine may have left before a worker stops taking another chapter.";
  "★ THIS IS THE MARK THAT ACTUALLY SAW THE WALL COMING, AND IT IS THE REASON THERE ARE TWO MARKS RATHER THAN ONE. On 2026-08-28 this machine ran out of memory five times in three and a half days, all five inside one recording run, and the kernel killed the browser three times and the editor once. Available memory read about seven gigabytes throughout. Swap free read 382 megabytes of twenty four gigabytes. Only the second figure was anywhere near its limit, because a kernel with free swap still has somewhere to put a page and a kernel without it has only killing left.";
  "★ FOUR GIGABYTES IS TEN TIMES THE MARGIN THAT FAILED AND STILL ONLY A SIXTH OF THIS MACHINE'S SWAP. It is set well clear rather than close, because the run being stopped costs a relaunch and nothing else, while being wrong in the other direction costs the person at the keyboard their open windows.";
  arguments_assert(arguments, 0);
  let left = multiply(1024, 1024);
  let a_gigabyte = multiply(left, 1024);
  let bytes = multiply(4, a_gigabyte);
  return bytes;
}
