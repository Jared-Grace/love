import { arguments_assert } from "./arguments_assert.mjs";
import { folders_function_named_orphaned_walked } from "./folders_function_named_orphaned_walked.mjs";
import { property_get } from "./property_get.mjs";
export async function folders_function_named_orphaned() {
  arguments_assert(arguments, 0);
  ("Every folder of stored data named after a function that no function answers to any more.");
  ("The walk itself moved one name along when the gate above needed to know how much had been looked at, and this is what the three readers that only want the list kept. Two of them move folders about and one rewrites the record; none of them has any use for the count, and making all three take a record apart to reach past it would have spread a number nobody was asking for across every caller.");
  ("Nothing decided here. Where the folders are, which names still answer, what order they come back in - all of that is settled next door, so the two cannot disagree about what is orphaned.");
  let told = await folders_function_named_orphaned_walked();
  let orphaned = property_get(told, "orphaned");
  return orphaned;
}
