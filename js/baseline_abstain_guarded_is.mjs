import { arguments_assert } from "./arguments_assert.mjs";
import { list_filter_text_includes } from "./list_filter_text_includes.mjs";
import { list_empty_not_is } from "./list_empty_not_is.mjs";
export function baseline_abstain_guarded_is(imports) {
  "whether a ratchet writer importing these names refuses to write from a reading that reached nothing";
  arguments_assert(arguments, 1);
  ("the words present assert are looked for anywhere inside a name rather than at the end of it, because what each writer has to check the presence of differs - one asks after two named folders, another would ask after a page it downloads - and only the middle of the name stays the same");
  ("kept apart from the sweep, and handed a plain list of names rather than a function to go and read. the sweep comes back empty on a healthy repo, so a broken judgment and a working one look identical there, and this is the only place the difference can be seen. a list is also something a corpus can invent, which a real function's imports are not");
  let guards = list_filter_text_includes(imports, "present_assert");
  let guarded = list_empty_not_is(guards);
  return guarded;
}
