import { arguments_assert } from "./arguments_assert.mjs";
export function qa_gate_population_words() {
  "The words a gate can call a number by that promise how much it looked at, rather than how much was wrong with what it found.";
  "A key's name is half of what it claims, and it can lie on its own. A gate answering how much it checked, where how much it checked is the length of its offenders, says nothing checked on every run it passes - and says the same on the run its sweep stopped reaching anything. The number was already wrong; the word is what made it unreadable, because a reader who saw the word never thought to open the file.";
  "Matched whole rather than looked for inside the key, and that is the whole of why this is a list of words instead of a search. A gate that names its offenders the ones it could not check spells the promise inside a word that means the opposite of it, and a search would accuse exactly the gate that was being careful.";
  "Kept to words that can only mean the looking. A word that could fairly name either side - the cases found, the entries recorded - is left out, because this exists to catch a gate whose word and number disagree, and accusing one that was honest costs an afternoon of reading to undo.";
  arguments_assert(arguments, 0);
  let words = [
    "checked",
    "examined",
    "inspected",
    "looked",
    "opened",
    "reached",
    "scanned",
    "visited",
    "walked",
    "watched",
  ];
  return words;
}
