export function app_search_query_hash_key() {
  "The word an app's address uses to carry what was searched for, so the search survives a link being shared or a page being reloaded.";
  let v = "q";
  return v;
}
