export function machine_path_literals_allowed() {
  "The functions allowed to write out a folder belonging to one machine, each with the reason it is allowed to, so that adding a name here has to be argued rather than just typed.";
  "Named one at a time rather than counted. A count says how many are lived with and nothing about which, so the day a new one appears the count is the only thing that moves, and the reader has no way to tell the new one from the old ones. A name has to be added by somebody who can say why, and the why is here where they will read it.";
  "Five, and each for a different reason - so if a sixth wants adding and does not read like any of these, that is the sign it is a real fault rather than another of these.";
  let allowed = {
    machine_path_prefixes:
      "It is the list of openings itself, so the reading finds its own words. Holding them somewhere else moves the same writing one step along and answers nothing.",
    folder_drive_removable:
      "The one place the drive that can be unplugged is spelled. That is the whole point of it, and four other functions used to spell it as well.",
    folder_user_root:
      "The one place the machine's own store is spelled, which is the same reason the removable drive has one. It is written out rather than worked out from where the file sits because the gates run inside a frozen copy of the tree put somewhere else on disk, and a path derived from that copy's own place would name a folder inside the copy - which exists, and is empty, and so answers every question with nothing rather than failing.",
    folders_moved_expected:
      "A record of where folders used to be and where they went. It is history, and a record that followed a later move would stop being a record of anything.",
    permission_rule_file_cases:
      "Practice material for the rules about who may write where. A rule is matched as plain text, so a case that worked its folder out would work out the same wrong answer as the thing it is checking and prove nothing at all.",
  };
  return allowed;
}
