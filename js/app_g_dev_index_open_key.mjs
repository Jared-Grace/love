import { fn_name } from "./fn_name.mjs";
export function app_g_dev_index_open_key() {
  ("the session-storage key under which the g dev index remembers which category nodes are expanded, read by ",
    fn_name("app_g_dev_index"),
    " and written by ",
    fn_name("app_g_dev_index_open_persist"));
  let v = "dev_index_open";
  return v;
}
