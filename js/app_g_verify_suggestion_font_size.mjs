export function app_g_verify_suggestion_font_size() {
  "the size of the box a suggestion is typed into, at the plain body size so what is written there reads the way it will read once it is sent";
  let v = "1em";
  return v;
}
