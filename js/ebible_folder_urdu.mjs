export function ebible_folder_urdu() {
  "The name of the folder the Urdu bible is kept in, spelled once here so the several places that reach for it cannot come to disagree about it.";
  let u = "urdoucv";
  return u;
}
