export function app_code_lesson_quiz_token_select_count_badge_inside_percent() {
  "how much of the count badge lies over the piece it belongs to, as a part in a hundred; the rest of it hangs off the right hand side.";
  "It is asked for by name rather than written where it is used, because two places have to agree about it and they say it in different words. One places the badge, pulling it back by this much of its own width so it overlaps the piece a little. The other keeps room in the row for the part that hangs out, which is everything this is not. Written out twice, they would drift, and the row would close up over the badge again.";
  let v = 15;
  return v;
}
