import { app_code_expression_colored_over } from "./app_code_expression_colored_over.mjs";
import { app_shared_animation_duration_quick } from "./app_shared_animation_duration_quick.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_expression_colored_quickly(component) {
  arguments_assert(arguments, 1);
  ("say that whatever this piece is coloured next, it takes on that colour over the short while rather than over the whole one");
  ("The short while, because this is the colouring that carries no news: the blue is being let GO, and it is let go once everything it was there to say has already been said and read. Given the whole while it reads as one more thing to wait through at the very moment the learner is ready for the next line.");
  let duration = app_shared_animation_duration_quick();
  app_code_expression_colored_over(component, duration);
}
