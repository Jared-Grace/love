import { arguments_assert } from "./arguments_assert.mjs";
import { function_literal_route_generic } from "./function_literal_route_generic.mjs";
import { js_literal_calls_set_any } from "./js_literal_calls_set_any.mjs";
export async function function_literal_route_any(f_name, getter_f_name) {
  arguments_assert(arguments, 2);
  ("Points every place one file spells a written word at the function that hands");
  ("that word back, wherever in the file the word stands, and repairs the imports.");
  ("The two siblings beside this each read one kind of site and refuse a file");
  ("holding only the other kind. This one refuses only a file holding neither, which");
  ("is the refusal that actually means something: the word is not there.");
  let r = await function_literal_route_generic(
    f_name,
    getter_f_name,
    js_literal_calls_set_any,
  );
  return r;
}
