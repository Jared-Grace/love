export function js_operator_double_asterisk_symbol() {
  "the raising to a power symbol";
  "The same two marks mean bold in a written-out note, which is not this and is written out where it is meant.";
  let v = "**";
  return v;
}
