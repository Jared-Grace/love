export function app_code_label_value() {
  "The word a quiz puts in front of what an expression comes out as, both where the answer is shown and where it is asked for.";
  let v = "value: ";
  return v;
}
