import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_grow_surge() {
  arguments_assert(arguments, 0);
  ("How far past her new height the player rises before settling back onto it - a multiple of the height she is growing to.");
  ("The overshoot is what makes the growing READABLE. What she permanently gains is a small share of her height, and a small change shown at its own size is a change the eye has to be told about; the same change shown by rising well past it and coming back down is a movement, and a movement is seen without being explained. So the swell is deliberately larger than the thing it is showing.");
  ("It is bigger than it was, because the human watching it reported the growing as too little. It cannot grow without limit - past a point she is bouncing rather than rising, and a bounce reads as a spring in the picture instead of strength in the person - but well past the old figure there is room, since the whole gesture is a rise and a settle rather than a wobble.");
  ("She always comes back DOWN onto her real height, so this changes nothing about how tall she is. It is a way of showing a number, never the number.");
  let surge = 1.25;
  return surge;
}
