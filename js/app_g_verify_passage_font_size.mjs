export function app_g_verify_passage_font_size() {
  "the size of the passage itself on the verify page, larger than everything around it because reading it closely is the whole task there";
  let v = "1.3em";
  return v;
}
