export function app_en_learn_bible_gloss_urdu_explain_pointer_openings() {
  "The two ways an Urdu explanation opens when it begins by calling the word the same one the reader has already met, rather than by saying what the word is.";
  "Both are the one Urdu word for ‘that same’, written with and without the mark over its first letter. Which of the two an explanation used was never a decision anybody made - the store simply holds both spellings - so a reader looking for one of them and not the other would find about half of what is there and would report the other half as clean.";
  "An opening is not a fault on its own and must not be read as one. An explanation is free to say a word is the same preposition met above and then go on to say what it marks here, and thousands of them do exactly that. This names where a pointer starts; whether the pointer is all there is, and whether what it points at was ever written, are separate questions asked elsewhere.";
  let openings = ["وہی", "وُہی"];
  return openings;
}
