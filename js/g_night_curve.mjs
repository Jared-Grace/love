export function g_night_curve() {
  "the NIGHT tone-curve control points (feComponentTransfer table, input 0→1 in equal steps). unlike brightness (dims everything, killing the lights) or contrast (crushes every dark tone to the SAME black), a curve does it all at once: it DARKENS the mid-tones, and blooms the top to full WHITE so a light stays a light. the BOTTOM is lifted WELL up (floor .32) so the night is a hazy, faded blue — this is deliberate: the tint darkens the CHARACTERS too (they're z-1, below the z-5 tint), and a dark character on a dark tile lost all contrast. keeping the darkest tones a lifted mid-blue and the ground LIGHTER than a character's dark hair/clothing makes dark characters read as clear silhouettes. earlier floors (.01, .06, .14, .24) all crushed the characters. the top still blooms to white so lights stay lights. day interpolates from the straight IDENTITY ramp toward this by `darkness` (g_phase_curve)";
  let curve = [0.32, 0.36, 0.4, 0.44, 0.48, 0.55, 0.63, 0.78, 1, 1];
  return curve;
}
