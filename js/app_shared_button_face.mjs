import { app_shared_button_style } from "./app_shared_button_style.mjs";
import { html_style_padding_em } from "./html_style_padding_em.mjs";
export function app_shared_button_face(component) {
  "dress anything at all as a button in these apps: the fill, the edge, the corners, and the room its word is given inside them";
  "Held apart from the making of a button, because the thing wearing this face is not always a button element. A link that leads somewhere is an anchor and must stay one - a browser only offers to open something in a new tab, or to copy where it goes, when the thing pressed really is a link - and it should still look like every other thing on the page a reader presses.";
  app_shared_button_style(component);
  html_style_padding_em(component, "0.3");
}
