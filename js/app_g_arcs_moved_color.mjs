import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_arcs_moved_color() {
  "The ink a moved line is marked in on the review bench - a burnt amber, dark enough to be read as words rather than as a tint.";
  "IT IS NEITHER OF THE TWO VOICE COLOURS AND THAT IS THE WHOLE REQUIREMENT ON IT. A person's own words are drawn in a deep rose or a deep blue by gender, and a mark meaning this line was rewritten has to be tellable from both at a glance, on a line that is itself already drawn in one of them.";
  "IT IS ONE FUNCTION BECAUSE THE MARK IS MADE IN THREE PLACES. The bar down the side of the line that moved, the box holding what it used to say, and the count in the turn's heading are one signal wearing one colour; spelled three times they drift apart, and a reviewer then has to learn that three different colours all mean the same thing.";
  arguments_assert(arguments, 0);
  let color = "#b4530a";
  return color;
}
