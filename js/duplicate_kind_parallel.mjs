export function duplicate_kind_parallel() {
  "The answer meaning two functions look alike because the language gives two different types the same operation, not because one idea was written twice.";
  "Cutting a piece out of a list and cutting a piece out of some words are both spelled slice, and both read the same once the names are taken away - but they are two operations on two types, and collapsing them would throw away the type the name promises.";
  let r = "parallel";
  return r;
}
