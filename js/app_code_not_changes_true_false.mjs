import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_bang_symbol } from "./js_operator_bang_symbol.mjs";
import { js_keyword_true } from "./js_keyword_true.mjs";
import { js_keyword_false } from "./js_keyword_false.mjs";
export function app_code_not_changes_true_false(lead) {
  arguments_assert(arguments, 1);
  ("the one sentence the ! lessons say about what a ! does, as the pieces of a row, held in one place so the lessons saying it cannot drift into saying it almost the same way. The caller passes only its own lead-in, because the lessons that recall it open with Remember and the one that is arguing from it opens with Remember: a.");
  ("Written as pieces rather than as a string because three of the words in it are code - the mark itself and the two keywords - and a sentence handed over already joined has nowhere left to put a code chip.");
  ("THE APPLYING IS DONE, not the rule left standing. The opposite of what stands after it is a rule the learner still has to work; true becomes false and false becomes true is that rule already worked, and on a line whose next step is a value that is what they need. The recall lesson said the rule and the lesson after it said the applying, so a learner reading the two in a row met the same fact worded two ways with nothing saying they were the same fact.");
  ("A lesson with something of its OWN to say about the ! must not reach for this - it should say its own thing. Paraphrase earns a card only when it comes from a new angle; without one, this function is the answer.");
  let symbol = js_operator_bang_symbol();
  let true_word = js_keyword_true();
  let false_word = js_keyword_false();
  let parts = [
    lead,
    symbol,
    " changes ",
    true_word,
    " to ",
    false_word,
    " and ",
    false_word,
    " to ",
    true_word,
  ];
  return parts;
}
