export function seconds_day() {
  "How many seconds a day is.";
  "Written once and asked for by name, so that two places counting in days cannot disagree about how long one is, and so that a reader meeting the number in the middle of a sum is told what it is rather than left to recognise it.";
  let seconds = 86400;
  return seconds;
}
