import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_bless_arrow_animation_name() {
  arguments_assert(arguments, 0);
  ("What the prayer game's nodding arrow is called in the page's own stylesheet.");
  ("The keyframe writing the movement and the element asking for it have to spell the same word, and neither of them can see the other. Spelled once, they cannot disagree; spelled twice, a change to one leaves an element naming an animation nothing defines - and an arrow that never starts moving throws nothing, warns nothing, and looks exactly like an arrow somebody decided should be still.");
  let name_animation = "blessArrowNod";
  return name_animation;
}
