import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_container_light_blue } from "./app_code_container_light_blue.mjs";
import { html_div_cycle_code } from "./html_div_cycle_code.mjs";
export function app_code_lesson_expression_choose_order_pair_intro(root) {
  arguments_assert(arguments, 1);
  ("the one sentence that says what is different about this lesson, in a card of its own");
  ("It makes the card rather than being handed one, so that a lesson with nothing left to say can hand in a painter that draws nothing at all and leave no empty card behind - the shape the shared run above these lessons expects of every closing sentence.");
  let parent = app_code_container_light_blue(root);
  ("It names the one thing that changed. Every line the learner has pressed so far had exactly one part with a value on each side of it; here there are two, and what settles between them is where they stand.");
  ("★ IT USED TO SAY YOU MAY CHOOSE EITHER ONE, AND THAT WAS NOT TRUE OF THE COMPUTER. Either part can go without changing what a line of plain numbers comes to, which is a fact about arithmetic; JavaScript itself works out the left side of an operator all the way down before it touches the right, so the order is settled. A learner told they may choose would be holding a rule that has to be taken back the first time a part counts, prints or fetches - and taking a rule back costs more than never giving it.");
  html_div_cycle_code(parent, [
    "Now two parts have a value on each side, and the left one goes first",
  ]);
}
