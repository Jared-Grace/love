export function app_shared_caret_open() {
  "the mark on a group heading that is open, pointing down at what it holds - one place so every screen with foldable groups shows the same arrow";
  let v = "▾";
  return v;
}
