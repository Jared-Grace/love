export function app_shared_color_gold_text() {
  "the base (richer) gold text on a dark background (gold = God's word); used for God's-LEADING PROSE — the dove HS-warning message — while the lighter app_shared_color_gold_text_light is used for Scripture VERSES; pairs with app_shared_scripture_gold, the gold SURFACE form";
  let v = "#ffe680";
  return v;
}
