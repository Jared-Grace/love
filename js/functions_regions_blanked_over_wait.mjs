import { arguments_assert } from "./arguments_assert.mjs";
import { functions_ast_offenders_walked_generic } from "./functions_ast_offenders_walked_generic.mjs";
import { js_regions_blanked_over_wait } from "./js_regions_blanked_over_wait.mjs";
export async function functions_regions_blanked_over_wait() {
  arguments_assert(arguments, 0);
  ("Audit: every function in this repo that empties a part of the screen, waits, and only then writes into it again - handed back beside how many functions were opened to find them.");
  ("The judgment itself is next door, asked of one file at a time, so the same question can be asked of a single file without walking the folder.");
  ("HOW MANY WERE OPENED TRAVELS OUT WITH THE ANSWER, because finding nothing is what this says on a good day and it is also what it says when it has been pointed at a name that has moved. The count falling to nothing while the verdict stays green is the only thing that has ever told those two apart.");
  ("A file that will not parse is passed over rather than reported. A broken file is a different complaint with a gate of its own, and failing here would report the same breakage twice under a name explaining nothing.");
  let walked = await functions_ast_offenders_walked_generic(
    js_regions_blanked_over_wait,
    "regions",
  );
  return walked;
}
