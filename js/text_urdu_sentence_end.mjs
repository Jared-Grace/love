export function text_urdu_sentence_end() {
  "The mark that ends a sentence in Urdu.";
  "Urdu is written in a Perso-Arabic script and closes a sentence with its own full stop rather than with the dot English uses. Anything that cuts Urdu prose into sentences has to be told this, because the dot it would otherwise reach for occurs in Urdu only inside numbers and abbreviations.";
  let end = "۔";
  return end;
}
