import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_evil_pace_ms() {
  arguments_assert(arguments, 0);
  ("How long one step of the hunt takes. Quicker than the crowd around them, so the hunter reads as somebody moving with purpose, and slow enough that the player has time to get there.");
  let ms = 380;
  return ms;
}
