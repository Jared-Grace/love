export function ebible_version_books_upload_name() {
  "The name the list of a bible's books is uploaded under, spelled once here so whoever writes it and whoever reads it back cannot disagree about where it is.";
  let v = "books";
  return v;
}
