import { fn_name } from "./fn_name.mjs";
export function g_boundary_rather_not() {
  ("the deferral itself, said to somebody you have met before: still rather not get into that. the STILL is what makes it belong to a second meeting — it answers a question that has been asked before, and would read as a mistake to a stranger, which is why the just-met boundary asks for time instead. shared by ",
    fn_name("g_boundary"),
    " and ",
    fn_name("g_boundary_believer"),
    ", each of which offers it alongside a second phrasing of its own");
  let r = "I'd still rather not get into that, though.";
  return r;
}
