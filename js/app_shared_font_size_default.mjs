import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_font_size_default() {
  function_duplicate_kind_parallel();
  ("The size an app opens at before anybody has chosen one, as a multiple of the size this reader's own browser is set to.");
  ("1, so the words here are exactly the size the words on every other site are. A reader who browses zoomed, or who set a larger text in their browser because they need one, gets that same size on this site - which is the whole of what a base size is for. It used to be 20 pixels: a quarter larger than the web's own size, so every page here read bigger than the page they came from, and a count of pixels ignored their browser setting outright.");
  ("Named rather than written where it is used, because two pages opening at two different sizes is a difference nobody asked for and nothing would have said so - the second page would simply have looked wrong beside the first, and there is no reading of either that shows why.");
  arguments_assert(arguments, 0);
  let value = 1;
  return value;
}
