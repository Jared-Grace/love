export function g_sky_tone_id() {
  "the DOM id of the shared SVG tone-curve <filter> the sky's backdrop-filter references (url(#…)). one filter, rewritten per phase";
  let id = "g_sky_tone";
  return id;
}
