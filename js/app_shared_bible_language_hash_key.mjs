export function app_shared_bible_language_hash_key() {
  "The word standing before the language in the link a bible page shows, so a link somebody saved or sent still opens in the language it was saved in.";
  let v = "l";
  return v;
}
