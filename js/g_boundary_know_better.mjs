import { fn_name } from "./fn_name.mjs";
export function g_boundary_know_better() {
  ("the deferral said to somebody you have only JUST met — maybe once I know you a little better. it asks for time rather than refusing, so it leaves the door open and tells the player what would open it: come back. that is the whole difference from ",
    fn_name("g_boundary_rather_not"),
    ", which is what a second meeting says. shared by ",
    fn_name("g_boundary"),
    " and ",
    fn_name("g_boundary_believer"),
    ": a stranger is a stranger either way, so the first meeting sounds the same whether or not the person believes");
  return "Maybe once I know you a little better?";
}
