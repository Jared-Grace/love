import { arguments_assert } from "./arguments_assert.mjs";
import { function_literal_route_generic } from "./function_literal_route_generic.mjs";
import { js_literal_key_calls_set } from "./js_literal_key_calls_set.mjs";
export async function function_literal_route_key(f_name, getter_f_name) {
  arguments_assert(arguments, 2);
  ("Points the places one file names a field with a written word at the function");
  ("that hands that word back, and repairs the imports.");
  ("The sibling command routes everywhere a call may stand except the naming of a");
  ("field, and it leaves those alone for the pass that promotes names, where");
  ("turning a field name into a reference to a function of the same name is");
  ("catastrophic. Writing a call there is not that, and it is the one repair a word");
  ("kept in a browser address needs most: a page address is an object, and the word");
  ("under which a chapter or a language sits in it is named exactly this way.");
  ("Thirteen such sites were edited by hand in one sitting before this existed,");
  ("each one followed by the canonicalising pass, and the atom that does the work");
  ("had been sitting in the repo the whole time with nothing calling it.");
  let r = await function_literal_route_generic(
    f_name,
    getter_f_name,
    js_literal_key_calls_set,
  );
  return r;
}
