import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function ebible_letter_accounted_spaces() {
  function_duplicate_kind_parallel();
  ("How wide the record of what has been said to eBible is indented - one space a level, and narrow on purpose.");
  ("The record is read and edited by hand, and its values are whole sentences of reasoning about a chapter. A wider indent buys nothing on a file shaped like that and pushes the sentences off the side of the window.");
  ("It is written down here rather than measured off the file, because a command that rewrote the file at the wrong width would then be measured as correct by its own damage. This is the width the file is meant to be at, and a gate holds it to this rather than to itself.");
  arguments_assert(arguments, 0);
  let spaces = 1;
  return spaces;
}
