export function app_g_day_conversation_slices() {
  "how many SLICES one stubbed #day_unbelievers conversation costs — 3, standing in for its turn-pairs (opener + matches). single source so the day's slice BUDGET (app_g_day_slices_total) and what a stub-convert SPENDS (app_g_day_convert) can never drift; if they did, the sky would miss sunset. becomes the real conversation's part-count once the conversation is un-stubbed";
  return 3;
}
