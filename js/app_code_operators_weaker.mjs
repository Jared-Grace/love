import { arguments_assert } from "./arguments_assert.mjs";
import { js_operator_and_symbol } from "./js_operator_and_symbol.mjs";
export function app_code_operators_weaker() {
  arguments_assert(arguments, 0);
  ("the operators a code lesson solves after everything else, by their symbols: &&");
  ("A third class below the comparisons, because a line may now hold a comparison and an && at once and the two are not of equal strength: 3 < 5 && 2 < 4 works both comparisons out and only then combines them. Left in the same class as a comparison, the line would print with brackets round each side that a learner would then have to be told to ignore.");
  let v = js_operator_and_symbol();
  let symbols = [v];
  return symbols;
}
