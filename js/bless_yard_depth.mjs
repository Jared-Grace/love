import { arguments_assert } from "./arguments_assert.mjs";
export function bless_yard_depth() {
  arguments_assert(arguments, 0);
  ("How many rows of ground lie between the fronts of the houses and the road, pavement included.");
  ("THREE, with the pavement the middle one of them - grass, pavement, grass. It was two rows of grass with the pavement above them, sitting straight against the doorsteps, and the street asked for was one where the pavement has green on both sides of it. That is what a pavement usually has: a verge between it and the houses, and a verge between it and the traffic.");
  ("The pavement being in the MIDDLE is the whole of why this number is odd. One row of grass above and one below leaves the poured strip centred in the green, and the driveways cross the top row, the pavement and the bottom row in one straight run from the doorstep to the kerb.");
  ("It is the same depth on every street, unlike nearly everything else about a block. A yard is not how the player tells one road from another - the road, the fronts and the roofs already do that - and a depth that varied would make two streets different lengths in the one direction the player is not walking.");
  ("The grass here, the grass in the gaps between the houses, and the ground a low house leaves over inside its own slot are all the same lawn seen in pieces. They meet with nothing between them, so what the player sees is green wrapped round the row of houses with one pale line drawn along it and a driveway crossing from every door.");
  let depth = 3;
  return depth;
}
