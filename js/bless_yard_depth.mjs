import { arguments_assert } from "./arguments_assert.mjs";
export function bless_yard_depth() {
  arguments_assert(arguments, 0);
  ("How many rows of grass lie between the front of a house and the pavement.");
  ("ONE. What the row is for is to stop the front door opening straight onto the pavement, and one row of green does the whole of that: the house has ground of its own in front of it, and the street outside is plainly not it. A second row would say the same thing twice and buy it by pushing every house that much further from the road, on a street the player has to walk the length of.");
  ("It is the same row on every street, unlike nearly everything else about a block. A yard is not how the player tells one road from another - the ground, the fronts and the roofs already do that - and a depth that varied would make two streets different lengths in the one direction the player is not walking.");
  ("The grass here and the ground a low house leaves over inside its own slot are the same yard seen in two pieces. A house that stands back from its slot leaves a row behind, and that row is grass for the same reason this one is; the two meet with nothing between them, so a set-back house simply has a deeper garden than its neighbour.");
  let depth = 1;
  return depth;
}
