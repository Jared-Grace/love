import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_lead_seconds() {
  function_duplicate_kind_parallel();
  ("How long before a line is sung its card is put on the screen.");
  ("★ THIS IS A PROPERTY OF THE SHOWING, NOT A CLAIM ABOUT WHEN THE LINE WAS SUNG. The document says what was heard and is not touched; this says how what was heard is handed to somebody reading it, and it sits beside the fade for that reason. Reaching for the document instead would write the lead into the one record of the singing, so a later argument about how far ahead to reach could no longer be settled against when the words actually came - and it would have to be written into every document there is, one at a time, to say one thing once.");
  ("★ IT IS LONGER THAN THE FADE IN ON PURPOSE. A card fading in over a quarter of a second and beginning that fade on the first note is only fully lit a quarter of a second after the word has gone by, which is later than showing nothing at all would have felt. This clears the fade with about a seventh of a second to spare, so the words are standing still and readable at the moment the line is sung.");
  ("★ BOTH ENDS OF A CARD MOVE, NOT JUST ITS BEGINNING. The cards run one straight into the next - a card lets go a twentieth of a second before the following one lands - so pulling only the beginnings earlier would leave every pair of neighbours on the screen together for the length of the lead, at the same point, one printed over the other. Moving the whole card keeps the run unbroken and gives the reading ahead the lead is for: the last moment of a line being sung is already showing the line that follows it.");
  ("A lead that reaches back past the start of the song is not written as a time before the start, and that is not done here. The writer that spells a moment out clamps it, because it is the only place that knows what a moment is allowed to look like.");
  arguments_assert(arguments, 0);
  let seconds = 0.4;
  return seconds;
}
