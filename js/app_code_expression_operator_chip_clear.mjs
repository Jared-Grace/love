import { arguments_assert } from "./arguments_assert.mjs";
import { html_box_shadow_set } from "./html_box_shadow_set.mjs";
import { html_style_background_color_set } from "./html_style_background_color_set.mjs";
import { html_style_padding_x } from "./html_style_padding_x.mjs";
import { html_style_set } from "./html_style_set.mjs";
export function app_code_expression_operator_chip_clear(span) {
  arguments_assert(arguments, 1);
  ("take the pale chip back off an operator, so it goes on reading as part of the line rather than as something to press");
  ("The chip is the whole of how this screen says press me. An operator that cannot be pressed and still wears one is a control that answers nothing, and a learner pressing it learns only that the screen is broken.");
  ("All four of the things the chip put on go together - the fill, the lettering, the edge and the room either side - because any one of them left behind is still a mark, and a mark on a line of code is read as meaning something.");
  html_style_background_color_set(span, "transparent");
  html_style_set(span, "color", "inherit");
  html_box_shadow_set(span, "none");
  html_style_padding_x(span, "0");
}
