import { app_code_lesson_expression_parentheses_both_sides } from "./app_code_lesson_expression_parentheses_both_sides.mjs";
export function app_code_lessons_prod_last_fn() {
  "the last lesson shown to somebody reading the built site: the ones written after it are finished code and have not been read through by the person handing the track to a learner yet";
  "A learner meeting a lesson nobody has read through has no way to tell it from one that has been, so a run that stops at a known-good place is worth more to them than a longer one they have to judge for themselves. Every lesson after this one is still built, still opens on the working copies, and costs nothing to release - moving this name down the list is the whole of releasing them.";
  "This one, because it is where a learner has just been taken all the way from one operator in a line to three of them in their head, and the next thing after it starts a new kind of line. A run cut in the middle of a climb reads as the track having stopped working.";
  "The lesson is named by its own function rather than by a count, so it goes on meaning this lesson when lessons are put in ahead of it, and a rename follows it.";
  let fn = app_code_lesson_expression_parentheses_both_sides;
  return fn;
}
