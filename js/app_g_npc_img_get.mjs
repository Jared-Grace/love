import { app_g_npc_property_get_generic } from "./app_g_npc_property_get_generic.mjs";
export function app_g_npc_img_get(npc) {
  "the picture of a person, found by WHO that person is - the twin of the same lookup for the cross drawn over them, and keyed the same way so the two are found and moved together";
  "this name is the drawer as well as the way in - it hands ITSELF over as the store, so the pictures and the crosses stay in separate drawers however alike the two lookups read";
  let img = app_g_npc_property_get_generic(app_g_npc_img_get, npc);
  return img;
}
