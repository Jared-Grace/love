export function app_emoji_bible_tradition_orthodox() {
  "The name of the Eastern Orthodox drawing.";
  "The name is stored on the reader's device, so it is spelled in one place and read from there rather than written out wherever it is needed. A word that has left this repo cannot be changed later without stranding every reader who already chose it.";
  let name = "orthodox";
  return name;
}
