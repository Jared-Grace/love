import { permission_rows_grant_generic } from "./permission_rows_grant_generic.mjs";
import { permission_rule_grant_verdict_context } from "./permission_rule_grant_verdict_context.mjs";
export async function permission_rows_grant_verdicts(rows, key) {
  "For every dispatcher function a ranking of rows names, whether it could be granted an allow rule - and why not, where it could not - writing nothing.";
  "This is the half a daily reading wants. A reading that granted as it read would be an unattended thing editing the file that decides what may run unattended, which is the one thing the granting command is refused a grant of its own to prevent. So the reading gathers the case and the granting stays a hand on a key.";
  "The refusals are the more useful half of the answer: a name that keeps costing prompts and keeps being refused is a function that wants narrowing, and it stays visible here until somebody narrows it.";
  let grouped = await permission_rows_grant_generic(
    rows,
    key,
    permission_rule_grant_verdict_context,
  );
  return grouped;
}
