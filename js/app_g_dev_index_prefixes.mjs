export function app_g_dev_index_prefixes() {
  "the CATEGORY prefix for each dev route whose folder its own NAME does not say — keyed by route name, value = its `a: b: ` path (segments joined by ': ', trailing ': '). app_g_dev_index_prefixes_all lays this over the folders read off the names, and this WINS, so here is where a decision goes that a rule cannot reach: a path two levels deep, or a folder named a word the route is not called. a route named after its folder needs NO entry and must not be given one — day_hours files itself, and the line that had to be remembered is the line that was missing. BESPOKE (object literal) — do NOT auto-canonicalize";
  let conversation = "conversation: ";
  let unbeliever = "conversation: unbeliever: ";
  let prefixes = {
    unbeliever: conversation,
    quick: unbeliever,
    gospel_share: unbeliever,
    hru: unbeliever,
    believe: unbeliever,
    pray: unbeliever,
  };
  return prefixes;
}
