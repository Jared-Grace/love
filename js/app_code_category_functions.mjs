export function app_code_category_functions() {
  "The word the curriculum files its lessons on functions under, standing at the head of each of their names and inside the address that opens them.";
  let v = "functions";
  return v;
}
