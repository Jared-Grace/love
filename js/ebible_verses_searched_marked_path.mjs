export function ebible_verses_searched_marked_path() {
  "Where the record of the two cuttings measured against each other is kept.";
  "The measuring opens and parses every chapter of every bible on this machine twice over, once for each reading, which is far more than a gate can do. So the answer is written down once and whatever checks it reads the writing.";
  let p = "data/ebible_verses_searched_marked.json";
  return p;
}
