export function result_lines_claude() {
  "How many lines of a printed result Claude is handed before the middle is taken out and filed.";
  "Forty, and the number is measured rather than chosen. Of the twelve thousand hand cuts in a fortnight of logs the three commonest asked for twenty, thirty and forty lines, and asking for forty covers all three: a cut smaller than what was wanted costs a second call, and the whole point is to stop a second call being needed.";
  "One number rather than one per command, because the cutting was never about a particular command. It was bought as insurance against not knowing how much would come, and insurance bought everywhere has to be answered everywhere.";
  let lines = 40;
  return lines;
}
