import { arguments_assert } from "./arguments_assert.mjs";
export function color_contrast_floor_shape() {
  arguments_assert(arguments, 0);
  ("how far apart in contrast a drawn shape and what it stands on have to be before a reader can be expected to see that it is there: three to one");
  ("From the same web rule as the lettering floor, which sets this one separately for things that are not lettering - the edge of a control, a mark, a patch of colour carrying meaning. Same provenance and the same reason for trusting it: this repo did not pick either number.");
  ("★ LOWER THAN THE LETTERING FLOOR ON PURPOSE, AND USING THE LETTERING FLOOR HERE WOULD BE A FAULT RATHER THAN CAUTION. Making out that a shape is present is an easier task than telling one letterform from another, so the stricter number would refuse drawings a reader can see perfectly well. A check that refuses what is provably fine teaches whoever meets it to raise the number rather than fix the drawing, and then the floor protects nothing.");
  ("Kept apart from the lettering floor as its own name rather than handed in as an argument, so a caller has to say which of the two questions it is asking. A single floor passed around is a number that can be got wrong silently; two names cannot be confused for one another by a reader.");
  let floor = 3;
  return floor;
}
