import { arguments_assert } from "./arguments_assert.mjs";
import { html_style_padding_x } from "./html_style_padding_x.mjs";
export function app_code_expression_operator_chip_room_clear(span) {
  arguments_assert(arguments, 1);
  ("give back the room a chip was holding either side of an operator, so it takes up no more of the line than the symbol itself does");
  ("The twin of taking the chip's colour off, kept apart from it because the two are wanted at different moments. Nothing outside the operator's own square can tell that its fill has gone; the moment its room goes, every piece of the line to the right of it has to close up by that much.");
  ("So this is the half that MOVES the line, and it is the half that is worth watching. Given in the same breath as the colour, a learner is shown two changes at once and reads the line as having been rearranged behind their back.");
  html_style_padding_x(span, "0");
}
