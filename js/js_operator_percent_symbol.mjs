export function js_operator_percent_symbol() {
  "the remainder symbol";
  "The same mark means a part of a hundred when it stands in a width, which is not this and is written out where it is meant.";
  let v = "%";
  return v;
}
