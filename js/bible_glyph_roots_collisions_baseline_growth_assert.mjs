import { bible_glyph_roots_collisions_baseline_path } from "./bible_glyph_roots_collisions_baseline_path.mjs";
import { baseline_known_growth_assert } from "./baseline_known_growth_assert.mjs";
export async function bible_glyph_roots_collisions_baseline_growth_assert(
  known,
) {
  "$plain known";
  "the names are the record about to be written, a list of words. Nothing in them runs.";
  "Refuse to record a shared picture the baseline did not already hold. A ratchet that can be rewritten in both directions is not a ratchet, and the rewrite would be reached for at exactly the moment the gate went red, which is the moment it was doing its job.";
  "The first seeding has no file to compare against and is allowed, and so is any rewrite that only drops names.";
  let path = bible_glyph_roots_collisions_baseline_path();
  await baseline_known_growth_assert(
    known,
    path,
    "recording this as known would bless a second root moving onto a picture that already stands for one - give the new root a picture of its own, or if the two really are one word in English, drop one of them from the table rather than teaching a reader that two words look the same",
  );
}
