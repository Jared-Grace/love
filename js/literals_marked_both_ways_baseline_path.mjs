export function literals_marked_both_ways_baseline_path() {
  "Where the both-ways ratchet keeps the words the repo already carried. Reading it, rewriting it, and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path = "data/literals_marked_both_ways_baseline.json";
  return path;
}
