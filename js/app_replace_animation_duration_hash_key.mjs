export function app_replace_animation_duration_hash_key() {
  "the field of a page address holding how long the replacing animation runs - published in every link anybody saved so nothing here may reword it";
  let v = "d";
  return v;
}
