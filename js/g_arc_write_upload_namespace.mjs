import { text_frozen } from "./text_frozen.mjs";
export function g_arc_write_upload_namespace() {
  "The word written arcs are uploaded under, which is the folder they sit in on the shared bucket.";
  "Frozen rather than spelled as a reference, even though a function of that name is what writes them. The files land on a disk nothing here can move, and a shipped page builds this same address to read them - so a word that followed a rename would move where everything looks while leaving every arc already uploaded where it was.";
  "Named at all because two places need it and they must never disagree: the address the uploader writes to, and the list saying which folders a backup may take.";
  let v = text_frozen("g_arc_write_upload");
  return v;
}
