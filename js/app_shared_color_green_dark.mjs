export function app_shared_color_green_dark() {
  "a dark green. It is a value, not a meaning - what it is for is said by whoever asks for it.";
  "It is the green twin of the dark blue a category word is written in by default: the same depth, so a word written in it on a light green reads as clearly as the blue one does on a light background.";
  let v = "#14532d";
  return v;
}
