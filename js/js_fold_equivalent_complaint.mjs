export function js_fold_equivalent_complaint() {
  "What the fold says when the block it matched turns out not to be the function's body after all";
  let v = "fold equivalence check failed: matched block is not x's body";
  return v;
}
