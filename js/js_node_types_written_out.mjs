import { arguments_assert } from "./arguments_assert.mjs";
export function js_node_types_written_out() {
  arguments_assert(arguments, 0);
  ("The two kinds of thing a piece of code writes out in full rather than works out: a list of things and a thing with named parts.");
  ("Both are read the same way by somebody in front of them - one entry at a time, in any order, forgetting each before the next - and that is the whole reason they are named together here. A body is worked through; one of these is looked things up in.");
  let types = ["ArrayExpression", "ObjectExpression"];
  return types;
}
