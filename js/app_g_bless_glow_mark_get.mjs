import { arguments_assert } from "./arguments_assert.mjs";
import { app_shared_game_npc_property_get_null_generic } from "./app_shared_game_npc_property_get_null_generic.mjs";
export function app_g_bless_glow_mark_get(person) {
  arguments_assert(arguments, 1);
  ("The gold circle inside a person's light - the quiet mark that says they have been prayed");
  ("for - found by WHO that person is. Nothing, for anybody whose light was never drawn.");
  ("The light itself and the gold inside it are two elements rather than one, and this is");
  ("what makes the second of them reachable. The light is the thing that RIDES: it is made");
  ("before anybody has taken a step and carried from then on, and the blue that arrives on a");
  ("prayer is hung inside it so it rides too. The gold cannot be that same element, because");
  ("fading a light in fades everything hanging inside it - the blue would rise out of");
  ("nothing along with the gold instead of the gold rising underneath the blue.");
  ("So the gold sits alongside the blue rather than around it, and both ride. Remembered");
  ("here rather than looked for on the page, because the drawing that made it runs again on");
  ("every step and hands nothing back after the first time.");
  let mark = app_shared_game_npc_property_get_null_generic(
    app_g_bless_glow_mark_get,
    person,
  );
  return mark;
}
