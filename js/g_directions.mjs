import { fn_name } from "./fn_name.mjs";
export function g_directions() {
  ("the four character facings, as the rotation-PNG basenames under characters/<img>/rotations/ — the four the game WALKS on; preloading warms all eight instead, through ",
    fn_name("g_directions_all"),
    ", because a turn is now drawn through the diagonals. ",
    fn_name("g_direction"),
    " returns one of these from a move's dx/dy");
  let r = ["south", "north", "east", "west"];
  return r;
}
