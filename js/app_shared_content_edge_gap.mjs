export function app_shared_content_edge_gap() {
  "how far a screen's content sits from the window edge on a narrow screen - a phone, where the padding is not deciding a centered column but only leaving the words room to breathe. Smaller than the gap stacked items leave between each other, because an edge borrows from the reading width and a gap between two things does not";
  let v = "0.4em";
  return v;
}
