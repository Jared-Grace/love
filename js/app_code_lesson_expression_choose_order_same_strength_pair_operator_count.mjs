import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_lesson_expression_choose_order_same_strength_pair_operator_count() {
  arguments_assert(arguments, 0);
  ("how many operators a line of this lesson carries, which is also how many steps it takes to solve: 2");
  ("The telling above the card walks a line of this lesson's own kind all the way down, and the quiz hands out lines of the same kind. Written out at both addresses the two could drift, and the run a learner reads would then stop short of the line they are given.");
  ("Two, which is the fewest that is still a choice. The lesson is about what settles that choice when neither operator is the stronger, and a third operator would be a second new thing on the same screen - it has a lesson of its own further on, reading this same rule for one step longer.");
  let count = 2;
  return count;
}
