export function process_folder_deleted_note() {
  "What the kernel adds to a working folder it is still reporting after the folder has been thrown away.";
  "It is written out here rather than at the one place that reads it because it is not ours to choose - it is the shape Linux writes, and a name says that better than a bare piece of text sitting in a comparison.";
  let note = " (deleted)";
  return note;
}
