export function app_code_category_operators() {
  "The word the curriculum files its lessons on operators under, standing at the head of each of their names and inside the address that opens them.";
  let v = "operators";
  return v;
}
