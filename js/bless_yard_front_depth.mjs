import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function bless_yard_front_depth() {
  function_duplicate_kind_parallel();
  arguments_assert(arguments, 0);
  ("How many rows of grass lie between the fronts of the houses and the pavement.");
  ("ONE. This is the front garden, and it is what pushes the pavement off the doorsteps and down into the middle of the green band.");
  ("It is written on its own rather than worked out from the depth of the whole band, because it is the number that says WHERE the pavement is and the band depth is the number that says how far the road is. Those are two different questions and only one of them changes when somebody asks for a wider verge.");
  ("One and not two, because a door has to be reachable. A doorstep sits on this grass, the driveway crosses it, and every extra row here is another row of lawn between the player and the person they came to pray for.");
  let depth = 1;
  return depth;
}
