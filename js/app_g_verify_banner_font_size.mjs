export function app_g_verify_banner_font_size() {
  "the size of the strip saying writing is under way, just under the body size so it is noticed without pulling the eye off the passage";
  let v = "0.95em";
  return v;
}
