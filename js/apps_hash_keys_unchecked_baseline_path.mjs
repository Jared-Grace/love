export function apps_hash_keys_unchecked_baseline_path() {
  "Where the unanswered-link-word ratchet keeps what the repo already carried. Reading it, rewriting it, and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path = "data/apps_hash_keys_unchecked_baseline.json";
  return path;
}
