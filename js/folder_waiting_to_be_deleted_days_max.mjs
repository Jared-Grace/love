export function folder_waiting_to_be_deleted_days_max() {
  "How many days something may wait for somebody to decide about it before the waiting itself is the problem.";
  "A bin with no deadline turns a loud question into a quiet pile, which is worse than the red gate it cleared: the gate was asking somebody to decide and the pile is not asking anybody anything. So the deadline is what keeps it a queue rather than a grave.";
  "Thirty days is long enough that a person away for a fortnight comes back to a question rather than to a failure, and short enough that whoever binned it still remembers why. It is deliberately not a number anybody has to be right about - a wrong answer here costs one red gate saying somebody should look, which is exactly what it is for.";
  let days = 30;
  return days;
}
