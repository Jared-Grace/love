export function app_code_exponent_base_color() {
  "the color for the exponent BASE (the number being repeated) - a purple chip, shown identically in the sentence and in the ** expression so the eye matches the base to its role";
  return "rgb(120, 86, 190)";
}
