export function app_shared_button_border_width() {
  "how thick an edge a pale button is drawn with: enough to find on a pale ground, not enough to read as a frame";
  let width = "0.05em";
  return width;
}
