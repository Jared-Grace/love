export function js_name_handback_answer(f_name_new) {
  "$plain f_name_new";
  ("The name the few lines left behind give to the thing a moved body hands them, before they take the writes off it one at a time.");
  ("It is built out of the moved body's own new name, and that is what makes it safe rather than merely unlikely. The name is new to the whole repo, so nothing standing where these lines stand can already be using it. A short plain word would read better and could be the very name of a parameter of the function being left behind - and every line below it would then be putting a write onto the wrong name, quietly, which is the one failure this whole move exists to avoid.");
  let name = text_combine(f_name_new, "_answer");
  return name;
}
