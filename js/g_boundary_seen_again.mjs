import { fn_name } from "./fn_name.mjs";
export function g_boundary_seen_again() {
  ("the warm half of a boundary given to somebody you have MET before — good to see you, said before the topic is turned aside, so the refusal lands as a person keeping something back rather than as a door closing. it ends in a space because a deferral follows it in the same breath. shared by ",
    fn_name("g_boundary"),
    " and ",
    fn_name("g_boundary_believer"),
    ", which both open that way: a believer and an unbeliever guard different things with different words, but they are equally glad to see you, so this one line is the same in both mouths");
  return "It's good to see you again. ";
}
