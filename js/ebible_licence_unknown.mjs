export function ebible_licence_unknown() {
  "The name this repo gives terms it could not read off the page - never a permission, always a reason to leave the text alone until somebody has read the page themselves.";
  "Kept apart from every named licence on purpose: an unread page and a page granting nothing look the same from here, and treating them as one would hide which of the two it was.";
  let v = "unknown";
  return v;
}
