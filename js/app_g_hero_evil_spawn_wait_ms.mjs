import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_evil_spawn_wait_ms() {
  arguments_assert(arguments, 0);
  ("How long the street is at peace after the evil one is burned, before somebody else turns.");
  let ms = 4000;
  return ms;
}
