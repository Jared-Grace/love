import { arguments_assert } from "./arguments_assert.mjs";
import { html_hash_name_get } from "./html_hash_name_get.mjs";
import { list_includes } from "./list_includes.mjs";
export function bless_hash_words_is(words) {
  arguments_assert(arguments, 1);
  ("Whether the address this visit was opened with names one of these words.");
  ("ASKED OF THE ADDRESS RATHER THAN REMEMBERED, so it is spelled afresh in the link every time and no visit inherits it. A skip that stuck would be a skip a player could fall into: the one prayer this game is built around, turned off by a setting nobody knew was on. Typed into the address, it cannot outlive the tab it was typed in.");
  ("It takes the list rather than naming one, because there is more than one set of words to ask about and they do not answer the same questions. The openings onto the street take the panel over a person away; every opening there is, the map included, comes past the door. Written twice with a different list in each, the second copy would be the one that stopped agreeing about what an address may do.");
  ("It is NOT the dev-gated question next door. This one is asked where a skip costs the player nothing they could not have done themselves; the gated one is asked where an opening would hand somebody prayers nobody said.");
  let name = html_hash_name_get();
  let named = list_includes(words, name);
  return named;
}
