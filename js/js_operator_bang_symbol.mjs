import { fn_name } from "./fn_name.mjs";
export function js_operator_bang_symbol() {
  ("the symbol the not operator is written with, on its own - the twin of ",
    fn_name("js_operator_triple_equal_symbol"),
    " and ",
    fn_name("js_operator_bang_double_equal_symbol"),
    ", for the callers that want the character without the operator object around it");
  let s = "!";
  return s;
}
