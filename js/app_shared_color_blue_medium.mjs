import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_color_blue_medium() {
  arguments_assert(arguments, 0);
  ("the blue halfway between the deep one used for lettering on blue and the strong one the pages lead with - lighter than the deep blue while still dark enough to be read on a very pale blue.");
  ("It was wanted because the two blues either side of it are chosen for opposite jobs: the deep one is as dark as the scale goes, so it reads as almost black rather than as blue, and the strong one is bright enough that small words written in it on a pale fill come out faint. Stepping half way keeps enough darkness to read small and bold, and enough brightness to be seen as a colour.");
  let c = "rgb(34, 91, 190)";
  return c;
}
