export function file_extension_license() {
  "What a build adds onto a script's own name to make the attribution file it writes beside it.";
  "Only the part that is added on, so that taking it off a name leaves the script's name exactly - the script extension is already carried in the name underneath and putting it back by hand at each reader is how the two would come to disagree.";
  let e = ".LICENSE.txt";
  return e;
}
