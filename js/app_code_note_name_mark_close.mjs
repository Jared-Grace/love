import { arguments_assert } from "./arguments_assert.mjs";
import { js_code_parenthesis_right } from "./js_code_parenthesis_right.mjs";
export function app_code_note_name_mark_close() {
  arguments_assert(arguments, 0);
  ("the character that closes the mark put around a name where it stands inside a note in a lesson's program");
  ("The sibling of the opener, and it is a function of its own for the reason the opener gives: the writer and the painter must agree about the character, and a shared spelling is the only thing that keeps them agreeing.");
  let c = js_code_parenthesis_right();
  return c;
}
