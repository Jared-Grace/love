export function app_shared_bible_verse_number_gutter() {
  "the fixed width of the verse number column in the whole chapter reader - wide enough to hold a three digit verse number (Psalm 119 reaches 176) inside a button that carries its own border and margin, so every verse number button in the chapter comes out the same width";
  let v = "3.6ch";
  return v;
}
