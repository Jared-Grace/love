export function app_shared_bible_history_maximum() {
  "How many remembered readings are kept, the oldest falling off the end as newer ones arrive.";
  "It is a list somebody reads down rather than searches, so a number that fits on a screen is worth more than a number that holds everything. A tab that closed a moment ago is near the top; a tab from last month is not what anybody is looking for.";
  let v = 20;
  return v;
}
