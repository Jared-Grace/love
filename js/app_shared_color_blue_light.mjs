export function app_shared_color_blue_light() {
  "a light, semi-transparent blue — the highlight for completed / already-read words (e.g. the words tapped in order on the study screen)";
  let v = "rgb(95 145 255 / 59%)";
  return v;
}
