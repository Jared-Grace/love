export function js_operator_greater_than_symbol() {
  ("the greater-than operator as it is written in JS");
  ("Named the way its opposite already is, so a lesson that offers a learner both of them spells neither of them by hand - the symbol was reachable only by taking it back out of the operator object it sits in, which is the whole record of how it behaves rather than how it is written.");
  let s = ">";
  return s;
}
