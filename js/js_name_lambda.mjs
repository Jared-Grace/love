export function js_name_lambda() {
  "The word this repo gives a function that was written without a name, before a number is put on the end of it to tell it from the others in the same file.";
  "One word in one place because two readers want it: the pass that hands out the names, and the reading that has to tell a name somebody chose apart from one a pass handed out. Spelled twice, a change in the pass would leave the reading quietly agreeing with a word nothing is called any more.";
  let word = "lambda";
  return word;
}
