import { arguments_assert } from "./arguments_assert.mjs";
export function song_image_color_text_quiet() {
  "the dimmer gray the song-image tool puts its second line in - the licence under a picture's title, the verse a couplet came from, a note saying nobody has searched for this symbol yet";
  "It sits a long step below the plain text rather than a shade below it, because what it carries is never what a person is deciding on. A licence has to be readable when somebody goes looking for it and has to stay out of the way while they are comparing pictures, and those two are only both true at a distance from the text above.";
  arguments_assert(arguments, 0);
  let c = "#676767";
  return c;
}
