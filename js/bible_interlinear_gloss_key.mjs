"The column in the Berean interlinear tables holding that word's English rendering.";
"The surrounding spaces are part of the real column name, not a typo — do not trim them here.";
export function bible_interlinear_gloss_key() {
  let r = " BSB version ";
  return r;
}
