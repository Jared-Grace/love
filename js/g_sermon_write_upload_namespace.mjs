import { text_frozen } from "./text_frozen.mjs";
export function g_sermon_write_upload_namespace() {
  "The word written sermons are uploaded under, which is the folder they are already sitting in on the shared bucket.";
  "Frozen rather than spelled as a reference, even though a function of that name is what uploads them. The files are on a disk nothing here can move, and a shipped page builds this same address to read them - so a word that followed a rename would move where everything looks while leaving every file already uploaded where it was.";
  "Named at all because two places need it and they must never disagree: the address the uploader writes to, and the list saying which folders a backup may take. Spelled twice, one of them as a reference, a rename would have moved the backup's reading and left the uploader's behind.";
  let v = text_frozen("g_sermon_write_upload");
  return v;
}
