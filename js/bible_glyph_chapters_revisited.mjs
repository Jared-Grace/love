export function bible_glyph_chapters_revisited() {
  "The picture Bible chapters that have already been read through a second time, once the whole book had a first pass over it.";
  "IT IS EMPTY ON PURPOSE AND WILL BE FOR A LONG TIME. The order of work was settled deliberately: one pass over every chapter first, then a second pass back over all of them. So a chapter authored today is not unfinished, it is at pass one, and the list of chapters that got the second read is the only thing that can tell those two apart.";
  "WITHOUT IT EVERY WRITTEN CHAPTER READS AS A FAULT FOREVER. Every picture seated after a chapter was authored leaves that chapter holding the word in bare English, and the reading that measures the gap cannot see whether anybody has been back yet. Under a one-pass-first order that number can never reach zero, and a count that is permanently red is a count people stop opening - which is the exact failure the count was built to prevent.";
  "SO THE SAME GAP MEANS TWO DIFFERENT THINGS EITHER SIDE OF THIS LIST. A chapter not named here is waiting its turn, and the words it leaves in English are the queue for the second pass. A chapter named here has been read again since, so a word still in English is a person having looked at it and left it - and that is a real fault, because a refusal that was argued would have been written into the list of deliberate ones instead.";
  "A NAME IS ADDED BY HAND AND NEVER BY A COMMAND, because no command can tell that a person read a chapter. What a command could check is that the chapter changed, and a second pass that correctly changes nothing would then be indistinguishable from a second pass nobody did.";
  let codes = [];
  return codes;
}
