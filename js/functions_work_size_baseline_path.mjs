export function functions_work_size_baseline_path() {
  "where the functions already holding more lines of work than the ceiling allows are recorded";
  let v = "data/given/baselines/work_size_baseline.json";
  return v;
}
