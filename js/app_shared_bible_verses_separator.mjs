export function app_shared_bible_verses_separator() {
  "What stands between two verse numbers in a link, so a link asking for a run of them says so in one word.";
  "It is in links already sent, so it cannot be changed - which is the reason for asking a function for it rather than spelling it where it is used. Spelled in four places it is four things that have to agree, and nothing anywhere would say they had stopped.";
  let v = "-";
  return v;
}
