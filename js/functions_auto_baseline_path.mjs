export function functions_auto_baseline_path() {
  "where the functions the normalize pass cannot carry through and leave loadable are recorded";
  let v = "data/auto_refused_baseline.json";
  return v;
}
