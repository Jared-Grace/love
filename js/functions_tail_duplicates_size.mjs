export function functions_tail_duplicates_size() {
  "how many closing lines two functions must share before the gate calls it duplication";
  "four rather than five because five was cleared the day this was written and four costs one entry - the pair holding the same four lines over a list and over a text, which is two ideas that finish alike rather than one idea written twice";
  "three is a different thing and not this: at three almost every group is a thin wrapper already collapsed onto a shared core, where the lines that remain are the two names that differ";
  let v = 4;
  return v;
}
