export function g_verify_chapter_storage_key() {
  "the browser-storage word the verify page reads a chapter override from - set by hand in a browser, so it may never be reworded";
  let v = "g_verify_chapter";
  return v;
}
