import { arguments_assert } from "./arguments_assert.mjs";
export function bless_crossing_clearance_tiles() {
  arguments_assert(arguments, 0);
  ("How many squares of empty road a walker claims on each side of herself while she is");
  ("crossing. No vehicle may enter that band until she is off the road again.");
  ("One square, because the band is a promise the traffic has to keep rather than a");
  ("comfort. A vehicle stopped one square short of a person is plainly stopped FOR her,");
  ("which is what the moment is meant to show; stopped four squares short it is just a");
  ("street that happens to be empty, and the player learns nothing from it.");
  ("It is also the smallest band that can be kept at all. A vehicle moves a whole square");
  ("at a time, so a band of nothing is a band it steps straight over: the square it lands");
  ("on is the square she is standing in, and the decision to stop is taken after the two");
  ("are already in the same place.");
  let tiles = 1;
  return tiles;
}
