export function apps_frozen() {
  "these apps should not be changed in prod or deleted";
  "What belongs here is a kept copy, not a living app. A dated copy is finished by the time it is made - somebody was given its address and it must answer the same way for as long as they hold it - so keeping it still costs nothing and is never in anybody's way. Naming a living app here instead protects it once and then blocks it for good, because releasing it again means taking it off this list, and this list cannot go empty";
  "It cannot go empty because a worked example in the corpus takes its subject from the first name here, while its own file is being read. Nothing named means that read throws, and a file in that folder that throws as it loads takes the whole corpus with it, every gate that reads the corpus, and so the release of every app. Measured 2026-08-14: five gates red at once, none of them about the app being released";
  "Spelled as names rather than as entry points, because the thing most worth keeping still is exactly the thing that has no entry point - a copy is a pair of files that were built once and are never built again, and asking it for a way in would mean writing one, which would invite the rebuilding that keeping it still forbids";
  let r = ["replace_2026_05_03"];
  return r;
}
