export function apps_frozen() {
  "these apps should not be changed in prod or deleted";
  "The replacing app was taken off this list on purpose, for one sending: its whole course of exercises was rewritten in plain words for a reader with no programming behind them, and an app named here is asserted unchanged before anything goes out, so the rewrite could not have reached anybody while it sat here. Put it back once that sending has gone, which is what makes the new wording the thing being protected rather than the old";
  let r = [];
  return r;
}
