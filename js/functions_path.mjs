import { folder_js } from "./folder_js.mjs";
export function functions_path() {
  "The folder every function's file lives in, named once here so nothing else has to spell it.";
  let second = folder_js();
  return second;
}
