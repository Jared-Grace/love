import { list_single_item } from "./list_single_item.mjs";
import { ebible_bible_folders_commercial_assert } from "./ebible_bible_folders_commercial_assert.mjs";
export async function ebible_bible_folder_commercial_assert(bible_folder) {
  "Refuses to go on when this one translation is offered on terms that do not leave this repo free to ship it and to earn from it - what a door about to publish a translation's text owes that text before a word of it leaves the machine.";
  "One at a time because that is the shape every publishing door has: each takes one folder and sends what belongs to it, so the question has to be askable of one. The list form underneath is the same question and stays the one place the terms are actually read.";
  let bible_folders = list_single_item(bible_folder);
  await ebible_bible_folders_commercial_assert(bible_folders);
}
