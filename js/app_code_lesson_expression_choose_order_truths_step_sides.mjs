import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_lesson_expression_choose_order_truths_step_sides() {
  arguments_assert(arguments, 0);
  ("the words standing between a part of a line and what that part comes to, on a lesson whose parts are trues and falses");
  ("Said once here because two lessons walk lines of this kind and every step of every walk on both of them says it. Written at each of them, one could be improved and the other left saying the older thing, and a learner reading the two screens in a row would meet the same step worded two ways.");
  let words = " has a true or a false on each side, and ";
  return words;
}
