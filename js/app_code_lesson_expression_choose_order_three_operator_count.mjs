export function app_code_lesson_expression_choose_order_three_operator_count() {
  ("how many operators a line of this lesson carries, which is also how many steps it takes to solve: 3");
  (
    "The front page walks a line of this lesson's own kind all the way down, and the quiz hands out lines of the same kind. Written out at both addresses the two could drift, and the run a learner reads would then stop short of the line they are given."
  );
  (
    "Three rather than two, because two is the length every earlier lesson has used. One more step is the whole of what is new here: the same reading of the operators, done once more before the line runs out."
  );
  let count = 3;
  return count;
}
