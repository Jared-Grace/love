import { arguments_assert } from "./arguments_assert.mjs";
export function js_code_math_floor_name() {
  arguments_assert(arguments, 0);
  ("the name a learner reads in front of the brackets of a rounding-down call: Math.floor");
  ("Named on its own because it is written in two quite different places - as the front of a piece of code a lesson prints, and as the symbol of a part of a line a learner can press - and the two have to spell it the same way or a pressable line and the line beside it disagree.");
  let name = "Math.floor";
  return name;
}
