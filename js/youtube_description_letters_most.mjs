export function youtube_description_letters_most() {
  "How many letters youtube will keep in the description of a video or a playlist.";
  "Youtube refuses a longer one outright rather than shortening it, so anything writing a description has to know this before it writes, not after. It is written down once because the two jobs that write descriptions would otherwise each carry their own copy of the same number.";
  let letters_most = 5000;
  return letters_most;
}
