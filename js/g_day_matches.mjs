export function g_day_matches() {
  "npc matches a player has in one in-game day (~36 standard plus ~4 allowed to run longer); paired with the day-lines budget it fixes the matches-per-line rate";
  let r = 56;
  return r;
}
