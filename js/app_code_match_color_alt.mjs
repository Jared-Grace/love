export function app_code_match_color_alt() {
  "a bright cyan, the SECOND match colour: when two worked lines each highlight a DIFFERENT first digit after the decimal point, one line uses the amber match colour and the other uses this, so the reader sees at a glance that the two digits are distinct - not the same digit repeated. Cyan sits well clear of the amber and reads clearly on the black code surface";
  let r = "#5ccfe6";
  return r;
}
