export function ebible_language_code_apart_maximum() {
  "How far from a real language code a mistyped one may be before this repo stops guessing what was meant.";
  "One edit, because these codes are two and three letters long and almost every one of them is two edits from almost every other. Guessing at two would answer any typing at all with a list, and a list that always has something in it says nothing about whether the right answer is in it.";
  let apart = 1;
  return apart;
}
