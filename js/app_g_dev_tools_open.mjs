import { html_hash_name_reload } from "./html_hash_name_reload.mjs";
export function app_g_dev_tools_open() {
  "open the #index dev-route directory from the player menu — the same going-there every card and the back-link do, so the button cannot work while they do not, or the other way round";
  html_hash_name_reload("index");
}
