export function app_code_expression_step_value_most() {
  ("the largest number a step-at-a-time line is allowed to hold or come to: 9");
  (
    "One digit, so every number a learner reads on the line and every value they are offered underneath it is a single character. The lesson is about which part of a line goes first, and a two-digit number would put a carry or a long division in the way of the one thing being asked."
  );
  let most = 9;
  return most;
}
