export function app_shared_style_control_padding() {
  ("the space a control keeps between its writing and its edge, measured against the control's own enlarged font");
  ("Named rather than written where it is used, because a control that has to give up the enlarged font still has to come out the same size, and the two numbers only stay in step if the second one is worked out from this one.");
  let padding = "0.55em";
  return padding;
}
