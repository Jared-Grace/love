import { arguments_assert } from "./arguments_assert.mjs";
export function firebase_hosting_stages_unsent() {
  "The two folders under the one that is sent which must never reach the public internet, spelled the way the sending asks to be told to leave a folder behind.";
  "There are three stages and only the last of them is finished. The first is where a change is tried out and is meant to be broken; the second holds something believed ready and not yet agreed to be. Neither is a thing to put in front of a person who came looking for the built app, and both sit inside the folder that goes out whole, so leaving them behind has to be said rather than assumed.";
  arguments_assert(arguments, 0);
  let folders = ["dev/**", "latest/**"];
  return folders;
}
