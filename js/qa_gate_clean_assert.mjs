import { arguments_assert } from "./arguments_assert.mjs";
import { list_empty_is_assert_json } from "./list_empty_is_assert_json.mjs";
export function qa_gate_clean_assert(accused, hint) {
  arguments_assert(arguments, 2);
  ("The ending every gate here shares - stop with what was found when anything was, and otherwise answer what a clean gate answers.");
  ("The accused are the only thing that travels as the record. Everything a reader needs in order to act - which command repairs it, and what each finding was - goes in the hint, because whoever reads a red gate reads the record for names and cannot tell a name being accused from a name being mentioned. That distinction had to be learned twice, once when a small atom named as the copied-from held a whole app out of a deployment, so it is worth having in one place rather than in each gate that must remember it.");
  ("A clean gate says nothing was found rather than merely returning, so that a caller collecting many gates has the same shape from each of them.");
  list_empty_is_assert_json(accused, {
    hint,
  });
  let r = {
    offenders: 0,
  };
  return r;
}
