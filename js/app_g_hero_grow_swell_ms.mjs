import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_grow_swell_ms() {
  arguments_assert(arguments, 0);
  ("How long the player takes to rise to her new height, in milliseconds.");
  ("Long enough to be WATCHED rather than noticed afterwards. The camera has already come in close and stopped, nothing else on the screen is moving, and the whole reason the moment exists is to let the player see a change too small to see at playing distance. A swell over in a third of a second is a flinch: the eye registers that something happened and has nothing left to look at, which is exactly the thing the close-up was spent to avoid.");
  ("Not so long that she is standing still waiting for it either. This is the pause after a fight, and a pause the player is ready to leave becomes a delay.");
  let ms = 1100;
  return ms;
}
