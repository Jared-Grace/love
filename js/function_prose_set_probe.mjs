export function function_prose_set_probe() {
  "A first line that is right.";
  "A second line that is wrong and here to be corrected, so the command that corrects a line can be seen working.";
  "A third line that is right.";
  let v = 1;
  return v;
}
