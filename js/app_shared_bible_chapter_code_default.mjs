export function app_shared_bible_chapter_code_default() {
  "Where a bible page opens when nothing has said where to open. A link that names no chapter is somebody arriving with no request at all, and every bible surface answers that the same way rather than each choosing its own beginning.";
  let v = "JHN01";
  return v;
}
