export function phone_test_height() {
  "how tall a phone screen is taken to be when a page is checked at phone size - short on purpose, because the browser's own bar eats the top of a real phone and the page only ever gets what is left";
  let v = 640;
  return v;
}
