export function bible_dream_ink_width() {
  "How thick the bright line of a stroke is drawn once it has been traced, in the drawing's own units.";
  "★ IT IS NARROWER THAN THE CORRIDOR AND HAS TO STAY THAT WAY. The faint corridor is a promise about how much room a hand has, and a player can only read that promise off the gap between the corridor's edge and what sits inside it. Ink as wide as the corridor covers the promise completely, so the picture stops saying anything about room at all and the tolerance becomes an invisible rule that only announces itself by being broken - which is the one thing the corridor exists to prevent.";
  "It is thicker than it was because the corridor around it is, and because this is the line the passage actually gave. Everything else on the surface is either a direction or an attempt; this is the thing that arrived, and it should read as the most solid mark in the dream.";
  let r = 6;
  return r;
}
