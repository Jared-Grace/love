import { sleep_success_color } from "./sleep_success_color.mjs";
export async function app_code_expression_chosen_pause() {
  "a right press is held green for a moment before the line works it out, the same moment long as every other success in the app";
  "Handed the operator that was chosen and what it came to, and reads neither, so it declares neither and counts nothing - the same way noop is written. A count asserted here would be a count of what the caller says rather than of what this needs, and it would go wrong the day the caller has one more thing to say.";
  "This is what a quiz hands the pressable line as its answer to a right press. There is no replace to press: pressing replace is the front page teaching what the step IS, and a quiz asks for the choice alone - a button that only ever has one thing to do would be a press that marks nothing.";
  await sleep_success_color();
  ("and the line is told the swap was NOT watched, so it holds the value green once it is written: here it simply appears where the working out was, and appearing is over in an instant");
  return false;
}
