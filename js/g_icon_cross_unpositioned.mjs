import { g_icon_cross_color_unpositioned } from "./g_icon_cross_color_unpositioned.mjs";
export function g_icon_cross_unpositioned(parent) {
  "the GREEN believer cross (believes) — the shared cross body in green";
  let i = g_icon_cross_color_unpositioned(parent, "#1fd000ff");
  return i;
}
