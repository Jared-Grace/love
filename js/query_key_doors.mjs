import { fn_name } from "./fn_name.mjs";
export function query_key_doors() {
  "The calls that take the name of a field of the part of a page address after the question mark, first, and read it there.";
  "There is one of them, and it is written down rather than worked out, which is the shape that goes stale. What keeps it honest is asked separately, by the gate that reads every caller of every address door and fails on one handing its own parameter into the field slot.";
  "Kept apart from the doors onto the part after the hash even though both take the field first and both are asked the same question, because the two halves of an address are found by different walks and each walk names its own doors. Joining them here would make every later change to one a change to both.";
  let f_name = fn_name("html_query_property_get");
  let doors = [f_name];
  return doors;
}
