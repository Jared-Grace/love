import { folder_gitignore_join } from "./folder_gitignore_join.mjs";
export function psalms_videos_descriptions_payload_parts_path() {
  "Where the words of every song sit once they have been cut into pieces small enough to hand over one at a time.";
  let path = folder_gitignore_join(
    "psalms_videos_descriptions_payload_parts.json",
  );
  return path;
}
