export function functions_head_duplicates_baseline_path() {
  "where the groups of functions already beginning with the same run of work are recorded";
  let v = "data/head_duplicates_baseline.json";
  return v;
}
