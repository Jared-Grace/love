import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function suno_lyrics_characters_limit() {
  function_duplicate_kind_parallel();
  ("How many characters of words Suno accepts in one song.");
  ("It is written down here rather than at the place that cuts a chapter up, because the number belongs to somebody else's website and will change when they change it. One place to correct is the whole of the repair; a number spelled at each of the places that needs it is a search nobody remembers to run.");
  ("The counter on their own page is what this was read off - it shows the count against the ceiling as the words are typed in, and it refuses the song rather than trimming it.");
  arguments_assert(arguments, 0);
  let r = 3000;
  return r;
}
