export function youtube_privacy_words() {
  "The three words YouTube knows for who may watch a video, and there is no fourth.";
  "They are written down so that a word outside them can be refused before anything is sent. YouTube does not refuse an unknown word - it falls back to the most closed of the three, so every mistyped setting fails the same way and the failure is a video nobody can see reported as a video that went up.";
  let words = ["public", "unlisted", "private"];
  return words;
}
