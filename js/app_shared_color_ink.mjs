import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_color_ink() {
  arguments_assert(arguments, 0);
  ("the near-black that lettering is written in when it sits on a patch of colour rather than on the page");
  ("NEAR-BLACK RATHER THAN BLACK, AND NEUTRAL RATHER THAN TINTED. It is written on a red patch, a green one and a blue one, and a tint that flattered one of them would show as a cast on the other two - the letters would stop looking like one kind of writing, which is the thing that makes three patches read as three of the same thing.");
  ("It is also what a patch's edge is drawn in. A light patch can be faint against a pale page, and an edge in this colour ends that question for good: the patch is a shape with a line round it, and how well its fill happens to stand out from what is behind it no longer decides whether it is seen at all.");
  ("It sits four hundredths of lightness from the near-black a whole page is painted, which is a near miss by this repo's own colour rule and is left standing. Collapsing the two means choosing one near-black for a page and for lettering on a patch, and that is a judgment about colour rather than about how a colour is written down.");
  let ink = "#111111";
  return ink;
}
