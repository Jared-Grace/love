export function functions_inside_duplicates_size() {
  "how many lines of work two functions must share, anywhere inside them, before this calls it duplication";
  "four, the same as the two gates over openings and endings, so that all three ask one question and differ only in where the run is allowed to sit. Measured on 2026-08-02 the middle answers with 64 groups at four, against nothing at all from the other two - which is the whole reason this exists.";
  "three is not a smaller version of this number, it is a different report. At three the answer fills with runs that are three ways of saying one thing rather than one thing said twice - three lines binding a name to nothing, three lines quoting a word.";
  let v = 4;
  return v;
}
