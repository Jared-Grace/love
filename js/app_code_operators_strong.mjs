import { js_operator_asterisk_symbol } from "./js_operator_asterisk_symbol.mjs";
import { js_operator_division_symbol } from "./js_operator_division_symbol.mjs";
export function app_code_operators_strong() {
  "The operators a code lesson solves before the others, by their symbols: * and /.";
  "One place for the class rather than a * written out at each address that needs one. Three of them wanted the same fact - the generator picking an operator, and the two readers working out which of a line's operators it was - and each held the single symbol, so widening the class meant finding all three and hoping there were only three.";
  "A class rather than a table of every operator against every other. What a lesson here mixes is one of these with one of the weaker pair, so which of two classes an operator falls in settles the question, and the finer ordering inside a class is a different lesson.";
  let times = js_operator_asterisk_symbol();
  let divided = js_operator_division_symbol();
  let symbols = [times, divided];
  return symbols;
}
