import { arguments_assert } from "./arguments_assert.mjs";
export function functions_numbers_shared_spread() {
  "The most functions a number can be written in and still read as a decision somebody made more than once rather than as a fact of the language.";
  "Six, measured. Below it the groups are things like a delay written in five overlays and a colour part written in four screens; above it they are the counts and indexes - nought in four hundred and seventy-five functions, one in four hundred and fifty-four - which nobody chose and nothing could hold.";
  arguments_assert(arguments, 0);
  let r = 6;
  return r;
}
