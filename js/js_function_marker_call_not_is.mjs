import { js_function_marker_call_is } from "./js_function_marker_call_is.mjs";
import { not } from "./not.mjs";
export function js_function_marker_call_not_is(statement) {
  "The opposite reading, so a body can be filtered down to the statements that are the work rather than a mark left for a reader.";
  let marker_is = js_function_marker_call_is(statement);
  let n = not(marker_is);
  return n;
}
