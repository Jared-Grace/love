export function app_g_dev_index_hash_name() {
  "the word after the hash mark that asks for the game's dev directory - the screen listing every dev route there is. it is in a bookmark and in a link from outside the game, so it is a word that has left this repo and does not get respelled";
  let v = "index";
  return v;
}
