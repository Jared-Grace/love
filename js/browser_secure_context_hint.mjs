import { arguments_assert } from "./arguments_assert.mjs";
import { fn_name } from "./fn_name.mjs";
import { text_combine_multiple } from "./text_combine_multiple.mjs";
export function browser_secure_context_hint(lead) {
  "The complaint made about a file that reaches for one of the browser's https-only things, given the opening words that differ between the two places it is made.";
  "The gate is refusing a file that was just written; the rewrite of the record is refusing a change that would bless one. So they open differently and there is nothing to share there. But both end the same way - by naming the one function that has already been through this, so that whoever reads the complaint can see the remedy done rather than described. That ending was written out twice, and a sentence written twice is a sentence that can drift.";
  arguments_assert(arguments, 1);
  let f_name = fn_name("uuid_browser");
  let r = text_combine_multiple([
    lead,
    f_name,
    " where it is done: ask whether the thing is there, and give the page something to fall back on when it is not",
  ]);
  return r;
}
