import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_color_brand_blue_rgb() {
  "The strong blue this repo's pages lead with, as the three parts of a colour rather than as a written-out colour.";
  "Two pages spelled these three numbers out for themselves - one calling it the brand blue at the near end of a fading list, the other calling it the brightest end of a ramp of remainders. Whether those are one idea or two is not something either page can answer, and nothing is lost by holding the value once: a page that later wants a different blue writes a second name for it and moves across on its own, which leaves everything already written exactly where it stood.";
  "Three parts rather than a written-out colour because both places hand it to the reading that walks from one colour to another, and that reading needs the parts.";
  arguments_assert(arguments, 0);
  let r = [37, 99, 235];
  return r;
}
