export function app_shared_color_yellow_tint() {
  "a yellow with almost all of it taken out - yellow mixed so far into white that it reads as white gone warm rather than as a colour. It is a value, not a meaning; what it is for is said by whoever asks for it.";
  "It is written at the same faintness as the faint green and the faint red and belongs with them, because the three are read against each other on one page rather than each on its own. A wash noticeably stronger than its neighbours does not read as a third kind of line, it reads as the important one.";
  "Yellow is the third of the three because it is the one that is neither of the other two. Green and red are already spoken for and are opposites; a thing that is both at once cannot be drawn as either, and mixing them gives a muddy colour that reads as a fault rather than as a kind.";
  let v = "#fffbe0";
  return v;
}
