export function self_call_probe_delete_me(items) {
  "a planted fault, to watch the gate fire on a real file rather than on a written-down case. Deleted immediately after.";
  self_call_probe_delete_me(items);
}
