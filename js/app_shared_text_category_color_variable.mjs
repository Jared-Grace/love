export function app_shared_text_category_color_variable() {
  "the name a row uses to tell the category word inside it what colour to be, for the rows whose own colour the ordinary dark blue would disappear into.";
  let v = "--text-category-color";
  return v;
}
