import { arguments_assert } from "./arguments_assert.mjs";
export function bless_crossing_glance_tries() {
  arguments_assert(arguments, 0);
  ("How many looks at the road go by between one turn of the waiting walker's head and the");
  ("next.");
  ("THREE, which at the quick pace the road is checked at is a little over half a second a");
  ("glance. That is about how long a person actually holds a look down a street before");
  ("turning the other way - fast enough to read as watching, slow enough not to read as a");
  ("head shaking.");
  ("It is a count of looks rather than a length of time because the checking is what the");
  ("waiting is made of, and a second clock would be a second thing that could drift away from");
  ("it. Turning the head is something that happens every so many looks, which is also the");
  ("plainest way to say it.");
  let tries = 3;
  return tries;
}
