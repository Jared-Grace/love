import { arguments_assert } from "./arguments_assert.mjs";
export function reply_names_kalenjin() {
  arguments_assert(arguments, 0);
  ("Given names in Kalenjin, spoken in Kenya.");
  let names = ["chebet", "cheruiyot", "kiptoo", "korir", "rotich"];
  return names;
}
