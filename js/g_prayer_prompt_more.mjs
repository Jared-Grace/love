export function g_prayer_prompt_more() {
  "the same prayer prompt asked AGAIN, once at least one part of a multi-part prayer has already been prayed - else names what is left, so it only fits after a first part";
  let v = "What else would you like to pray to God?";
  return v;
}
