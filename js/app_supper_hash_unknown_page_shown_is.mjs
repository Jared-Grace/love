import { app_supper_hash_fields } from "./app_supper_hash_fields.mjs";
import { app_shared_hash_fields_unknown_page_shown_is } from "./app_shared_hash_fields_unknown_page_shown_is.mjs";
export function app_supper_hash_unknown_page_shown_is(context, hash) {
  "What a supper page puts in front of its own work: did the link name a bible we do not ship, and has the reader been told so and offered the near ones.";
  "Both supper pages ask through here, so neither can end up answering for a link the other lets past.";
  let fields = app_supper_hash_fields();
  let shown = app_shared_hash_fields_unknown_page_shown_is(
    context,
    hash,
    fields,
  );
  return shown;
}
