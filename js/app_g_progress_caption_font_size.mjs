export function app_g_progress_caption_font_size() {
  "the size of the line under g's progress bar saying how far along it is, matching the caption on the shared bar it was modelled on";
  let v = "0.8em";
  return v;
}
