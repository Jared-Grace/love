import { arguments_assert } from "./arguments_assert.mjs";
import { js_keyword_true } from "./js_keyword_true.mjs";
import { js_keyword_false } from "./js_keyword_false.mjs";
export function app_code_lesson_value_words() {
  arguments_assert(arguments, 0);
  ("the two words a comparison comes out as, which are the only values these lessons write that are not numbers.");
  ("Asked for as a pair because every reading that wants one of them wants the other in the same breath: whether a card shows a value at all, and which end of the line that value sits at.");
  let t = js_keyword_true();
  let f = js_keyword_false();
  let words = [t, f];
  return words;
}
