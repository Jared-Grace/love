export function app_g_verify_book_label_font_size() {
  "the size of the book name written over a row of chapter buttons, small because the buttons under it are what is being chosen";
  let v = "0.85em";
  return v;
}
