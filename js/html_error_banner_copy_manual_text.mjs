export function html_error_banner_copy_manual_text() {
  "The words the copy control on the development error band wears when the browser would not hand the clipboard over, and the message has been selected for the reader instead.";
  "A phone reaches these pages over plain http, where a browser refuses the clipboard outright - so this is the ordinary answer on the very machine the band was built for, not a rare one. The message is selected first and then these words appear, so the next thing to do is already set up and only needs a press and hold.";
  let c = "Hold to copy";
  return c;
}
