export function app_g_verify_title_font_size() {
  "the size of the chapter name at the top of the verify page, the largest thing there because it says which chapter everything below belongs to";
  let v = "1.5em";
  return v;
}
