export function app_replace_rule_set_hash_key() {
  "The word standing before the rule set's number in the link the replace app shows - published in every link anybody saved, so nothing here may reword it.";
  let v = "set";
  return v;
}
