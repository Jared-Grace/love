export function app_shared_code_padding_x() {
  "the sideways room inside a piece of code: enough that the lettering is not touching the edge of the block it sits in, and little enough that the block still reads as the width of what it says";
  let v = "0.37em";
  return v;
}
