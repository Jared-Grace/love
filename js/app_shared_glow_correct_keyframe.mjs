export function app_shared_glow_correct_keyframe() {
  "the gold→white 'correct answer' pulse keyframe, shared by app_g's pray-for-discernment glow and app_code's correct-answer highlight";
  let keyframe =
    "@keyframes correctPulse { 0% { box-shadow: 0 0 0.5em 0.15em #ffd633; } 100% { box-shadow: 0 0 1em 0.35em #ffffff; } }";
  return keyframe;
}
