export function app_shared_color_pink_pastel() {
  "a soft pink, as strong as the medium blue card and the green card so it stands beside them as one more card colour rather than as an error";
  let c = "rgb(255, 205, 215)";
  return c;
}
