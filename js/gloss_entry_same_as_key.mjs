export function gloss_entry_same_as_key() {
  "The name of the part of a word explanation that points at another word's explanation instead of carrying one of its own.";
  "It is a name rather than a spelling at each place that reads it, for the same reason the explanation's own name is: the store is written by one thing and read by several, and a pointer nobody can find reads as an explanation that says nothing.";
  let r = "same_as";
  return r;
}
