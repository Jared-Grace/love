export function app_code_expression_chip_border_width() {
  "how thick an edge an operator chip is drawn with";
  "Thicker than a button's, because a chip is a single character wide and set in the size of the writing around it, so an edge measured to be found on a button the width of the screen is barely there on this.";
  let width = "0.1em";
  return width;
}
