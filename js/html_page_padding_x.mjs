import { html_style_padding_x } from "./html_style_padding_x.mjs";
export function html_page_padding_x(element) {
  ("a breath of space on the left and right so content near the screen edges stays easy to see and easy to tap on small screens; desktop doesn't need it but the same room does no harm there");
  html_style_padding_x(element, "5dvw");
}
