export function ebible_credits_name() {
  "What the one file holding every translation's credit is called, and the folder it sits in beside the translations themselves.";
  "One name spelled once rather than twice, because the side that publishes it and the side that reads it have to agree, and two spellings of a name agree only until somebody changes one of them.";
  "No translation eBible carries is called this, so a folder of this name cannot collide with one.";
  let v = "credits";
  return v;
}
