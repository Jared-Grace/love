import { arguments_assert } from "./arguments_assert.mjs";
export function js_comment_start() {
  arguments_assert(arguments, 0);
  ("the two slashes that begin a note in JavaScript, and that tell the machine to skip the rest of the line");
  ("Two places spell it and so it is one function: the line a note is built into, and the screen that names the symbol on its own so a learner can be shown what to look for. Written twice, a screen could name one symbol while the code beside it used another, and nothing would say so.");
  let slashes = "//";
  return slashes;
}
