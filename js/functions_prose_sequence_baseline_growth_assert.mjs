import { functions_prose_sequence_baseline_path } from "./functions_prose_sequence_baseline_path.mjs";
import { baseline_known_growth_assert } from "./baseline_known_growth_assert.mjs";
export async function functions_prose_sequence_baseline_growth_assert(known) {
  "Refuse to record an offender the baseline did not already hold. A ratchet that can be rewritten in both directions is not a ratchet, and the rewrite would be reached for at exactly the moment the gate went red, which is the moment it was doing its job.";
  "The first seeding has no file to compare against and is allowed, and so is any rewrite that only drops names.";
  let path = functions_prose_sequence_baseline_path();
  await baseline_known_growth_assert(
    known,
    path,
    "recording these as known would bless a new offence rather than repair it - these functions explain themselves in a shape the reader that gathers explanations cannot see, so nothing they say can be found by meaning - write each explanation as its own sentence standing alone",
  );
}
