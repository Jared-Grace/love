import { arguments_assert } from "./arguments_assert.mjs";
import { firebase_hosting_ignore_walked } from "./firebase_hosting_ignore_walked.mjs";
import { property_get } from "./property_get.mjs";
export async function firebase_hosting_ignore_missing() {
  "The testing-stage folders the sending has not been told to leave behind, which is to say the ones the next sending would put on the public internet.";
  "The reading itself lives next door and answers this beside how many stages it asked about. Both are wanted and neither is wanted everywhere: a ratchet is measured against a flat list and a gate needs the count, so the narrowing is done once here rather than at each place that only wants the names.";
  arguments_assert(arguments, 0);
  let told = await firebase_hosting_ignore_walked();
  let missing = property_get(told, "missing");
  return missing;
}
