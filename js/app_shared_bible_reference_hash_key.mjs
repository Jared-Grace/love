export function app_shared_bible_reference_hash_key() {
  "The word standing before a written bible reference in the link a bible page shows, so a link somebody saved or sent still opens on the passage it names.";
  let v = "ref";
  return v;
}
