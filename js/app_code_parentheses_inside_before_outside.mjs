import { arguments_assert } from "./arguments_assert.mjs";
import { text_combine } from "./text_combine.mjs";
export function app_code_parentheses_inside_before_outside(lead) {
  arguments_assert(arguments, 1);
  ("the one sentence the parentheses lessons say about what ( and ) do, held in one place so the lessons saying it cannot drift into saying it almost the same way. The caller passes only its own lead-in - the first lesson has none, and the one-side lesson prefixes show that whatever is inside, because there the brackets change no answer and so what they do is SAY something rather than reorder any work.");
  ("Two lessons is early for a shared string, and bytes are not the reason. These two must stay word-identical: a learner meets this sentence as the definition and then meets it again as a recall, and a recall they recognise is passed over in the second it takes, while a near-copy stops them to compare two sentences that mean the same thing. Sameness here is the feature, so it is worth a function to make it unbreakable.");
  ("A lesson with something of its OWN to say about the brackets must not reach for this - it should say its own thing. The same-strength lesson says they beat working left to right; the both-sides lesson says each side is worked out on its own. Paraphrase earns a card only when it comes from a new angle; without one, this function is the answer.");
  let r = text_combine(lead, " is solved before whatever is outside");
  return r;
}
