export function app_shared_color_gold_glow() {
  "the one sacred gold used by every glow keyframe — the correct-answer pulse, the sacred-emoji breath, and the gold guide tile; rich and saturated so it reads as light rather than as a surface, and it fades to white at the peak of every pulse";
  let c = "#ffd633";
  return c;
}
