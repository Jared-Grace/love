export function app_shared_spaced_neighbor_gap() {
  "the gap a small card or chip keeps from the one standing next to it - the thinnest space in the repo, small enough that two of them read as touching rather than as separated, which is what tells a reader the pair belongs together. Below the tiny gap, which is what a card holds its own contents off its edges by";
  let v = "0.15em";
  return v;
}
