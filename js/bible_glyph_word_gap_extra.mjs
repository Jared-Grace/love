export function bible_glyph_word_gap_extra() {
  "How much wider than an ordinary space the gap between two picture Bible words is drawn.";
  "IT IS THE MARK THAT SAYS WHERE ONE WORD ENDS, and it is the only one this Bible has for the job. Several glyphs standing together are ONE word, so the reader has to be able to tell two glyphs of one word from two glyphs that are two words - and what tells them is that the first pair touch and the second pair do not. That comparison is only available if the two gaps are obviously different sizes, and an ordinary space next to a glyph an em and a bit wide is not obviously anything.";
  "A GAP COSTS NOTHING TO TEACH AND A DRAWN MARK COSTS A LESSON. Every symbol this Bible uses is paid for before the first verse, in a tutorial that has to teach it from nothing; the space between words is the one device every writing system on earth already taught its readers, so it arrives free. That is the whole argument for spacing over a ring drawn round the group, which is what this replaced.";
  "AND IT NESTS, WHICH A RING COULD NOT. A ring is drawn or it is not, so a group inside a group had no way to be written and was flagged as not built. Gaps have sizes, so a smaller gap inside a larger one inside a word gap says the same thing in three levels without a new mark. Nothing writes a nested group yet and this is not that feature - it is the reason the door is left open.";
  "The width is in EMS because the pictures are, so a reader who made this Bible bigger gets the gaps bigger too and the comparison the whole scheme rests on survives at every size.";
  let extra = "0.4em";
  return extra;
}
