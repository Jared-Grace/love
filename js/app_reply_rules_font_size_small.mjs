export function app_reply_rules_font_size_small() {
  "The size of the writing in a proposed change shown for review - the file names under it and the lines of the change itself - kept smaller than the page so whole files fit where a reviewer can see them.";
  let v = "0.75em";
  return v;
}
