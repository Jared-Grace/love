import { arguments_assert } from "./arguments_assert.mjs";
export function app_original_bible_gloss_text_index() {
  "Where the wording this store explains stands among a passage's texts, once the passage has been laid out for reading.";
  "This store explains the Hebrew and the Greek themselves, and those lines are kept under their own name rather than among the texts. The laying out puts them first and keeps nothing else, so the wording under explanation is the only text there is.";
  arguments_assert(arguments, 0);
  let r = 0;
  return r;
}
