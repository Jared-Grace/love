export function app_shared_glow_correct_animation_name() {
  ("what the gold correct-answer pulse is called in the page's own stylesheet");
  ("The keyframe writing it and the element asking for it have to spell the same word, and neither can see the other. Spelled once, they cannot disagree; spelled twice, a change to one leaves the element naming an animation nothing defines, and a glow that never starts throws nothing and says nothing.");
  let name_animation = "correctPulse";
  return name_animation;
}
