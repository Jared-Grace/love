export function ebible_book_code_apart_maximum() {
  "How far a mistyped book of the bible may be from a real one and still be worth offering back as a guess.";
  "Two, where a language code allows one, and the reason is that a book is guessed against its name as well as its code. Somebody who writes JOH means John and has spelled the name nearly right while spelling the code nearly wrong; measured against the code alone that is two edits, and refusing two would refuse the one guess they actually meant. Only the closest tier is ever offered, so raising the limit widens what can be found without widening what is shown.";
  let v = 2;
  return v;
}
