export function app_code_lesson_statement_name_identifier_word() {
  ("the value the cup holds on the screen that says a name can be any identifier");
  ("A word of the same kind as the name, and not the same word. The one thing this screen has to make unmistakable is which of the two words on a line comes out, and it can only do that if a learner cannot tell them apart by what kind of word they are - if the name were a letter and the value a word, the right answer could be found without reading anything.");
  let word = "peace";
  return word;
}
