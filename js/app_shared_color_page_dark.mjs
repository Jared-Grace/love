export function app_shared_color_page_dark() {
  "The ground a dark page is painted on.";
  "It was written straight into the one function that makes a dark page, which was right while exactly one thing needed it. A second thing needs it now - a bar that stays at the top of a page as it scrolls has to be painted the page's own colour or the page shows through it - and two places spelling one colour is the shape that drifts silently: somebody improves one of them and nothing anywhere says the other exists.";
  return "#0b0b0b";
}
