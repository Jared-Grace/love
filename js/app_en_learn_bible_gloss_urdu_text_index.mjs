import { arguments_assert } from "./arguments_assert.mjs";
export function app_en_learn_bible_gloss_urdu_text_index() {
  "Where the wording this store explains stands among a passage's texts.";
  "This store teaches English to a reader who already has Urdu, so the English stands first and the Urdu beside it is what explains it. Every word explanation is about the first, and the explanation itself is written in the second.";
  arguments_assert(arguments, 0);
  let r = 0;
  return r;
}
