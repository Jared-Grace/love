export function ebible_readaloud_lines_offered_unchecked_baseline_path() {
  "Where the record of bibles already known to be offered without a chapter of them having been read is kept.";
  let path =
    "data/given/baselines/ebible_readaloud_lines_offered_unchecked_baseline.json";
  return path;
}
