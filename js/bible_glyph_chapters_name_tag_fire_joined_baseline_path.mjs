export function bible_glyph_chapters_name_tag_fire_joined_baseline_path() {
  "Where this ratchet keeps the verses that write the two marks side by side on purpose. Reading it, rewriting it and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path =
    "data/given/baselines/bible_glyph_chapters_name_tag_fire_joined_baseline.json";
  return path;
}
