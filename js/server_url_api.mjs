export function server_url_api() {
  "The path on the dev server that runs a function by name: a page posts the name and its arguments here and receives what the function returned. It is a value rather than a spelling repeated at each end, because the server matching on it and every caller addressing it have to agree, and two sides of one word are exactly what drifts.";
  let a = "/api";
  return a;
}
