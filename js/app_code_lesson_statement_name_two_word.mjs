export function app_code_lesson_statement_name_two_word() {
  ("what the second cup holds on the screen that says you can have more than one name");
  ("Olives, beside the grapes the first cup has held since the cup was drawn. The first cup keeps what it always had, so the only thing new on the screen is the cup beside it - had both changed, a learner would have two changes to account for and neither would be the lesson.");
  let word = "olives";
  return word;
}
