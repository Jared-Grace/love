import { arguments_assert } from "./arguments_assert.mjs";
export function bible_glyph_chapter_rosetta_lines_prose_lines() {
  arguments_assert(arguments, 0);
  ("The paragraphs every written-out Rosetta band file opens with, in the order they are stamped into it: what the file is, that a command wrote it, why it is committed instead of fetched, and on what terms its two bands may be shown at all.");
  ("THE LAST ONE IS A LICENCE CLAIM AND THAT IS WHY THIS IS A FUNCTION RATHER THAN A SENTENCE INSIDE THE WRITER. Two files of somebody else's scripture once carried a hard-coded line saying they held a public-domain bible, and both were Creative Commons Attribution Share-Alike; the line was corrected in the writer and the files that were already written went on saying the old thing for as long as nobody opened them. A claim about what may be published is the last text in a repo that should be spelled twice.");
  ("SO THE WRITER READS IT HERE AND A GATE READS IT HERE, and improving a word of it turns every band file already on disk red until it is written again. That is the whole point: the red is the notice that files on disk are saying something their writer no longer says.");
  ("WHAT THE LAST PARAGRAPH CLAIMS IS TRUE OF THESE TWO BANDS AND OF NOTHING ELSE. The original band is the Berean interlinear's own base text, dedicated to the public domain under CC0, with the readings belonging to editions the Berean people do not own stripped out before anything reads a word. The English band is the Berean Standard Bible, whose publisher's own copyright page on this machine says Public Domain. Neither asks for attribution, which is why no credit block is written here and why one would be a different gate; a band built from any other translation would owe one.");
  let lines = [
    "One chapter of the picture Bible as its two known Rosetta bands: each verse in the language it was written in, and the same verse in English.",
    "THIS FILE IS WRITTEN BY A COMMAND AND NOT BY HAND. It is the interlinear read once, at authoring time, for the verses the hand-written picture chapter has reached.",
    "IT IS COMMITTED RATHER THAN FETCHED because the browser is the one place these lines are read and the thing that builds them cannot run there. Authored Bible text already lives as committed functions here, and this is authored Bible text.",
    "Both bands are text anybody may be shown: the original is the public-domain base text, and the English is the Berean wording, which this repo already publishes as one of its own translations and reads as public domain off the publisher own licence page. The English stands in English order, because the column it is built from is that translation cut into chunks and hung on the original words - chunks that straddle the words they hang on, so read in the original order they come out as neither language.",
  ];
  return lines;
}
