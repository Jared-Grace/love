export function app_shared_bible_verses_count_default() {
  "How many verses a bible page shows when the link says nothing about it.";
  "One, because that is what every link written before this word existed meant, so nothing already sent reads differently now than it did when it was sent.";
  "It is written as text rather than as a number because a link is text, and the word this stands in for arrives as text. Reading the two the same way is what keeps the missing case and the given case on one path.";
  let v = "1";
  return v;
}
