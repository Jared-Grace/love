export function hash_number_label(number) {
  "How a count is worded on a button offering it to a reader whose link wrote something else where the count should stand.";
  "A number is already the plainest form of itself, so it is offered back exactly as it will be written into the link. The fields naming things - a language, a book, a lesson - all say more on their buttons than their links carry, and this one has nothing more to say.";
  let said = number;
  return said;
}
