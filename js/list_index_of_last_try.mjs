import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function list_index_of_last_try(list, item) {
  "Where an item sits in a list counting from the end, or a number below zero when the list never holds it.";
  function_duplicate_kind_parallel();
  let v = list.lastIndexOf(item);
  return v;
}
