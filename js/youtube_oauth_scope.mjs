export function youtube_oauth_scope() {
  "The one permission asked of Google: to manage this account's YouTube channel.";
  "It is the narrowest word that still allows a video's title to be written, which is the whole errand. The read-only permission cannot write and the partner permissions reach accounts this is not, so anything else asked for would be permission held and never used.";
  let scope = "https://www.googleapis.com/auth/youtube";
  return scope;
}
