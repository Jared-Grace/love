export function indexeddb_name_parameters() {
  "The words a browser-database function calls a parameter when what stands there is the name of a kept thing - the database itself, or one store inside it.";
  "A name like this is published the moment anything is written under it. It is on a disk in somebody's browser after that, and no later edit here can reach it, so it may never be reworded in place - which is the whole reason for watching where it comes from.";
  "Spelled once because two readings ask about it: the one that works out which argument of which function is a name, and the one that judges what is standing there. Two lists would be free to disagree, and the disagreement would show up as a gate quietly watching fewer places than it says.";
  let names = ["name", "store"];
  return names;
}
