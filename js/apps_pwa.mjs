export function apps_pwa() {
  "apps opted in to install as a PWA — a manifest link + service worker are emitted, and .webmanifest + icon PNGs are generated for them; add one short app name per line to opt an app in";
  let r = ["verses"];
  return r;
}
