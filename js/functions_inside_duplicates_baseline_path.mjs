export function functions_inside_duplicates_baseline_path() {
  "where the groups of functions already sharing a run of work somewhere inside them are recorded";
  let v = "data/inside_duplicates_baseline.json";
  return v;
}
