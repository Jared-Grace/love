import { arguments_assert } from "./arguments_assert.mjs";
export function app_music_lines_instruction_text() {
  "The one sentence at the top of a song telling a reader that the lines open.";
  "IT IS THERE BECAUSE A SHUT LINE LOOKS LIKE A FINISHED ONE. The words of the song are all a reader sees at first, and nothing about a line says that the passages it rests on are folded up behind it - so somebody could read the whole page and never learn that the thing they came for is one tap away.";
  arguments_assert(arguments, 0);
  let said = "Click a line to open the Scripture it rests on.";
  return said;
}
