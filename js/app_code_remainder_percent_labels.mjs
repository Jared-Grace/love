export function app_code_remainder_percent_labels() {
  "the topic-specific quiz and example labels shared by every plain n % d remainder lesson (remainder by 2, remainder by 3, remainder by 4, and remainder for any divisor): the % form is the remainder expression, and the value it works out to is the remainder value. Single-sourced here so the wording stays identical across all of them";
  let labels = {
    forwards_question_label: "Remainder expression: ",
    forwards_answer_label: "remainder value: ",
    backwards_question_label: "remainder value: ",
    backwards_answer_label:
      "What remainder expression produces the remainder value? ",
    unscramble_label:
      "Build the remainder expression that produces the remainder value: ",
  };
  return labels;
}
