export function verse_number_key() {
  "The word under which a verse keeps its number, in every chapter this repo has written to storage and in every offline package a reader is holding.";
  let v = "verse_number";
  return v;
}
