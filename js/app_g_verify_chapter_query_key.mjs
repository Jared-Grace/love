export function app_g_verify_chapter_query_key() {
  "the word a verify link carries a chapter in after the question mark - saved and sent in links, so it may never be reworded";
  let v = "chapter";
  return v;
}
