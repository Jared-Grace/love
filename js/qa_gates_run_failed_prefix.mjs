export function qa_gates_run_failed_prefix() {
  "what a run of the gates writes in front of its closing summary when it has asked every gate and some of them complained, and what reading a run back looks for";
  "It is spelled once here because two different readers want it. The run writes it as the last thing it does; a reader working out whether a run got that far at all looks for it. Spelled twice, one of them could be corrected and the other left behind, and the reader would then quietly answer no about every run.";
  let v = "qa gate: ";
  return v;
}
