import { fn_name } from "./fn_name.mjs";
export function app_shared_color_gold_text_light() {
  ("a LIGHTER gold text for Scripture VERSES on a dark background (",
    fn_name("app_g_scripture_verse"),
    ") — lighter than the base ",
    fn_name("app_shared_color_gold_text"),
    ", which the dove's God's-LEADING PROSE now uses, so the verse and the leading message read distinct on the same card");
  ("The song-image tool marks what is being looked at in the same gold - the couplet now open, its words, and the outline of the candidate standing in the frame. Whether the two mean the same thing by it is not a question anybody can answer while they still agree, so they share one function until something forces them apart, and the forcing is what would answer it.");
  let v = "#ffe994";
  return v;
}
