import { arguments_assert } from "./arguments_assert.mjs";
export function song_image_brighter_swaps_name() {
  "The name the list of the hymn's pictures set beside their brighter copies is kept under, among the lists of a song's pictures on offer.";
  "IT IS NOT A SONG'S NAME, and nothing offers it as one: the songs are read off their documents, and this has a list of offers and no document.";
  arguments_assert(arguments, 0);
  let name = "the_FATHERs_SON_brighter";
  return name;
}
