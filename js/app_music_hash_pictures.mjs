import { arguments_assert } from "./arguments_assert.mjs";
import { text_frozen } from "./text_frozen.mjs";
export function app_music_hash_pictures() {
  "The word after the slash in a song's address that opens the song with every picture already showing, as in #the_FATHERs_SON/pictures.";
  "IT IS FROZEN because it is handed out inside links, and a link already sent keeps spelling it the old way whatever this is renamed to.";
  arguments_assert(arguments, 0);
  let word = text_frozen("pictures");
  return word;
}
