export function example_note_code_font_size() {
  "the size of a piece of code written inside the prose of an example note, a little under the prose so it reads as a quotation rather than as a heading";
  let v = "0.9em";
  return v;
}
