export function android_alarms_install_steps() {
  `One-time phone setup, then run android_alarms_install to put the app on the phone:

  1. On the phone: open Settings, tap "About phone", tap "Build number" seven times to unlock Developer options
  2. Back in Settings, open "Developer options" and turn on "USB debugging"
  3. Connect the phone to this computer with a USB cable
  4. On the phone, tap "Allow" when it asks whether to trust this computer
  5. Run android_alarms_install (this builds the app and installs it onto the phone in one step)
  6. On the phone, open "JESUS rose to life Alarms" and tap "Sync alarms" — the alarms appear in the phone's Clock app`;
}
