import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { app_shared_game_npc_property_get_null_generic } from "./app_shared_game_npc_property_get_null_generic.mjs";
export function app_shared_game_npc_ring_get(npc) {
  function_duplicate_kind_parallel();
  ("The ring drawn under a person to say they share a house with somebody already prayed");
  ("for, found by WHO that person is - the same lookup their picture and their light use,");
  ("and keyed the same way so all of them are found and moved together.");
  ("It is a second mark and not a change to the first, because the two say different things");
  ("and one of them ends. A light says this person has been prayed for and never goes out; a");
  ("ring says this person is one of the ones left in a house the player has started, and it");
  ("is taken away the moment they are prayed for. Written into the one light, the light");
  ("would have to be able to go backwards.");
  ("It is filed rather than drawn again each time the screen is painted, for exactly the");
  ("reason the light is: a mark placed from a tile lands at the square a walking person is");
  ("heading for rather than the one they are crossing, so it runs on ahead of them and waits");
  ("there. Made before anybody has taken a step, it is carried by their steps instead.");
  ("Nothing is the ordinary answer here rather than a fault, because a game where nobody has");
  ("been prayed for yet has rung nobody.");
  let ring = app_shared_game_npc_property_get_null_generic(
    app_shared_game_npc_ring_get,
    npc,
  );
  return ring;
}
