import { app_shared_color_gray_medium } from "./app_shared_color_gray_medium.mjs";
export function app_shared_text_deemphasized_color_on_dark() {
  "the quieted-down grey as it has to be on a strong dark background: light instead of dark, so it is still plainly readable while staying a step back from the white words beside it.";
  "the palette's middle gray, rather than a shade beside it: light enough on the dark row to read plainly - measured at about seven to one - while staying a clear step back from the white name next to it.";
  let c = app_shared_color_gray_medium();
  return c;
}
