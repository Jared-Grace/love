export function app_code_match_color() {
  "a bright amber used to paint two tokens the SAME colour so the eye links them - here the first digit after a decimal point inside a number and the standalone digit named in the sentence. Distinct from the normal near-white code font, and warm rather than the orange literal colour, so it reads as a highlight that says these two are the same";
  let r = "#ffd23f";
  return r;
}
