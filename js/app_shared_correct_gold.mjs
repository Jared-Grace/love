import { app_shared_scripture_gold } from "./app_shared_scripture_gold.mjs";
export function app_shared_correct_gold(element) {
  "the gold surface applied to the choice God reveals as correct — the same written-Word gold surface as app_shared_scripture_gold (pairs with app_*_glow_correct)";
  app_shared_scripture_gold(element);
}
