import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_privacy_policy_updated() {
  "The day the privacy policy last said something different from what it says now.";
  "It is kept away from the promises rather than sitting at the end of them, because it is not one. Every other line is a claim about what the code does and can be checked against the code; this one is a fact about the document, and a reader deciding whether to trust the page should not have to work out which kind each line is.";
  "Change it when the words change, and only then. A date that moves whenever the file is touched tells a reader nothing, and a reader who has read the policy before is looking at this line to find out whether they have to read it again.";
  arguments_assert(arguments, 0);
  return "Last updated 21 August 2026.";
}
