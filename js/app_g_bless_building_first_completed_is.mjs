import { arguments_assert } from "./arguments_assert.mjs";
import { bless_hash_building_first_completed } from "./bless_hash_building_first_completed.mjs";
import { app_g_bless_dev_opening_is } from "./app_g_bless_dev_opening_is.mjs";
export function app_g_bless_building_first_completed_is() {
  arguments_assert(arguments, 0);
  ("Whether this visit asked for a world that opens with the first building one prayer from finished.");
  ("Held back unless the dev tools are on offer, like every other opening that writes into the record. A record that begins with eight prayers nobody said is a lie about the player's own work, and the one gate that asks about that is spelled once rather than here.");
  let word = bless_hash_building_first_completed();
  let words = [word];
  let asked = app_g_bless_dev_opening_is(words);
  return asked;
}
