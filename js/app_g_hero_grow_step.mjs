import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_grow_step() {
  arguments_assert(arguments, 0);
  ("How much taller the player stands for each evil person she has stopped.");
  ("A FOURTEENTH of her height, which is about the width of the line her own picture is drawn");
  ("with. That is deliberately close to the smallest change that can be seen at all: the");
  ("player is not told this is happening and there is no number anywhere on the screen, so it");
  ("has to be discovered, and a thing is only discovered if it was too small to be announced.");
  ("Big enough to see one of, though, because a growth nobody ever catches in the act is the");
  ("same as no growth. The step is taken all at once and with a swell rather than eased in");
  ("over the following minute, so whoever is looking at her when it happens sees it happen.");
  ("It is a SHARE of her height rather than a number of pixels, so it means the same on a");
  ("phone held close and a laptop across a desk, and so it compounds - which is what makes");
  ("the tenth one feel like the tenth rather than like the first again.");
  let step = 0.07;
  return step;
}
