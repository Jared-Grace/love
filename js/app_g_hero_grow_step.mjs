import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_grow_step() {
  arguments_assert(arguments, 0);
  ("How much taller the player stands for each evil person she has stopped.");
  ("A TENTH of her height. That is comfortably more than the smallest change that can be seen, which is where it used to sit - a fourteenth, about the width of the line her own picture is drawn with. Sitting at the threshold was a deliberate choice and it was wrong in one direction only: the player is not told this is happening, so it has to be discovered, and a change nobody catches in the act is not discovered, it is missed. The human watching a first victory reported the growing as too little.");
  ("It is the same for every victory, the first and the tenth alike. A step that started large and tapered would say the later ones were worth less, and they are not - what is worth less is a figure taller than the houses, and that is what the ceiling is for.");
  ("It is a SHARE of her height rather than a number of pixels, so it means the same on a phone held close and a laptop across a desk.");
  ("Bigger steps reach the ceiling sooner - about ten of them now rather than about fifteen. That is the price, and it is the right way round to pay it: a player who never noticed the growing got nothing from any of the fifteen.");
  let step = 0.1;
  return step;
}
