export function app_code_head_gap_above_code() {
  "the room a card's words stand off by when the Code label is the next thing under them";
  "Less than the one gap the app spaces things by, because these two are not two things on a card - the words are about the very line the label is introducing, and the app's full gap between them reads as the saying having finished rather than as it pointing one line down. Nothing at all read as the two being one line of text broken in the middle, which is what this is here to stop.";
  let v = "0.5em";
  return v;
}
