export function permission_settings_local_baseline_path() {
  "the file recording which per-machine allow rules the shared list does not hold";
  let v = "data/permission_local_baseline.json";
  return v;
}
