export function app_replace_abbreviations_baseline_path() {
  "Where the record of explanations and rules already failing to answer to each other is kept.";
  let p = "data/app_replace_abbreviations_baseline.json";
  return p;
}
