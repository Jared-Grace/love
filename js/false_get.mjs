import { arguments_assert } from "./arguments_assert.mjs";
export function false_get() {
  "False, as something that can be handed over and called later.";
  "It is here for the places that take a question as a function and are being told the answer is always no. Written out as a lambda at each of those sites it is the same four words again, and the lambda hides from any search for who answers that question with a flat no.";
  arguments_assert(arguments, 0);
  return false;
}
