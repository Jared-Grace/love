export function commands_only_hook_path() {
  "where the file that refuses the editing tools while the commands-only switch is on lives";
  let v = ".claude/hooks/commands_only_write_deny.mjs";
  return v;
}
