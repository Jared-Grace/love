import { arguments_assert } from "./arguments_assert.mjs";
export function song_image_color_border_resting() {
  "the near-black line the song-image tool draws where nothing is being pointed at - the outline of a candidate row nobody is looking at, the box the prompt is copied out of, and the rule between one couplet and the next on the audit page";
  "It is the shade the marked outline steps away from, so the two are chosen together: a row goes from this to the warm yellow beside it and back, and anything drawn in a third shade would read as a third state the tool does not have.";
  arguments_assert(arguments, 0);
  let c = "#222222";
  return c;
}
