export function qa_in_flight_probe() {
  "temporary: references a repo function it never imports, to make one gate go red on an uncommitted file";
  let items = [];
  let count = list_size(items);
  return count;
}
