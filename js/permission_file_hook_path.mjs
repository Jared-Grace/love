export function permission_file_hook_path() {
  "where the file that grants the file tools straight from the settings rules lives, so a new grant works in the session that wrote it";
  let v = ".claude/hooks/permission_rule_file_allow.mjs";
  return v;
}
