import { baseline_names_ending } from "./baseline_names_ending.mjs";
export async function baseline_paths_names() {
  "every function in the repo that names where a ratchet's record is kept, found by what it is called";
  "the sibling of the writers sweep, and now literally so - the finding lives in the one they both call, and what is left here is the word that says which of them is wanted";
  let path_names = await baseline_names_ending(
    "_path",
    "no ratchet record path was found at all, so whatever asked has learned nothing - the repo is not this small, so the sweep itself is what has stopped working",
  );
  return path_names;
}
