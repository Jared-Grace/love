export function app_code_string_value_color_on_light() {
  "the string VALUE colour tuned for the pale-blue definition container (the bolded term 'value of a string'): the same blue hue as the value colour on the black tiles, but darker/more saturated so it has enough contrast on the light background. Same concept, one shade darker for the light surface";
  let r = "#2b6fff";
  return r;
}
