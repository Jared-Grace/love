export function folder_backups() {
  "Where a copy of a file is kept before something is about to overwrite it, said as the path from the repo's own root.";
  "Inside the data folder rather than beside it, because a kept copy of a data file is data - one of the same files, at an earlier moment. Sitting at the root it read as a ninth thing the repo is made of, which is the one thing it is not.";
  "In the found half of that folder rather than the given half, because a copy taken before an overwrite is a record of what was already there, not something a later run reads to decide anything. That is not a filing preference. The two commands that ask whether a function name is still spoken for read the given half only, so every name spelled in a kept copy used to make its function undeletable and rewritable by a rename - four hundred and forty live names, held by nothing but a copy of how the file once read.";
  let f = "data/found/backups";
  return f;
}
