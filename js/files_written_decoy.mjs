export function files_written_decoy() {
  "Present only to be left alone: a file dirty in the same folder at the same";
  "moment, written by a hand rather than by a command, so a targeted commit that";
  "picks it up has not targeted anything.";
  let left = "alone once more";
  return left;
}
