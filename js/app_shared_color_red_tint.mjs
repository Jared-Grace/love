export function app_shared_color_red_tint() {
  "a red with almost all of it taken out - red mixed so far into white that it reads as white with a cast to it rather than as a colour. It is a value, not a meaning; what it is for is said by whoever asks for it.";
  "It is the partner of the faint green and is written down at the same faintness on purpose, because the two are read against each other rather than on their own. A red that carried further than its green would make the same amount of writing look like the more serious half, which is a claim the colour has no business making.";
  let v = "#ffeef0";
  return v;
}
