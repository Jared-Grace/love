export function app_code_exponent_power_color() {
  "the color for the exponent POWER (how many times to multiply) - an orange chip, shown identically in the sentence and in the ** expression so the eye matches the count to the exponent";
  return "rgb(198, 110, 44)";
}
