export function ebible_testament_new_name() {
  "The New Testament's own name, spelled exactly as the book divisions tag it, so a reading that narrows to it matches by the one spelling rather than by a copy of it.";
  let v = "New Testament";
  return v;
}
