export function functions_shadowing_baseline_path() {
  "Where the shadowing ratchet keeps what the repo already carried. Reading it, rewriting it, and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path = "data/shadowing_baseline.json";
  return path;
}
