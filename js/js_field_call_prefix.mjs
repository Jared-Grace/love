export function js_field_call_prefix() {
  "The front of the name every call that names a field of an object shares - the one place that word is spelled, so a reading that looks for such a call and a reading that asks what it missed cannot come to disagree about which calls those are";
  let v = "property_";
  return v;
}
