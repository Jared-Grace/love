export function versions_key() {
  "The word under which a language keeps the translations it may be read in, the first of them being the one it is read in unless somebody chooses another.";
  let v = "versions";
  return v;
}
