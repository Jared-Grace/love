import { arguments_assert } from "./arguments_assert.mjs";
export function git_history_heavy_absent_kept() {
  "The large forgotten paths this repo's history is meant to go on carrying, so that the gate reading beside them does not report one of them as a fault.";
  "Not the baseline, and the difference between the two is the direction each name is travelling. The baseline is a queue of things still to be taken out and it may only ever shrink, so a name parked there has to be re-earned every time somebody tidies the queue. These are staying, and a record that only shrinks is the wrong shape for a thing that is staying.";
  "A name here is a claim and the claim has to be checkable, so it is stated rather than asserted. data/aliases.json is here because the commit log is written in shorthand. Nine thousand and thirty six commits carry a subject that is a short word rather than a function name, and that word is an alias key. Commit 13b2b74d92 says fws fw generic, and the live table has no fws in it at all - the key was not repointed somewhere else, it was retired. So the aliases this repo answers to today cannot read its own old log, and the only thing that can is the table as it stood at each of those commits, which is this file's own past. It weighs a hundred and five kilobytes and there will never be another copy of it.";
  "What each retired key stood for is deliberately not written out here. That would be a live name sitting inside a record of something that happened once, and a later rename would follow it and leave the record saying a thing nobody did. The commit is named instead, because a commit cannot be renamed.";
  arguments_assert(arguments, 0);
  let paths = ["data/aliases.json"];
  return paths;
}
