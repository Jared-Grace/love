export function app_shared_success_emoji_font_size() {
  "the size of the row of emojis in a success message, larger than its words because the picture is what carries the gladness";
  let v = "1.5em";
  return v;
}
