export function text_from_number(num) {
  "$plain num";
  "A number written out as text, in the plainest way there is to write it.";
  "THE OTHER DIRECTION HAS BEEN NAMED HERE FOR A LONG TIME AND THIS ONE HAD NOT, so callers wanting it reached for the padding function and asked it to pad to a width of one. That works, but it says the caller cares about a width when it does not, and a reader has to stop and work out that a width of one is the width every number already has.";
  let t = String(num);
  return t;
}
