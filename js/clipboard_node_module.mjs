import { arguments_assert } from "./arguments_assert.mjs";
export async function clipboard_node_module() {
  "The machine's own clipboard, fetched at the moment it is wanted, for the times this is running at a terminal rather than inside a page.";
  "IT IS ONE PLACE ON PURPOSE, AND THAT IS THE WHOLE REASON IT EXISTS. Fetching a library while running is what tells the builder to cut it out into a script file of its own, and it cuts one out per place that asks. Copying and pasting asked separately, so two files came out carrying the same three hundred and seventy nine bytes, and the page only ever sends for one of them - the other stood in the published folder as a leftover nothing reached, refusing every sending of three of the reading apps, and rebuilt itself every time the app was built again.";
  "Asked from one place there is one file, and the page sends for it, so there is nothing left over to refuse anything.";
  "Nothing here is reachable from a page. The two that ask this both look first at whether they are in one, and this is only ever reached when they are not - which is what lets a library that needs a machine be named at all.";
  arguments_assert(arguments, 0);
  let clipboard = await import("clipboardy");
  let r = clipboard.default;
  return r;
}
