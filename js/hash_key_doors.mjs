import { fn_name } from "./fn_name.mjs";
export function hash_key_doors() {
  "The calls that take the name of a field of the address of a page first and write it there themselves - the short way onto an address, as against changing the address as an object.";
  "Both of them store the field name they are handed, so a word written at a call to either is published the moment somebody saves the link. That makes the set of them the thing every reading of published address words rests on, and a reading that missed one would simply find nothing there and look exactly like a repo with nothing wrong.";
  "It is written down rather than worked out, which is the shape that goes stale. What keeps it honest is asked separately, by a gate that reads every caller of these two and fails on one that hands its own parameter into the field slot - because such a caller would be a third door, and a word written at a call to it would be published with nothing watching.";
  let f_name = fn_name("html_hash_property_set");
  let f_name2 = fn_name("html_hash_object_property_set");
  let doors = [f_name, f_name2];
  return doors;
}
