import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_view_render_study_fresh_save_pending(word_index) {
  arguments_assert(arguments, 1);
  let current = word_index;
  let save_pending = null;
  let r = {
    current,
    save_pending,
  };
  return r;
}
