export function app_shared_screen_hash_key() {
  "The word standing before the screen in an app's link, so a link somebody saved or sent still opens on the screen it was saved on - published in every link anybody saved, so nothing here may reword it.";
  let v = "screen";
  return v;
}
