export function app_shared_column_content_max_width() {
  ("the content column: a touch wider than the plain column cap (which is 40rem), so a full-width band can center its text and code tiles in one column while a button nested inside still fits without spilling off the right edge");
  return "42rem";
}
