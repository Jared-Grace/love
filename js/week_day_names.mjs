export function week_day_names() {
  "the seven weekday names in order, starting on Sunday, as short labels for calendar column headers";
  let names = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
  return names;
}
