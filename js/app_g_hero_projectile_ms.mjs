import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_projectile_ms() {
  arguments_assert(arguments, 0);
  ("How long a thrown power is in the air.");
  ("It is shared rather than written twice because the player's fire and the killer's dark");
  ("power are thrown at each other at the same moment and have to MEET. Two numbers that");
  ("happened to agree would come apart the first time either was tuned, and what would show");
  ("on the screen is one power arriving at an empty patch of road and waiting there.");
  let ms = 650;
  return ms;
}
