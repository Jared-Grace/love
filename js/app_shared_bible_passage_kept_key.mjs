import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_bible_passage_kept_key() {
  "The word this tab keeps the passage under while somebody is away choosing a different one, so the writer and the readers of it cannot disagree about the spelling.";
  arguments_assert(arguments, 0);
  let v = "passage_kept";
  return v;
}
