import { fn_name } from "./fn_name.mjs";
export function app_g_bless_storage_app() {
  "The praying game, named for storage keys only.";
  "Its own name rather than the gospel game's, though the two share a world and most of";
  "the code that draws it. A storage key is a note left in a browser saying what somebody";
  "was doing on THIS screen, and the two games are two screens: a directory drilled open in";
  "one of them says nothing about the other, and sharing the key would have each game";
  "quietly rearranging the other's dev screens between visits.";
  "Written as a reference rather than as plain text, so renaming the app carries every key";
  "it has left in a browser along with it.";
  "The name is all that is taken. Handing over the app's own front door instead would drag";
  "the whole app into every small file that keeps one word for one tab - measured at 410 KiB";
  "on a bundle next door.";
  let r = {
    name: fn_name("app_g_bless"),
  };
  return r;
}
