import { app_code_expression_green_uncolored } from "./app_code_expression_green_uncolored.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
import { html_display_inline } from "./html_display_inline.mjs";
import { html_style_padding_x } from "./html_style_padding_x.mjs";
export function app_code_expression_chosen_clear(node_span) {
  arguments_assert(arguments, 1);
  ("take the green off the block the learner chose: its colour, and then the shape and the room the colour needed");
  ("The room goes in the same breath as everything else that is settling, because the line closes up by exactly that much and the closing has to be one movement.");
  ("A colour switched off between two frames is read as a thing being taken away. Faded out, the same colour is read as having finished saying what it was there to say, which is the truth of it: the working out has become its value and the green was only ever there to hold the two of them together.");
  app_code_expression_green_uncolored(node_span);
  html_display_inline(node_span);
  html_style_padding_x(node_span, "0");
}
