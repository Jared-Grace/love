import { app_code_string_code } from "./app_code_string_code.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_string_any_code() {
  arguments_assert(arguments, 0);
  ('a string written as code with nothing said about what is in it: "..."');
  ("A title on the home list shows the shape of the line its lesson teaches, and a shape is not one particular line. Three dots is what a person writes when the place matters and what fills it does not, so the title can show where the value goes without picking a value - a picked one would be read as part of what the lesson is about, and a learner would wonder why that word.");
  let word = "...";
  let code = app_code_string_code(word);
  return code;
}
