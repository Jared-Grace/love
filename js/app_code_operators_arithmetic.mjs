import { app_code_operators_arithmetic_generic } from "./app_code_operators_arithmetic_generic.mjs";
import { js_operator_symbol } from "./js_operator_symbol.mjs";
export function app_code_operators_arithmetic(parent) {
  "the four arithmetic operators as code tiles joined by and - the local operator_map it used to carry read the same key the named symbol reader reads, so it was the lambda that function exists to stop callers hand-rolling";
  app_code_operators_arithmetic_generic(parent, js_operator_symbol);
}
