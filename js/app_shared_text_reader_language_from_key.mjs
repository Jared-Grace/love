export function app_shared_text_reader_language_from_key() {
  "The name a saying files its record of the english each translation was made from under, so nothing has to guess which entry is a language and which is the record.";
  "It is not a language code and never can be, because it holds an underscore and no language is named with one. That is what lets the count of who says their piece in every language take every other key as a language without keeping a list of what to leave out.";
  let key = "translated_from";
  return key;
}
