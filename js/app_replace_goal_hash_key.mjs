export function app_replace_goal_hash_key() {
  "The word standing before the goal's number in the link the replace app shows - published in every link anybody saved, so nothing here may reword it.";
  let v = "goal";
  return v;
}
