export function app_code_review_hash_key() {
  "The word standing before the review in the link the code app shows, so a link somebody saved or sent still opens on the review it was saved on.";
  "Which review a reader is on was kept in the tab alone, and a tab is what a refresh and a paste both throw away. What came back instead was the first review, because that is what the reading of it falls back to when the tab says nothing - so the reader was quietly moved to a different checkpoint and the link they sent opened somewhere else again.";
  let v = "review";
  return v;
}
