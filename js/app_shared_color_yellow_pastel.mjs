export function app_shared_color_yellow_pastel() {
  "a soft yellow, as strong as the medium blue card and the green card so it stands beside them as one more card colour rather than as a warning";
  let c = "rgb(255, 236, 160)";
  return c;
}
