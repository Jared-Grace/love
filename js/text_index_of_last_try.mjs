import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function text_index_of_last_try(s, search) {
  "Where a piece of text sits inside a larger one counting from the end, or a number below zero when it is never there.";
  function_duplicate_kind_parallel();
  let r = s.lastIndexOf(search);
  return r;
}
