`Background for a code surface — dark, terminal-like. Shared by the ${fn_name("app_code")} /`;
`${fn_name("app_a")} dark code styling and the examples page command chip, so "this is code`;
"you run\" reads the same everywhere.";
export function app_shared_color_code_background() {
  let r = "black";
  return r;
}
