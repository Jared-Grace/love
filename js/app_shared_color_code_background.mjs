// Background for a code surface — dark, terminal-like. Shared by the app_code /
// app_a dark code styling and the examples page command chip, so "this is code
// you run" reads the same everywhere.
export function app_shared_color_code_background() {
  let r = "black";
  return r;
}
