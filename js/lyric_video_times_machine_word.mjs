import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_times_machine_word() {
  "What a timing document says in its times_from field when the moments in it were placed by a machine listening rather than by a person.";
  "★ IT IS WRITTEN DOWN BECAUSE IT CANNOT BE WORKED OUT AFTERWARDS. Where the times came from was read off their shape at first - a document still holding the flat spread it was drafted with had nobody's ear in it, and anything else did. That stopped being true the moment a machine was allowed to write times, because the machine writes them through the same function the tapping screen does, down to the same twentieth of a second between one line and the next. Twenty three documents a machine had just written then looked exactly like twenty three documents a person had sat through, and the guard meant to protect a person's work started protecting the machine's from being improved on.";
  "It says machine and not the name of the command that wrote it, because the field is a permission and not a signature. What the guard has to know is whether anybody would be sorry to lose these times, and a second machine writer would want the same answer as the first without the guard having to be taught its name.";
  arguments_assert(arguments, 0);
  let word = "machine";
  return word;
}
