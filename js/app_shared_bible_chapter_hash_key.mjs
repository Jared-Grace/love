export function app_shared_bible_chapter_hash_key() {
  "The word standing before the chapter in the link a bible page shows, so a link somebody saved or sent still opens the chapter it was saved on.";
  let v = "c";
  return v;
}
