export function ebible_offline_index_flat_name() {
  "the list of every place this bible has a verse is kept beside the chapters themselves, so opening a passage with no internet does not wait on the network to be told which verses exist";
  let name = "index_flat";
  return name;
}
