export function argument_alias_group_show() {
  "The name of the group of spellings for whether a result is put on the human's screen or printed.";
  "Two places say this word - the group's own row, and the lookup that reads it - so it is one fn rather than two literals that agree only by luck.";
  let group_name = "show";
  return group_name;
}
