export function commands_only_level_off() {
  "the setting the commands-only switch is turned to when it is not limiting anything";
  "spelled once here because two places need it and they must not be able to disagree: the reader hands it back when the file is missing, and the writer accepts it as the way back. A word typed twice is a word that can be typed differently once.";
  let v = "off";
  return v;
}
