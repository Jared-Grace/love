import { fn_name } from "./fn_name.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function gloss_word_sound_compression_level() {
  "How hard the mp3 of a single word button is squeezed, where nought is the best the encoder does and one is the smallest file it will make.";
  "★ A WORD BUTTON IS NOT SQUEEZED AT ALL, WHICH IS THE OPPOSITE OF WHAT A CHAPTER WANTS. A chapter reading is minutes long and there are a thousand of them, so the difference between the two settings is the difference between two gigabytes and three for a whole Bible in one voice, and that is worth paying for. A word is half a second, and the app's entire vocabulary at the best setting is under twenty megabytes - so there is nothing to buy, and something to lose.";
  "★ WHAT THE SQUEEZING TAKES FIRST IS THE BREATH OF A FRICATIVE - the hiss on an s, the puff on a th - because it is quiet and it is broad, which is exactly what an encoder is built to spend first. On a chapter that is a reading a person follows for the sense of it. On a button whose only job is to teach a learner what one word sounds like, the breath is the part they came for. Handed the same word at both settings with no label on either, a person picked the unsqueezed one.";
  ("This is a number rather than a literal at the call site for the same reason ",
    fn_name("gloss_word_sound_speed"),
    " is: the value is the small half and the reason is the large half, and a reader who wonders whether the buttons should be smaller needs the reason, not the number.");
  arguments_assert(arguments, 0);
  let r = 0;
  return r;
}
