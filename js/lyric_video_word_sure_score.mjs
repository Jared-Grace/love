import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function lyric_video_word_sure_score() {
  function_duplicate_kind_parallel();
  arguments_assert(arguments, 0);
  ("How sure the blind transcriber must be of a word before that word counts as evidence about the recording.");
  ("★ THE SCORE IS THE ONE THING THAT TELLS REAL SINGING FROM THE DECODER TALKING TO ITSELF, and the two do not overlap in the middle - they sit at opposite ends. Words actually sung come back between seven tenths and nearly one, each with its own moment. A decoder that has run out of audio and started looping comes back between zero and three tenths with the same moment stamped on a dozen words in a row: seventeen words of Psalm 134 all marked at 99.36 seconds. Read as text those seventeen words are three tidy repeats of the opening verse, and they were very nearly written into the document as such.");
  ("Half is chosen rather than a tighter number because the two populations are so far apart that anything between three and seven tenths separates them identically, and the looser end of that range is the one that cannot quietly drop a real word sung softly.");
  let score = 0.5;
  return score;
}
