import { arguments_assert } from "./arguments_assert.mjs";
export function functions_work_chain_flat_ceiling() {
  arguments_assert(arguments, 0);
  ("The deepest a chain of lines may run and still count the function flat - three.");
  ("Three is what naming a value, gathering it, and handing it back costs: the list is made, an entry goes into it, the list is returned. A function that only writes down what it knows never needs a fourth link, so three lets a table of any length through while a body that works something out and then works on the answer does not.");
  ("It is deliberately the smallest number that admits the plainest shape, rather than the largest that admits everything currently listed. An excuse is only worth having if a reader can say what it excuses without reading the list of who has it, and a bigger number buys quiet at the price of that sentence.");
  ("Measured on 2026-08-15 over the hundred and one functions above the size ceiling: five are three deep or less, none at all is four deep, and the next ones up are five. So the line falls in a gap rather than through a crowd, and moving it to four would forgive nobody.");
  let r = 3;
  return r;
}
