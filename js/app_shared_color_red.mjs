export function app_shared_color_red() {
  "the one red every app here uses - dark enough for white writing to sit on it and plainly not the green that means go ahead";
  let r = "#b91c1cff";
  return r;
}
