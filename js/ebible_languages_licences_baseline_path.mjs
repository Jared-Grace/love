export function ebible_languages_licences_baseline_path() {
  "Where the record of translations already offered on terms this repo may not ship is kept.";
  let p = "data/ebible_languages_licences_baseline.json";
  return p;
}
