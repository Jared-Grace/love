export function app_g_verify_column_max_width() {
  "the verify dashboard reads wider than the 40rem reading column: it lays a passage's English lines beside the words they draw from, so it needs room for two things side by side, not one comfortable line of prose";
  let v = "48em";
  return v;
}
