import { text_frozen } from "./text_frozen.mjs";
export function bible_glyph_chapters_built_bible_folder() {
  "The bible folder the built picture Bible chapters are uploaded under, beside every translation's own folder.";
  "It is frozen because it is an address a phone already asks for. A word that followed a rename would move where the page looks and leave every chapter already uploaded where nobody reads it.";
  let folder = text_frozen("glyph");
  return folder;
}
