import { arguments_assert } from "./arguments_assert.mjs";
export function bless_hash_family_first() {
  arguments_assert(arguments, 0);
  ("The word written after the hash mark to open the praying game onto the street with the");
  ("first household nearly finished - two of its three already prayed for, so one prayer");
  ("completes it.");
  ("It exists because the household celebration is the longest and the most fiddly thing on");
  ("this screen, and reaching it the ordinary way costs two prayers said carelessly before");
  ("the one being looked at. Two prayers of the wrong kind is exactly the price the door");
  ("skip was built to stop somebody paying.");
  ("Nearly finished rather than finished, because what is being checked is the FINISHING -");
  ("the camera moving, the ground turning, the flash. A world opened with the house already");
  ("gold would show the result and never the thing that produces it.");
  let word = "household_first";
  return word;
}
