export function language_code_key() {
  "The word under which a bible language keeps its short code, in the language list this repo ships and in every package a reader has downloaded for reading with no internet.";
  let v = "language_code";
  return v;
}
