export function bless_hash_map() {
  "The word written after the hash mark to open the praying game with the whole world on the screen at once.";
  "Named rather than spelled where it is read, for the reason every other opening word here is: an address has two ends, the page that reads it and the link that carries it, and a word spelled at both ends is a word a rename moves at one of them. The end left behind then names something nothing answers to, which is not an error - it is a page that quietly opens the ordinary way while the person who typed it believes otherwise.";
  "'map' rather than 'far' or 'out' because it says what you are given rather than where the camera went. What arrives is the street seen whole, and that is a map; how far back the camera had to stand to draw one is the answer and not the request.";
  let word = "map";
  return word;
}
