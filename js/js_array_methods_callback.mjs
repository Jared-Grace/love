export function js_array_methods_callback() {
  "the native array methods that hand their callback more than one argument - the index and the whole list follow the item, every time, whether or not the callback asked for them";
  "a new method joins the question by being added here, the same way an operator joins the auto pass by being added to its own list next door";
  let names = [
    "map",
    "filter",
    "forEach",
    "some",
    "every",
    "flatMap",
    "find",
    "findIndex",
    "findLast",
    "findLastIndex",
  ];
  return names;
}
