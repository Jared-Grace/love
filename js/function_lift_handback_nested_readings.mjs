export async function function_lift_handback_nested_readings(ast, nested) {
  arguments_assert(arguments, 2);
  ("Of the functions written inside one function, those whose body the move that hands its writes back would take, each with the reading that says what it must be given.");
  ("The third of the three, and the only one that reaches a piece writing to a name it reached out for. That one refusal is most of what the other two stop at, so this list is largely the complement of theirs rather than a narrower slice of it - a piece on this list is usually a piece neither of the others could take at all.");
  ("Why one would not go ahead is asked next door, so this list and the move itself cannot disagree about a single function. The walk is the same one the wider move's list takes; all this says is which judgement to ask.");
  let readings = await function_lift_nested_readings_unrefused(
    ast,
    nested,
    js_function_handback_refusals,
  );
  return readings;
}
