export function g_verses_hs_warning_name() {
  "the verse-pool name for the Holy-Spirit-warning verses — shared by the upload and the runtime download so both target the same batched file";
  let name = "hs_warning";
  return name;
}
