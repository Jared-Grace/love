export function app_code_note_name_tag_padding_x() {
  "the sideways room inside a name's coloured ground when that name is written on a cup: enough that the letter is not touching the edge of its own ground, and no more";
  "SET SEPARATELY FROM THE SHARED CODE PADDING, WHICH IS THE POINT OF HAVING IT. A run of code is a strip of lettering standing in open page, and the shared amount gives it room so the block reads as the width of what it says. A name on a cup is one letter standing inside a drawn cup, and the cup narrows towards its foot - the wall is curved nearly the whole way down. At the height the name is written, the shared amount put the ground out to the drawn wall, so the cup looked like it had been filled with the label rather than written on.";
  "Smaller than the shared amount rather than none at all. With no room the letter's strokes touch the edge of the colour, and the mark stops reading as a letter on a ground and starts reading as a coloured letter with a fringe.";
  let v = "0.2em";
  return v;
}
