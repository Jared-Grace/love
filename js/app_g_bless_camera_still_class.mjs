import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_bless_camera_still_class() {
  arguments_assert(arguments, 0);
  ("The class the map wears for the single frame in which it is resized: while it is on,");
  ("nothing inside the map slides anywhere, though a light on it may still fade.");
  ("Named in one place because it is spelled three times over - in the rule that says what");
  ("it does, in the moment it goes on, and in the moment it comes off - and a class whose");
  ("name is right in two of those three places and wrong in the third fails by simply not");
  ("happening, with nothing to report.");
  let r = "g_bless_camera_still";
  return r;
}
