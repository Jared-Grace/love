export function app_replace_screen_hash_key() {
  "The word standing before the screen in the link the replace app shows - published in every link anybody saved, so nothing here may reword it.";
  let v = "screen";
  return v;
}
