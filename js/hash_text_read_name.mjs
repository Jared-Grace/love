import { fn_name } from "./fn_name.mjs";
export function hash_text_read_name() {
  "The name of the one function that hands back the address of a page as the text it was written as.";
  "The twin of the one beside it that hands the address back as an object. Both are real ways in and a reading that knew only the object one would find nothing in a page that does its own parsing - which looks exactly like a page that reads no address at all.";
  let name = fn_name("html_hash_get");
  return name;
}
