import { bible_glyph_chapters_verse_marks_overdrawn_baseline_path } from "./bible_glyph_chapters_verse_marks_overdrawn_baseline_path.mjs";
import { baseline_known_growth_assert } from "./baseline_known_growth_assert.mjs";
export async function bible_glyph_chapters_verse_marks_overdrawn_baseline_growth_assert(
  known,
) {
  "Refuse to record an offender the baseline did not already hold. A ratchet that can be rewritten in both directions is not a ratchet, and the rewrite would be reached for at exactly the moment the gate went red, which is the moment it was doing its job.";
  "The first seeding has no file to compare against and is allowed, and so is any rewrite that only drops names.";
  let path = bible_glyph_chapters_verse_marks_overdrawn_baseline_path();
  await baseline_known_growth_assert(
    known,
    path,
    "recording these as known would bless a picture standing on a root the verse never held rather than repair it - open the verse in the interlinear, and either write the word out in plain English letters or say in the chapter's prose which of the three answers that are not faults this one is",
  );
}
