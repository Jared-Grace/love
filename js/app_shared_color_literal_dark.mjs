// Literal (string/number) color on a DARK code surface — a light salmon,
// readable on black (the light-theme app_shared_color_literal() orange dims).
export function app_shared_color_literal_dark() {
  let r = "#ce9178";
  return r;
}
