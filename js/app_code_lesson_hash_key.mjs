export function app_code_lesson_hash_key() {
  "The word standing before the lesson in the link the code app shows, so a link somebody saved or sent still opens on the lesson it was saved on.";
  let v = "lesson";
  return v;
}
