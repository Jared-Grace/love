export function text_slice_from(s, from) {
  "The rest of the text from this position onward — the twin of the one that keeps";
  "the front instead.";
  let sliced = s.slice(from);
  return sliced;
}
