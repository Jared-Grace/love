export function messages_firebase_path() {
  "The place in storage a person's own messages are kept under, written once here so every reader and writer of them spells the same opening.";
  let v = "user/uuid/";
  return v;
}
