export function number_largest() {
  "The largest whole number arithmetic here can be trusted with, for a limit that is meant not to bind.";
  "A limit is asked for in places where there is honestly no such thing - every item in the list is one the reader could have meant, so nothing is too far away to offer. Saying so with a number this size is plainer than growing a second way of asking that leaves the limit out, and it cannot be reached by anything being counted here.";
  let v = Number.MAX_SAFE_INTEGER;
  return v;
}
