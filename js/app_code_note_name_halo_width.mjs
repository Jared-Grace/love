export function app_code_note_name_halo_width() {
  "how far the darker ring reaches out from a name written on a cup";
  "TAKEN FROM THE LETTER'S OWN STROKE RATHER THAN PICKED BY EYE. The name is set bold, and a bold letter of this kind is drawn with strokes about a seventh of its height across. A ring half that is plainly there beside the stroke it is ringing and still narrower than the gaps inside the letter, so the hole in an a stays a hole.";
  "Going wider closes those gaps and the letter turns into a blob of the darker colour - which is the same thing as losing the letter, and worse than no ring, because a ring that wide has replaced the very shape it was added to make readable.";
  "Written in the same units the letter is sized in, so the ring keeps its share of the letter at every size. Fixed in pixels it would be a smudge on a large screen and a sliver on a small one, and the letter's readability would depend on the device.";
  let v = "0.07em";
  return v;
}
