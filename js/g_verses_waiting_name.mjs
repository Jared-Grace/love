export function g_verses_waiting_name() {
  "the verse-pool name for the waiting-on-the-Lord prayer verses — shared by the upload and the runtime download so both target the same batched file";
  let name = "waiting_on_the_lord";
  return name;
}
