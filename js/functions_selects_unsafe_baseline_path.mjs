import { path_join } from "./path_join.mjs";
import { data_given_baselines_folder } from "./data_given_baselines_folder.mjs";
export function functions_selects_unsafe_baseline_path() {
  "Where the ratchet keeps the transforms that can be handed written code today.";
  "Reading it and checking against it are separate functions, so the name of the";
  "file is spelled once here rather than once in each of them.";
  let path = path_join([
    data_given_baselines_folder(),
    "selects_unsafe_baseline.json",
  ]);
  return path;
}
