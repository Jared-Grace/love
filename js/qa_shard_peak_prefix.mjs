export function qa_shard_peak_prefix() {
  "What a share of a judging writes in front of the most memory it held, and what reading one back looks for";
  "The same shape as the marker in front of a complaining gate's name and the one in front of how long a gate was outstanding, and for the same reason: a share is asked in a process of its own, and the only thing it can hand back other than what it printed is a result it never reaches when it goes red. A marked line survives both ways, and this is the one number a red share is most worth being asked for - a share killed for want of room is red about nobody, so what it held on the way there is the whole of the evidence.";
  let v = "SHARE HELD  ";
  return v;
}
