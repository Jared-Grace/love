export function app_code_lesson_statement_name_value_names() {
  ("the two names the lessons on giving a value a name give values: the first is the one used while there is only ever one cup, and the second is the one that joins it on the screen where there are two");
  ("Which of the two is written out is not fixed. It was, while the two-cup screen was only imagined; a screen that always writes out the second one can be answered by taking the last word on it, so the questions swap the two over and the pair here is just the pair, in the order they are met.");
  ("Single letters, because a name that says something about what it holds would be a second thing to notice on a screen whose whole point is that a name holds a value at all.");
  let names = ["a", "b"];
  return names;
}
