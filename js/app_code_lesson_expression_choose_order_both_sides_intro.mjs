import { arguments_assert } from "./arguments_assert.mjs";
import { html_div_cycle_code } from "./html_div_cycle_code.mjs";
export function app_code_lesson_expression_choose_order_both_sides_intro(
  parent,
) {
  arguments_assert(arguments, 1);
  ("the one sentence that says what is different about this lesson, in a card of its own");
  ("It names the one thing that changed and nothing else. The learner already chooses a part and then chooses what it comes to, on lines of numbers and on lines of comparisons alike; the whole of what this screen has to add is that one line now holds both, so some parts come to a number and the last one comes to true or false.");
  html_div_cycle_code(parent, [
    "Now one line has both kinds of operator, so some parts come to a number and the last part comes to true or false",
  ]);
}
