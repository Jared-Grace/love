export function app_shared_column_max_width() {
  "the reading column cap: a mobile-first screen holds this width and centers instead of stretching full-bleed on a wide desktop, so the top bar, verse text, and buttons all share one width and line up. about the width of a full button row, and a comfortable line length for the body text";
  let r = "40rem";
  return r;
}
