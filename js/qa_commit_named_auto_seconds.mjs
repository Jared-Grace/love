import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function qa_commit_named_auto_seconds() {
  "How long the judging daemon waits between looks";
  "Short, because looking is nearly free - an answer already written down is one small file read, and a full machine is one look at the load. What the shortness buys is that judging starts within a minute of the machine having room for it, rather than within whatever the wait happens to be.";
  function_duplicate_kind_parallel();
  let seconds = 60;
  return seconds;
}
