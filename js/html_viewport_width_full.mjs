export function html_viewport_width_full() {
  "the full width of the window a screen is being shown in, as a CSS length - what a backdrop or an overlay spends to cover everything behind it";
  let v = "100vw";
  return v;
}
