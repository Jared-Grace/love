import { arguments_assert } from "./arguments_assert.mjs";
export function app_g_hero_evil_color() {
  arguments_assert(arguments, 0);
  ("Blood red, the colour of somebody who has killed. It is kept apart from the orange of the fire that stops them, so the two never read as one thing, and it is never gold, because gold belongs to God.");
  let color = "rgba(220, 0, 30, 1)";
  return color;
}
