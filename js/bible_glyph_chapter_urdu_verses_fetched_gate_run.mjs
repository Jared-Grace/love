import { arguments_assert } from "./arguments_assert.mjs";
import { fn_name } from "./fn_name.mjs";
import { bible_glyph_chapter_language_verses_fetched_gate_run_check } from "./bible_glyph_chapter_language_verses_fetched_gate_run_check.mjs";
import { bible_glyph_chapter_urdu_verses } from "./bible_glyph_chapter_urdu_verses.mjs";
import { bible_glyph_chapter_urdu_verses_fetched } from "./bible_glyph_chapter_urdu_verses_fetched.mjs";
export async function bible_glyph_chapter_urdu_verses_fetched_gate_run() {
  "Gate: the sent-for Urdu of every picture chapter is the Urdu the repo holds for it. Read-only.";
  arguments_assert(arguments, 0);
  ("THE WALK ITSELF IS NOT HERE. It is in ",
    fn_name("bible_glyph_chapter_language_verses_fetched_gate_run_check"),
    ", which is where the reasoning for sending rather than reading the source is written, and this names which pair to ask.");
  ("IT GUARDS A HOLE THAT WAS OPEN THE DAY THE URDU READERS WERE WRITTEN. The sending names its module in an address written out as text so a bundler can split a third of a megabyte off the page, and a written-out address is exactly what a rename does not follow. Nothing goes red when it breaks, because the address is only read when a reader opens the key.");
  ("NOTHING PUTS THE URDU ON A PAGE YET AND THAT IS NOT A REASON TO WAIT. The text is generated, credited and read from two ways in; the wiring that would show it is a decision still to be made, and a gate written now is the reason that decision can be made about words somebody can trust rather than about words nobody has checked.");
  let f_name = fn_name("bible_glyph_chapter_urdu_verses_fetched");
  let r = await bible_glyph_chapter_language_verses_fetched_gate_run_check(
    "Urdu",
    f_name,
    bible_glyph_chapter_urdu_verses,
    bible_glyph_chapter_urdu_verses_fetched,
  );
  return r;
}
