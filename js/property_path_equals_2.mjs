import { arguments_assert } from "./arguments_assert.mjs";
import { property_path_get_2 } from "./property_path_get_2.mjs";
import { equal } from "./equal.mjs";
export function property_path_equals_2(item, name_outer, name_inner, value) {
  arguments_assert(arguments, 4);
  ("Whether the value two names in is this one.");
  ("Two steps in and then a comparison is how a tree is asked what shape it is - is");
  ("the thing this statement says a run of backtick text, is the name this call");
  ("reaches the wrapper, is the function this step names the one running now. The");
  ("value two steps in is wanted for the comparison and for nothing else, so the");
  ("name given to it is invented for one line and read once.");
  let inner = property_path_get_2(item, name_outer, name_inner);
  let same = equal(inner, value);
  return same;
}
