import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_lesson_expression_choose_order_same_strength_pair_operator_count } from "./app_code_lesson_expression_choose_order_same_strength_pair_operator_count.mjs";
import { app_code_lesson_expression_choose_order_same_strength_above_generic } from "./app_code_lesson_expression_choose_order_same_strength_above_generic.mjs";
export function app_code_lesson_expression_choose_order_same_strength_pair_above(
  root,
  context,
) {
  arguments_assert(arguments, 2);
  ("what stands above the card: one two-operator line of this lesson's own kind taken all the way down, and nothing else");
  ("Laid out by the one that lays out both same-strength pressing lessons, handed this lesson's count. What to leave off such a screen - the card naming the stronger operators, a closing sentence, a heading - was reasoned once there and holds here for the same reasons.");
  let count =
    app_code_lesson_expression_choose_order_same_strength_pair_operator_count();
  app_code_lesson_expression_choose_order_same_strength_above_generic(
    root,
    count,
  );
}
