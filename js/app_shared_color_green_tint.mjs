export function app_shared_color_green_tint() {
  "a green with almost all of it taken out - green mixed so far into white that it reads as white with a cast to it rather than as a colour. It is a value, not a meaning; what it is for is said by whoever asks for it.";
  "It sits below the light green rather than beside it, and the gap between them is the whole reason it exists. A light green is a colour a thing IS; this is a colour a thing is STANDING ON. Writing laid over the light one is writing on a green rectangle and has to fight it; laid over this one it is still writing on the page, with the page faintly saying which lines belong together.";
  let v = "#e6ffed";
  return v;
}
