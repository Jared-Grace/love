export function app_shared_bible_mode_apart_maximum() {
  "How far a mistyped way of reading may be from a real one and still be worth offering back as a guess.";
  "Two, because there are only ever two words to choose between and they are nothing like each other. Nothing arbitrary can be reached by widening the search when the whole search is two words long, so the limit is here to refuse a word that means something else entirely rather than to keep a list short.";
  let v = 2;
  return v;
}
