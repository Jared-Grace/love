export function functions_search_shown_count() {
  "How many names a name search shows before it stops and says how many more there were.";
  "Thirty because that is the largest number the log actually asked for. Fourteen days of commands cut this answer down by hand at ten, fourteen, twenty, thirty and forty, so a number below thirty would leave most of those cuts still being typed, and forty was asked for once.";
  let count = 30;
  return count;
}
