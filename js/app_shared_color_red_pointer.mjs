import { arguments_assert } from "./arguments_assert.mjs";
import { color_oklch } from "./color_oklch.mjs";
export function app_shared_color_red_pointer() {
  arguments_assert(arguments, 0);
  ("a crimson light enough to be read against a black code line, for marking a third thing a reader is being sent to look at");
  ("THE HUE IS 15 DEGREES AND IT WAS PICKED BY THE EYE AFTER THE ARITHMETIC GOT IT WRONG. The hue equally far from the blue near 264 degrees and the green at 150 is 340, and 340 read as another kind of blue - which is worse than no colour at all, because it says two things are related when the whole job is to say they are different. Equal degrees on the wheel are not equal to the eye, and the split the eye makes here is warm against cool.");
  ("A crimson rather than a pure red. The pure reds in this repo mean a wrong answer and land on a whole button or a whole block after something has been pressed; a colour covering that much of the page after a press reads as a verdict. This lands inside a line nobody has pressed yet, so it has nothing to answer and is read as a place to look.");
  ("It measures 3.89 to 1 against the black of a code line and 5.40 against the white the line is written in, so it stands where the other two stand and none of the three is the faint one. A set where one is dimmer says that one matters less, and these are three places to look, not a ranking.");
  ("IT IS THE ONE OF THE THREE THAT GIVES WAY FOR A READER WHO CANNOT TELL RED FROM GREEN. Blue against green is the pair that survives that reader and it is the pair that carries anything which must never be confused; this may collapse into the green. Whatever wears this colour must therefore be told apart by its shape as well, and a screen wanting three marks of the same shape has no business using it.");
  ("It sits six hundredths of lightness from the red a filled chip is painted, which is a near miss by this repo's own colour rule and is left standing, for the same reason as its green neighbour.");
  let color = color_oklch(0.55, 0.2, 15);
  return color;
}
