export function g_times() {
  "the four times of day in order — the world advances through these as the player ministers (each unbeliever conversation part steps one along, morning→noon→afternoon→night, then wraps)";
  let times = ["morning", "noon", "afternoon", "night"];
  return times;
}
