export function g_times() {
  "the times of day in order — the world advances through these as the player ministers (each unbeliever conversation part steps one along, morning→noon→afternoon→sunset→night, then wraps). sunset is a PROTOTYPE keyframe under evaluation";
  let times = ["morning", "noon", "afternoon", "sunset", "night"];
  return times;
}
