import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_lesson_expression_choose_order_same_strength_operator_count() {
  arguments_assert(arguments, 0);
  ("how many operators a line of this lesson carries, which is also how many steps it takes to solve: 3");
  ("The telling above the card walks a line of this lesson's own kind all the way down, and the quiz hands out lines of the same kind. Written out at both addresses the two could drift, and the run a learner reads would then stop short of the line they are given.");
  ("Three, and the third step is the whole of what this lesson adds. Every line before it has taken two, and every operator on this one is the same strength as the others, so a learner meets the extra step with nothing else on the line changed.");
  let count = 3;
  return count;
}
