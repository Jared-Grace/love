export function app_g_scripture_separator_font_size() {
  "the size of the small book mark drawn between a reference and the verse it points at, under the words on either side so it separates them without interrupting them";
  let v = "0.8em";
  return v;
}
