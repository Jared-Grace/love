import { arguments_assert } from "./arguments_assert.mjs";
export function html_color_blue_on_dark() {
  "the blue that stays readable on a near-black page - a link to tap, and the light the loading spinner is made of";
  "The spinner had this value to itself and named it after the spinner, so the first link written on a dark page reached for a blue of its own and landed two hundredths away from it. Two blues that near read as one, and which of them a reader met depended only on which part of the page they were looking at.";
  arguments_assert(arguments, 0);
  let c = "#8cb4ff";
  return c;
}
