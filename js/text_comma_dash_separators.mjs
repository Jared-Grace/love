export function text_comma_dash_separators() {
  "the two ways a set of numbers is written down in this repo: a comma list and a dash range - so a reader of either splits on both at once";
  let separators = [",", "-"];
  return separators;
}
