export function app_code_placeholder_color() {
  "a muted light grey for a PLACEHOLDER that stands in for real code but is not real - like the ... inside a shown string shape. Grey (no hue) and dimmer than the white code text, so it reads as 'fill this in yourself', clearly distinct from the coloured value and from the literal quotes";
  let r = "#9aa0a6";
  return r;
}
