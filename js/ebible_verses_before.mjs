export function ebible_verses_before() {
  "The verse number anything standing before a chapter's first numbered verse is filed under - a title or a heading is in the book, so it is kept rather than dropped, and nought is the number no verse of scripture answers to.";
  let v = "0";
  return v;
}
