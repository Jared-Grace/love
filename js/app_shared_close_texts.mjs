export function app_shared_close_texts() {
  "the word on the button that puts a panel away, written out in every language an app here speaks to its reader in";
  "it is the way back out of anything laid over the app, so it is the word a reader most needs to be able to read without knowing any english. somebody who cannot read it is shut in.";
  "the words are kept apart from the asking for them because there are two moments this word is needed and only one of them can ask who is reading. inside a running app the reader is known; on the notice that comes up when the app never started there is no app to ask, so the language is handed in as the page is built. one table serves both, and neither can drift into saying close in a way the other does not.";
  let r = {
    en: "Close",
    ur: "بند کریں",
    translated_from: {
      ur: "Close",
    },
  };
  return r;
}
