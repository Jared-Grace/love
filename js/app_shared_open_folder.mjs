import { text_frozen } from "./text_frozen.mjs";
export function app_shared_open_folder() {
  "the folder the daily opens sit in, one step inside the place a person's own uploads are kept";
  "Inside that opening because it is the only place in storage a browser is allowed to write at all. A folder of its own beside the error reports, because a count of who opened an app and a list of what broke are read by different questions.";
  let folder = text_frozen("open/");
  return folder;
}
