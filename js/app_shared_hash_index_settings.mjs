import { arguments_assert } from "./arguments_assert.mjs";
export function app_shared_hash_index_settings(app_fn, new_tab) {
  arguments_assert(arguments, 2);
  ("the two things an app decides about its own directory of hash routes: WHOSE session storage remembers which categories are drilled open, and whether picking a screen LEAVES the list or opens it beside the list.");
  ("they travel together because they travel the same way - down through the tree to every card - and a tree is drawn by a function that calls itself, so each one added on its own is another word to carry through every level and another place to drop it.");
  ("whether to open a new tab is a real disagreement rather than a preference. A game's dev route REPLACES the game, and coming back is what the back-link is for. A sandbox preview is one of a list you are working through, so leaving the list to see one and having to find your way back to it is the wrong way round.");
  let r = {
    app_fn,
    new_tab,
  };
  return r;
}
