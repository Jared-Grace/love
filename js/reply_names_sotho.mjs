import { arguments_assert } from "./arguments_assert.mjs";
export function reply_names_sotho() {
  arguments_assert(arguments, 0);
  ("Given names in Sotho and Tswana, spoken in southern Africa.");
  let names = ["lerato", "thabo"];
  return names;
}
