export function app_bible_picker_standard_pad_x() {
  "the side padding of a bible picker button at its standard size - the value happens to equal the shared label font size and means nothing like it, so it is named here rather than routed through that";
  let v = "0.85em";
  return v;
}
