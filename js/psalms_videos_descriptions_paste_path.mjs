import { folder_gitignore_join } from "./folder_gitignore_join.mjs";
export function psalms_videos_descriptions_paste_path() {
  "Where the piece being worked on right now waits to be copied out of.";
  "One place rather than one per piece, because the pieces are done one at a time and a folder of eight files, some already pasted and some not, is a folder nobody can tell the state of by looking. What is here is what has not been pasted yet, or was, and either way what is here is the last one asked for.";
  let path = folder_gitignore_join("psalms_videos_descriptions_paste.js");
  return path;
}
