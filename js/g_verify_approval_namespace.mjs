import { text_frozen } from "./text_frozen.mjs";
export function g_verify_approval_namespace() {
  "The word a chapter's approvals are uploaded under, which is the folder they are already sitting in on the shared bucket.";
  "Frozen for the same reason as its neighbours, and shaped like a function name while naming none - the files are on a bucket nothing here can move, so the word may not move either.";
  let v = text_frozen("g_verify_approval");
  return v;
}
