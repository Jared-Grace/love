import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { app_shared_game_npc_property_get_null_generic } from "./app_shared_game_npc_property_get_null_generic.mjs";
export function app_shared_game_npc_pointer_get(npc) {
  function_duplicate_kind_parallel();
  ("The nodding arrow drawn over a person's head to say they are one of the ones to pray for next, found by WHO that person is - the same lookup their picture, their light and their ring use, and keyed the same way so all of them are found and moved together.");
  ("It is a mark ABOVE the crowd where the ring is a mark under their feet, and both are wanted. A street is drawn from overhead and packed close, so a ring at the feet of somebody standing behind two other people is hidden by the two of them; an arrow at head height is the one place on a person that nothing in front of them covers up.");
  ("Filed rather than drawn again each time the screen is painted, for the reason the light and the ring are: a mark placed from a person's tile lands on the square a walking person is heading FOR rather than the one they are crossing, so it runs on ahead of them and stands waiting there. Made before anybody has taken a step, it is carried along by their steps instead.");
  ("Nothing is the ordinary answer here rather than a fault, because most games draw people who are not in a prayer game at all and have never been given one.");
  let pointer = app_shared_game_npc_property_get_null_generic(
    app_shared_game_npc_pointer_get,
    npc,
  );
  return pointer;
}
