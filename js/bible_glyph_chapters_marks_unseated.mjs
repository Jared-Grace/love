import { arguments_assert } from "./arguments_assert.mjs";
import { bible_glyph_chapters_marks_unseated_walked } from "./bible_glyph_chapters_marks_unseated_walked.mjs";
import { property_get } from "./property_get.mjs";
export async function bible_glyph_chapters_marks_unseated() {
  arguments_assert(arguments, 0);
  ("Every mark an authored picture Bible chapter draws in a chapter where the root table seats it NOT ONCE, each one saying which chapter, which mark, and how many times it was drawn.");
  ("THIS IS THE ONE A PERSON OPENS. It is the reading next door with the count of how much was reached left behind, because the count answers a question only a gate has - whether the sweep went and looked - and a person repairing one of these already knows it ran, because they are holding its answer.");
  ("What is left is the whole of a repair: the chapter to open, the picture to look for, and how many times the same mistake was made in that chapter.");
  let told = await bible_glyph_chapters_marks_unseated_walked();
  let offenders = property_get(told, "offenders");
  return offenders;
}
