import { arguments_assert } from "./arguments_assert.mjs";
import { html_display_inline } from "./html_display_inline.mjs";
import { html_style_padding_x } from "./html_style_padding_x.mjs";
export function app_code_expression_chosen_room_clear(node_span) {
  arguments_assert(arguments, 1);
  ("give back the shape and the room the colouring needed, so the block stands in the line as plain lettering again");
  ("Kept apart from the colour it was holding, because the two are wanted one after the other rather than together: the room going is what the rest of the line closes up by, and a line closing up while there is still colour in it reads as the colour being squeezed out instead of as it having finished saying what it was for.");
  html_display_inline(node_span);
  html_style_padding_x(node_span, "0");
}
