import { fn_name } from "./fn_name.mjs";
export function firebase_folder_seams() {
  "Every call that takes a folder on the shared bucket, kept beside which of its arguments the folder is.";
  "A folder there is a place files are already sitting in, and nothing in this repo can move them. So a word that reaches one of these slots and is written into the source is a word that has escaped, and the only safe home for it is a function the frozen record is watching.";
  "Held by hand rather than read off a shape, because being a bucket folder is a fact about what the address means, and nothing in the way an argument is written says it. The address builder underneath them all is here in its own right, so a call that reaches straight past the wrappers is still asked about.";
  "The number beside each name is which argument the folder is, and it is not the same for all of them - the json one takes the file first and the folder second. That is exactly the kind of difference a reading keyed on the first argument gets silently wrong, so it is written down rather than assumed.";
  let f_name = fn_name("firebase_deploy_function_destination");
  let f_name2 = fn_name("firebase_chapter_upload_path");
  let f_name3 = fn_name("firebase_deploy_function_destination_json");
  let seams = [
    {
      builder: f_name,
      index: 0,
    },
    {
      builder: f_name2,
      index: 0,
    },
    {
      builder: f_name3,
      index: 1,
    },
  ];
  return seams;
}
