import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
export function color_near_miss_threshold() {
  function_duplicate_kind_parallel();
  ("how close two colours may sit before they read as the same colour spelled twice: the largest single channel difference, out of 255, that still counts as a near miss. Sixteen is about where a side-by-side pair stops being tellable apart on an ordinary screen.");
  ("A PAIR UNDER THIS NUMBER IS A QUESTION AND NOT A VERDICT. It used to say here that anything under it was either one decision written twice or a distinction nobody could see, and that second half was false. What sixteen measures is two patches held side by side, which is the hardest way there is to tell two colours apart. An EDGE between them is the easiest, and a boundary a reader picks out at a glance sits well inside the distance at which those same two colours look identical as blocks. So a gap doing no work and a gap drawing an edge arrive here as the same number, and only somebody who knows which element sits on which can tell them apart.");
  ("The picker for the hymn video draws its frame at #000000 on a page of #0b0b0b, eleven apart. Read as two patches that is one colour spelled twice; read as what it is, it is the only thing saying where the video cut ends - which matters most for a drawn emblem on black, having no edge of its own to show. Collapsing it would have satisfied this gate and taken the boundary with it, and nothing afterwards would have said so.");
  ("So a near miss has two right answers rather than one. Where the two are the same decision made twice, collapse them - that is the common case and what the gate is for. Where the gap draws an edge, record it: ");
  (fn_name("color_near_miss_baseline_add"), " takes the pairs by name, so the ramp is blessed while every other near miss stays red.");
  let n = 16;
  return n;
}
