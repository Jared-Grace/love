import { text_frozen } from "./text_frozen.mjs";
export function g_objection_generate_upload_namespace() {
  "The word generated objections are uploaded under, which is the folder they are already sitting in on the shared bucket.";
  "Frozen for the same reason as its neighbours: the files are on a disk nothing here can move, and a shipped page builds this same address to read them, so the word may not follow a rename of the function that writes it.";
  let v = text_frozen("g_objection_generate_upload");
  return v;
}
