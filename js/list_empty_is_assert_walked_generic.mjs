import { arguments_assert } from "./arguments_assert.mjs";
import { list_empty_is_assert_json } from "./list_empty_is_assert_json.mjs";
export function list_empty_is_assert_walked_generic(walked, offenders, hint) {
  "Refuse a sweep's offenders outright and hand back how much the sweep reached.";
  "The ratchets beside this one carry a record of what the repo already had wrong, because clearing that was a bigger job than the gate. A fault found before anything in the repo has it needs no such record - there is nothing to grandfather, and a baseline seeded at nothing is a file that exists only to say so.";
  "How much was walked still travels out, and for the same reason it does there. Nothing wrong is what this says on a good day and also what it says on the day its sweep stopped visiting anything, and the walk is the only part of the answer that falls in the second case.";
  "The count is asked for rather than worked out here. The one number this could reach on its own is the length of the offenders, which is nothing on every passing run by construction, so working it out here would be writing the silence it exists to break.";
  arguments_assert(arguments, 3);
  list_empty_is_assert_json(offenders, {
    hint,
  });
  let r = {
    walked,
  };
  return r;
}
