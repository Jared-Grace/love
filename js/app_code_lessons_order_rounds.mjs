import { function_duplicate_kind_parallel } from "./function_duplicate_kind_parallel.mjs";
import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_lessons_order_rounds() {
  function_duplicate_kind_parallel();
  arguments_assert(arguments, 0);
  ("How many times each lesson is asked for a line when the course order is being measured.");
  ("A lesson hands out a line built fresh each time, so the hardest line it can reach is found by asking it more than once and keeping the worst answer. One ask would report whatever the first draw happened to be, and a lesson that only sometimes reaches three operators would read as a two-operator lesson on most runs and a three-operator one on the rest.");
  ("Two hundred, because the gate that reads this has to give the same answer twice running or it is worse than nothing. Eight whole runs at two hundred were compared and every one named the same seventeen lessons, and two runs at five hundred named those seventeen as well, so two hundred is not merely a plateau short of the true answer.");
  ("Six stood here until 2026-09-16 and had been earned the same way, against the measure as it read then. The measure changed underneath it - lines shown only to be matched stopped being priced as lines to be solved, and a lesson stopped being blamed by a later one on a different topic - and the number of lessons standing out of place fell from ninety-eight to seventeen. What was left was almost all near the line, where one lucky draw flips a verdict, and at six the answer wobbled between seventeen and eighteen from one run to the next. A number earned against one measure is not earned against the next one; it has to be won again.");
  ("More asks is the safe direction and not merely the cautious one. Each ask can only raise the hardest line a lesson is known to reach, never lower it, so a lesson wrongly called simple is corrected by asking again and a lesson wrongly called hard cannot be invented by it.");
  ("Said here rather than at each caller, because the reading and the gate that judges the reading have to ask in the same breath. Two numbers would mean the gate judging a course it had measured differently from the report a person read before changing it.");
  let n = 200;
  return n;
}
