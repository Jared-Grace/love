import { arguments_assert } from "./arguments_assert.mjs";
import { html_style_padding_bottom } from "./html_style_padding_bottom.mjs";
export function app_code_head_flush_above_code(head) {
  arguments_assert(arguments, 1);
  ("take back the room the words at the top of a card stand off by, for as long as what ends them is a button rather than a line of words");
  ("A button already carries room of its own underneath it, so the gap left for words on top of that is more room than either of them asked for. Taken back, the button ends up standing a little closer to the Code label than the words do - which is what a button wants, being a thing to press rather than a thing to read on the way down the card.");
  let none = "0px";
  html_style_padding_bottom(head, none);
}
