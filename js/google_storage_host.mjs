export function google_storage_host() {
  "The name the same file store answers to through the cloud's own storage service, which is what serves a public file and what answers a listing.";
  let r = "storage.googleapis.com";
  return r;
}
