import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_button_replace_text() {
  arguments_assert(arguments, 0);
  ("what the button that swaps a worked-out operator for its value is called");
  ("Named in one place because the lesson both writes the button and tells the learner to press it by name, and a button called one thing and asked for by another leaves the learner hunting.");
  let text = "replace";
  return text;
}
