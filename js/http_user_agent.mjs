export function http_user_agent() {
  "The name this repo gives when it asks another machine for something.";
  "SAYING WHO IS ASKING IS THE POLITE HALF OF ASKING, and on some services it is the required half - they refuse a request that names nobody, because an unnamed caller cannot be told apart from an unattended one and cannot be contacted if it misbehaves. The refusal reads as the address being wrong, which is a long way from the truth.";
  "It names the project rather than pretending to be a browser. Pretending would work everywhere and would be a lie told to every machine asked, and there is no need for it: what this repo fetches is public files that anyone may have.";
  let name = "love (https://github.com/Jared-Grace/love)";
  return name;
}
