export function bible_word_voice_slow_trial_speeds() {
  "The speeds the Greek slow trial is heard at, as a share of the ordinary speed, so a listener can say which is slow enough.";
  "0.6 is the speed the Urdu reader's English uses. The listener heard it on Greek and said it sounded good but could be even slower, so two slower ones sit beside it.";
  let speeds = [0.6, 0.5, 0.4];
  return speeds;
}
