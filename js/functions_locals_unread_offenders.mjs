import { functions_locals_unread } from "./functions_locals_unread.mjs";
import { property_get } from "./property_get.mjs";
export async function functions_locals_unread_offenders() {
  "Just the functions binding a name nothing goes on to read, without the count of how many were walked to find them.";
  "The sweep answers with the offenders and the size of the walk together, because a reader of the report needs both to tell a clean run from a blind one. The driver that repairs a few at a time wants a plain list and asks each entry for its name, so handed the pair it asks an object for a property it has not got and stops before repairing anything.";
  "That is what it had been doing. The repair for this family threw on its first line every time it was run, named nothing, changed nothing, and still came back through the dispatcher looking like an answer - so the five standing offences read as unfixable when nothing had ever been offered to the fix.";
  "Written as its own name rather than by making the sweep answer with a bare list, because the count is wanted where it is wanted and dropping it would move this same problem next door.";
  let swept = await functions_locals_unread();
  let offenders = property_get(swept, "offenders");
  return offenders;
}
