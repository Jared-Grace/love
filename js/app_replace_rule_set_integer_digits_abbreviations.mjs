export function app_replace_rule_set_integer_digits_abbreviations() {
  "this exercise's rules grow an integer under the bare name g, and the later integers one does the same job under ig - so the words stay the words and only the letter they are shortened to moves";
  let r = {
    g: ["integer ", "g", "rower"],
  };
  return r;
}
