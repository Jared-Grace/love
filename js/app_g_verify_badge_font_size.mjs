import { app_shared_font_size_label } from "./app_shared_font_size_label.mjs";
export function app_g_verify_badge_font_size() {
  "the size of a small note beside a passage saying it has been looked at, quiet enough to be read only when looked for";
  let v = app_shared_font_size_label();
  return v;
}
