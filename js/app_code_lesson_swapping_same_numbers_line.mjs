import { arguments_assert } from "./arguments_assert.mjs";
import { html_div_cycle_code } from "./html_div_cycle_code.mjs";
export function app_code_lesson_swapping_same_numbers_line(parent) {
  "the one sentence every swapping lesson ends its cannot-swap half on: the exception. Subtraction, division and the ordering comparisons all flip when the two numbers differ - and all four lessons then have to say that two numbers which are already the same are the case where swapping changes nothing.";
  "it says because swapping them leaves the same two numbers rather than because both numbers are the same before and after: the old wording named the two numbers as the thing that stayed the same, which is true of every swap ever made and so explained nothing. What is particular here is that the pair is unchanged, not merely present.";
  arguments_assert(arguments, 1);
  html_div_cycle_code(parent, [
    "However, we can always swap two numbers that are the same, because swapping them leaves the same two numbers",
  ]);
}
