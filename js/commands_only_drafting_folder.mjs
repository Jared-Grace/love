export function commands_only_drafting_folder() {
  "the one place inside the repo the commands-only switch still lets an editing tool write, where a new atom is drafted before a command promotes it";
  let v = "scripts/temp/";
  return v;
}
