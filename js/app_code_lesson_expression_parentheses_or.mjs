import { arguments_assert } from "./arguments_assert.mjs";
import { app_code_lesson_expression_parentheses_or_title_name_id } from "./app_code_lesson_expression_parentheses_or_title_name_id.mjs";
import { app_code_lesson_expression_parentheses_value_generic } from "./app_code_lesson_expression_parentheses_value_generic.mjs";
import { app_code_lesson_expression_parentheses_or_intro } from "./app_code_lesson_expression_parentheses_or_intro.mjs";
import { app_code_lesson_expression_parentheses_or_value_expression } from "./app_code_lesson_expression_parentheses_or_value_expression.mjs";
export function app_code_lesson_expression_parentheses_or() {
  arguments_assert(arguments, 0);
  ("the whole line answered in one go: false && (true || true) asked for its value, with nothing to press");
  ("The twin of the lesson that walks the same line a press at a time, and the one the run was missing.");
  ("Its true line is drawn by the very same maker that pressing lesson draws its lines with, which is what makes the two lessons one family met twice.");
  ("Its FALSE line is not, and that is the whole of what its own maker is for. Asked for from the pressing lesson's maker like the other one, four lines in sixteen were lines the parentheses changed and they came up one draw in ten - so nine questions in ten could be answered by reading past the marks this lesson is about and giving the && its order against the ||, which is exactly what an earlier lesson teaches. A student working through said this one duplicated what they had already done, and they were reading it right.");
  ("That used to say the lesson before it. The press-at-a-time twin was put in between, so the screen immediately behind this one is that twin rather than the lesson being duplicated. Named by what it teaches rather than by a place. Corrected 2026-09-10.");
  ("The false line carries the point because it is the only one that can: parentheses round a || under an && can only ever make a line harder to reach true, so wherever they change anything they change it to false.");
  let name_id = app_code_lesson_expression_parentheses_or_title_name_id();
  let lesson = app_code_lesson_expression_parentheses_value_generic(
    name_id,
    app_code_lesson_expression_parentheses_or_intro,
    app_code_lesson_expression_parentheses_or_value_expression,
  );
  return lesson;
}
