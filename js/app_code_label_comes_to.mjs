export function app_code_label_comes_to() {
  "what a step-at-a-time quiz asks once an operator has been chosen and its value is what is wanted: choose what it comes to";
  "It replaces the asking for an operator rather than standing beside it, because at that moment there is nothing on the line left to choose - the operator is chosen and lit, and the only thing being waited for is its value.";
  "It says 'it' rather than naming the part, because the part is standing blue on the line directly above these very buttons; writing the part out again would be the same thing said twice, once in code and once in words.";
  let r = "Choose what it comes to: ";
  return r;
}
