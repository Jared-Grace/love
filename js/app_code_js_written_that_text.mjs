import { arguments_assert } from "./arguments_assert.mjs";
export function app_code_js_written_that_text() {
  arguments_assert(arguments, 0);
  ("the line that opens a program box after a cup story has been told in pictures: what follows is the same story, written in JS");
  ("Said by one function because two lessons open their program box with it, and the two had to be reworded together.");
  let text = "In JS, here's how we write that:";
  return text;
}
