// Syntax color for language keywords (export, function, return, let, ...).
// Shared by the app_a structural editor and the examples reading page so both
// render code in one visual grammar.
export function app_shared_color_keyword() {
  let r = "purple";
  return r;
}
