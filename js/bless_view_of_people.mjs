export function bless_view_of_people(people) {
  "A view of exactly these people - what a renderer hands back once it has worked out";
  "for itself who the player can see, and what the prayer game's brain is developed";
  "against before any renderer exists at all.";
  let view = { people: people };
  return view;
}
