export function app_shared_bible_languages_offered_key() {
  "Where an app writes the languages it is willing to put in front of a reader, on the one object every screen of that app is already handed.";
  "It is spelled once here because two sides read it and neither is near the other: the app says it at its own entry, and a shared screen asks for it several files away. A word spelled twice is a word that can be spelled differently once.";
  let r = "languages_offered";
  return r;
}
