export function app_original_bible_gloss_generate_upload_namespace() {
  "The word the glosses of the original languages are uploaded under, which is the folder they are already sitting in on the shared bucket.";
  "Frozen for the same reason as its neighbour, and spelling a function for the same reason - that function wrote the files, and a rename of it must leave them where they are.";
  let v = text_frozen("app_original_bible_gloss_generate_upload");
  return v;
}
