export function bible_glyph_roots_collisions_baseline_path() {
  "Where this ratchet keeps what the alphabet already carried. Reading it, rewriting it, and refusing to grow it are three separate functions, so the file name is spelled once here rather than once in each of them.";
  let path = "data/given/baselines/bible_glyph_roots_collisions_baseline.json";
  return path;
}
