export function ebible_offline_chapter_codes_name() {
  "the list of which chapters exist is kept beside the chapters themselves, so turning the page works with no internet";
  let name = "chapter_codes";
  return name;
}
