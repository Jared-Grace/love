import { property_transform } from "../../../love/public/src/property_transform.mjs";
export function list_transform_at(multiplied, multiplied_index, lambda) {
  property_transform(multiplied, multiplied_index, lambda);
}
