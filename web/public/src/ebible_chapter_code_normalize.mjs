import { text_upper_to } from "../../../love/public/src/text_upper_to.mjs";
export function ebible_chapter_code_normalize(chapter_code) {
  "consider doing more like mk mark";
  let u = text_upper_to(chapter_code);
  return u;
}
