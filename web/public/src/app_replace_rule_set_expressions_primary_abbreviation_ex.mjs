import { object_merge } from "../../../love/public/src/object_merge.mjs";
export function app_replace_rule_set_expressions_primary_abbreviation_ex(
  abbreviations,
) {
  object_merge(abbreviations, {
    ex: ["", "ex", "pression"],
  });
}
