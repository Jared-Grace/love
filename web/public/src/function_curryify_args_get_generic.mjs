import { list_slice_from } from "../../../love/public/src/list_slice_from.mjs";
export function function_curryify_args_get_generic(fn, arg_names) {
  let fn_new_result_args = fn(arg_names);
  let v2 = list_slice_from(list, item_from, item_to);
  let fn_new_args = [first];
  let r = {
    fn_new_result_args,
    fn_new_args,
  };
  return r;
}
