import { app_code_lesson_last_is } from "../../../love/public/src/app_code_lesson_last_is.mjs";
export function app_code_lesson_current_last_is(context, lesson_id) {
  return app_code_lesson_last_is(context, lesson_id);
}
