import { function_identifier_replace } from "./function_identifier_replace.mjs";
export async function function_identifier_replace_suffix(
  identifier_name,
  replacement,
) {
  return await function_identifier_replace(identifier_name, replacement);
}
