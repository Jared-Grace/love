import { list_adder_each } from "../../../love/public/src/list_adder_each.mjs";
export function list_map_cycle(list, mappers) {
  function lambda(la, item) {}
  let r = list_adder_each(list2, lambda);
}
