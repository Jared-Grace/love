import { marker } from "../../../love/public/src/marker.mjs";
export function g_themes_verb(root) {
  marker("1");
  let r = [root, root + "s", root + "ing"];
  let v = "tell tells telling";
  return v;
}
